# CLAUDE.md

증권 포털 프로젝트. 국내 주식 자동매매·정보수집·시장분석을 하나의 화면에서 관리한다.

## 문서

작업 전 아래를 확인한다.

| 문서 | 내용 |
|---|---|
| `docs/PROJECT.md` | 전체 구조, 설계 원칙 |
| `docs/SCHEMA.md` | DB 테이블 정의 |
| `docs/INTERFACES.md` | 모듈 인터페이스 규격 |
| `docs/ROADMAP.md` | 작업 순서, 현재 단계 |

**설계를 임의로 바꾸지 않는다.** 문서와 다르게 만들어야 할 이유를 발견하면
코드를 먼저 쓰지 말고 사용자에게 알리고 확인을 받는다.

---

## 절대 규칙

아래를 어기는 코드는 작성하지 않는다.

### 1. 파라미터 하드코딩 금지

손절폭, 익절폭, 진입 금액, 임계값 등 숫자를 코드에 직접 쓰지 않는다.
전부 `config/`에서 로드한다.

```python
# 금지
if profit_rate > 0.03:

# 올바름
if profit_rate > ctx.params["take_profit"]:
```

**파라미터를 변경하는 API나 화면 요소를 만들지 않는다.** 표시만 한다.

### 2. `datetime.now()` 직접 호출 금지

전략과 피드 구현체에서는 `feed.now()`를 쓴다.
직접 호출하면 백테스트가 미래를 참조하게 된다.

시스템 레벨 로깅·heartbeat 등 전략과 무관한 코드는 예외.

### 3. 주문 자동 재시도 금지

주문 응답을 받지 못했을 때 재시도하지 않는다.
`get_order_status`로 실제 접수 여부를 확인한 뒤 판단한다.
`client_order_id`는 주문 전에 DB에 기록한다. (`INTERFACES.md` 2.1)

### 4. 금액에 `float` 사용 금지

`Decimal`을 쓴다. 누적 오차가 손익 계산을 틀리게 만든다.

### 5. 시간 저장 규칙

- 시각: `TIMESTAMPTZ`, **UTC 저장**
- 거래일: `DATE`, 시장 현지 기준

혼용하지 않는다. 거래일을 UTC로 변환하지 않는다.

### 6. 종목 식별자

`stock_id` 형식은 `{market}:{code}` (예: `KRX:005930`).
종목코드만 단독으로 키로 쓰지 않는다.

### 7. 비밀값 커밋 금지

API 키, 계좌번호, 토큰은 `.env`에 두고 git에 올리지 않는다.
코드나 문서에 실제 값을 쓰지 않는다.

### 8. 매매 엔진은 포털에 의존하지 않는다

엔진이 포털의 HTTP 응답을 기다리는 코드를 작성하지 않는다.
통신은 DB(`heartbeat`, `command`)로만 한다.

---

## 코드 규약

### 언어·스타일

- Python 3.12+
- 타입 힌트 필수
- 포맷: `ruff format`, 린트: `ruff check`
- 함수는 한 가지 일만. 100줄 넘으면 분리를 검토한다

### 구조

```
portal/        웹 서버
engines/       daytrade, swing
collectors/    news, market
common/        broker, strategy, db, notify
backtest/      CLI
config/        설정 파일
```

`common/strategy/`는 실전과 백테스트가 **동일하게** 사용한다.
백테스트 전용 전략 코드를 만들지 않는다.

### DB 접근

- `common/db/`의 모델을 통해 접근한다. 각 모듈에서 raw SQL을 흩뿌리지 않는다
- 스키마 변경은 마이그레이션 파일로 남긴다. 직접 `ALTER`하지 않는다
- 트랜잭션 범위를 명시한다. 특히 주문 기록은 원자적으로 처리한다

### 에러 처리

- 예외를 삼키지 않는다. 최소한 `event_log`에 남긴다
- 수집기의 예외가 다른 수집기에 전파되면 안 된다
- `TransientError` / `PermanentError`를 구분한다 (`INTERFACES.md` 2.3)

### 로깅

- 표준 `logging` 사용. `print()` 금지
- 중요 이벤트는 `event_log` 테이블에도 기록한다
- 주문 관련 로그에 계좌번호 전체를 남기지 않는다 (마스킹)

---

## 테스트

### 필수 테스트

아래는 반드시 테스트를 작성한다.

| 대상 | 확인 사항 |
|---|---|
| `BacktestFeed` | `now()` 이후 데이터가 반환되지 않는가 |
| `RiskManager` | 한도 초과 시 거부하는가 |
| 주문 흐름 | 중복 `client_order_id`가 차단되는가 |
| `RegimeEngine` | 지표 결측 시 정규화가 맞는가 |
| 브로커 어댑터 | 응답 파싱 (실제 응답 샘플 고정) |

### 실계좌 보호

- 테스트는 `is_paper=TRUE` 계좌 또는 목(mock)으로만 실행한다
- 테스트 코드에서 실계좌 ID를 참조하지 않는다
- 통합 테스트는 모의투자 계좌로만 수행한다

---

## 작업 방식

### 진행 단위

`ROADMAP.md`의 단계 단위로 작업한다.
한 단계를 끝내고 완료 기준을 충족한 뒤 다음으로 넘어간다.

여러 단계를 한 번에 진행하지 않는다.

### 커밋

- 의미 단위로 나눈다. 한 커밋에 여러 기능을 섞지 않는다
- 메시지는 한국어 또는 영어. 무엇을 왜 바꿨는지 쓴다
- `.env`, `logs/`, `*.db`, `__pycache__/`는 `.gitignore`에 포함

### 배포

**국내장 마감 후(15:40 이후)에만 배포한다.** 장중 재시작 금지.

- 매매 엔진: systemd. 재시작 전 미체결 주문 확인
- DB 스키마 변경: 백업 후 적용
- 포털·수집기: 비교적 자유롭게 재시작 가능

### 확인이 필요한 상황

아래는 임의로 판단하지 말고 사용자에게 묻는다.

- 설계 문서와 다르게 구현해야 할 것 같을 때
- 스키마 변경이 필요할 때
- 외부 API 스펙이 문서의 가정과 다를 때
- 실계좌에 영향을 주는 작업
- 전략의 구체적 로직 (별도 논의 대상)

---

## 환경

```
서버      bh-server (Ubuntu 26.04 LTS)
          RAM 7.1GB / 4 cores / 디스크 203GB 여유
DB        PostgreSQL (portal_db)
접속      Tailscale
개발      집 PC에서 작업 → git push → 서버에서 pull
```

증권사는 키움 단일. 계좌는 단타·스윙·모의투자 3개.

---

## 범위 밖

아래는 이 프로젝트에서 다루지 않는다.

- **매매 전략의 구체적 내용** — 별도 논의. 지금은 인터페이스만 존재한다
- **미국 주식 매매** — 나중에 별도 엔진. 지금은 DB 컬럼만 확보
- **리치프랜즈랩** — 별도 서비스. 종목코드·시세만 공유
