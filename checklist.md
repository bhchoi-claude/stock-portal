# checklist.md — Phase 2 브로커 + 시세 수집

> Phase 1 체크리스트는 커밋 `fa30f27` 이전 이력에 있다.
> 완료 요약은 `docs/ROADMAP.md` 진행 현황 참조.

**이 단계가 끝나면 데이터가 쌓이기 시작한다. 이후 계속 돌린다.**

---

## 실측으로 확인된 것 (2026-08-27)

로더를 쓸 때 이 전제를 다시 확인하지 않아도 된다.

- KRX API 호스트는 `data-dbg.krx.co.kr`. `openapi.krx.co.kr` 는 포털이고 404 다
- 인증키와 별개로 **API 마다 이용신청**이 필요하다. 미신청은 401
- 일별매매정보 회당 약 1.2초. 10회 연속에 429 없음. 3년치 ≈ 시장당 15분
- 평일인데 0건이면 휴장일이다 (`20260817` 로 확인)
- **`ISU_CD` 가 API 마다 다른 값이다.** 종목기본정보는 표준코드,
  일별매매정보는 단축코드. 시장구분도 `MKT_TP_NM` / `MKT_NM` 으로 이름이 다르다
- 숫자에 콤마가 없다. 다만 문자열이라 `Decimal` 로 감싼다
- 일별매매정보 apiId 는 `sto/{stk,ksq,knx}_bydd_trd` 다 (2026-08-27 확인)
- **시·고·저가 `0` 으로 오는 행이 있다.** 종가로 채워 도지로 만든다 (승인됨).
  대부분 거래량 0 과 같이 오지만 **거래량이 있는데 시·고·저만 0 인 행이
  3년에 4건** 있었다. 거래량이 아니라 시·고·저 값으로 판정한다
- 종가가 `0` 인 행은 3년치 205만건에 하나도 없다
- **종목코드에 영문자가 들어간다** (`00104K`, `0030R0` 등 82건).
  `\d{6}` 으로 검증하지 않는다
- `stock` 에 없는 시세는 KOSPI 26건(전부 리츠 계열). KOSDAQ·KONEX 는 0건
- **KRX 는 당일 일별매매정보를 다음 날 공개한다.** 마감 직후에는 받을 것이 없다
- **종목기본정보는 휴장일·주말에도 응답한다** (2026-08-15 토요일에 2846건).
  거래일 판정에 쓸 수 없다. 시세 API 로만 판정한다
- 두 API 의 공개 경계는 같다. 미공개 날짜는 마스터도 시세도 0건이다

## 사전 작업

- [x] **일별매매정보 이용신청 3종 완료** (2026-08-27) — 첫 호출 전에 401 여부만 확인한다
- [x] **거래정지·거래량 0 종목의 OHLC 확인** (2026-08-27) — 시·고·저가 `0` 으로 온다.
      종가로 채우기로 결정. 위 '실측으로 확인된 것' 참조

## 일봉 수집 (KRX)

- [x] `price_daily` 매핑 함수 + 테스트 (2026-08-27, 실제 응답 샘플 고정)
- [x] **`stock` 에 없는 종목 건너뛰기** (2026-08-27) — 실적재에서 26건 건너뜀
- [x] 하루치 적재 CLI (2026-08-27) — `20260826` 2849건 적재 검증 완료
- [x] 3년치 일괄 적재 (2026-08-28) — 727 거래일, 3083 종목, 204만 8669행
- [x] 과거 종목 마스터 백필 — 생존편향 대응. 폐지 234건 포함해 `stock` 3083건
- [x] `delisted_at` 정밀화 (2026-08-28) — 185건 갱신.
      234건 전부 `마지막 거래일 + 1일` 로 맞음
- [x] 일 1회 갱신 배치 (2026-08-28) — `collectors.market.daily`.
      '오늘' 이 아니라 `price_daily` 에 빠진 거래일을 채운다
- [x] `exchange_holiday` 역산 적재 (2026-08-28) — 55일.
      일봉 구간 안의 거래 없는 평일. 주말은 담지 않는다

## 종목 상태

- [x] **종목 상태 갱신** (2026-08-28) — `collectors.market.stock_status`
  - [x] `stock` 현재값 갱신 + `stock_status` 변경 이력 기록
  - [x] 상장·폐지·이전상장 감지. 행을 삭제하지 않는다
  - [x] 폐지는 이전 적재와의 차집합으로 감지한다
  - [x] 폐지됐던 종목이 다시 나타나면 표시를 지운다 (오탐 자가 복구)
  - [ ] **`is_managed`·`is_suspended` 는 출처가 없어 채우지 못한다.**
        KRX 종목기본정보에 해당 필드가 없다. 출처를 찾아야 한다

## 브로커 (키움)

- [ ] `common/types.py` — 공통 타입 (2026-08-26 위치 승인됨)
- [ ] 브로커 목(mock)
- [ ] `KiwoomBroker` 조회 구현 — 주문은 Phase 8
- [ ] 에러 분류 `TransientError` / `PermanentError`
      **401 은 권한 문제이므로 `PermanentError` 다**
- [ ] 분봉 수집기 — 관심종목 200개로 시작, 한도 보며 확대

## 수정주가

- [x] **`corporate_action` 출처 확정** (2026-08-28) — KRX 일별매매정보의
      `LIST_SHRS`. 새 키·신청 불필요
- [x] 주식수 감소(감자·병합) 수집 (2026-08-28) — 377건 적재.
      354건 조정 대상, 23건은 인적분할·소각이라 기록만
- [ ] 인적분할 조정 — 분할 가치 비율이 필요하다. KRX 에 없다. DART 필요
- [ ] 주식수 증가(무상·유상증자) 구분 — DART 키 필요. `PARVAL` 로 액면분할 구분
- [ ] 현금배당 수집 — KRX 일별매매정보에 없다. DART 필요
- [ ] `price_daily.adj_factor` 계산 배치

## 그 외

- [ ] `trading_flow` 수집 (외국인 순매수)
- [ ] Nginx + systemd (Phase 1 에서 이관)
  - [x] **일 1회 배치 타이머 등록** (2026-08-28) — enabled+active,
        systemd 로 1회 실행해 로그까지 확인
  - [ ] 재부팅 후 자동 기동 확인 — `enabled` 이므로 설정상 기동한다.
        실제 재부팅 검증은 미실시
  - [ ] Nginx (포털이 생기는 Phase 4 에 필요)
- [ ] `price_minute` 월 파티션 자동 생성 배치 (현재 2027-12 까지만 존재)

## Phase 2 완료 기준

- [ ] 전 종목 일봉 3년치 적재 완료
- [ ] 분봉이 매일 자동으로 쌓임
- [x] 수집 실패 시 알림 동작 (2026-08-28) — 텔레그램 발송·배선 모두 실검증
- [ ] 종목 상태 변경이 `stock_status` 에 이력으로 남음
- [ ] 분할 종목의 조정가 시계열이 끊기지 않음
- [ ] 서버 재부팅 후 수집기가 자동 기동됨

---

## 다음 세션 시작점 (2026-08-28 새벽 중단)

### 바로 확인할 것

3년치 조정 이벤트 스캔을 서버에서 `nohup` 으로 띄워두고 중단했다.
02:40 전후에 끝났을 것이다. **먼저 결과부터 확인한다.**

```bash
ssh bh-server@bh-server "tail -3 ~/stock-portal/logs/corporate_action.log"
```

확인할 것.

- 마지막 줄이 `조정 이벤트 N건 적재` 인가 (아니면 중간에 죽은 것이다)
- `corporate_action` 건수가 가격제한폭 초과 788건보다 적은가
  (주식수 불변인 거래정지 해제가 걸러졌으므로 적어야 정상이다)
- 비율이 `0.02`, `0.1`, `0.5` 처럼 깔끔하게 떨어지는가
- 3% 같은 어중간한 비율이 섞였으면 자기주식 소각 오탐이다

중간에 죽었으면 그냥 다시 돌린다. upsert 라 중복되지 않는다.

```bash
ssh bh-server@bh-server "cd ~/stock-portal && nohup .venv/bin/python -m collectors.market.corporate_action > logs/corporate_action.log 2>&1 &"
```

### 그다음 작업

- [ ] `price_daily.adj_factor` 계산 배치 — `corporate_action` 에서 역순 누적한다.
      **이 날 이전 가격에 곱할 값은 `ratio` 의 역수를 누적한 것이다**
      (50:1 감자면 ratio 0.02, 그 이전 가격에 50 을 곱해야 이어진다)
- [ ] 검증 — 쌍방울(`KRX:102280`) 2024-11-11 앞뒤로 조정가 시계열이 끊기지 않는지

### 막혀 있는 것

`DART_API_KEY` 가 비어 있다. 아래는 키가 생겨야 진행된다.

- 주식수 증가분(무상증자 vs 유상증자) 구분
- 현금배당 수집

### 건드리지 않은 것

- 서버 `~/stock-portal/ion` — 정체불명 파일(398바이트, 8/25). 내 작업과 무관하다
- `docs/INTERFACES.md` 코드블록이 `ruff format` 기준에 걸린다. 기존부터 그랬다
