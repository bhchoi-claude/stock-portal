# checklist.md — Phase 6 백테스트 프레임워크

> **전략 내용은 만들지 않는다.** 틀만 만들고 더미 전략으로 완주를 확인한다.
> 전략은 Phase 7 에서 별도로 논의한다.
> Phase 4·5 체크리스트는 커밋 `b0b782f` 이전 이력에 있다.

---

## 시작 전에 정한 것 (2026-08-30)

- **체결은 다음 봉 시가.** 종가로 신호를 계산하고 다음 거래일 시가에 체결한다.
  미래 참조가 구조적으로 불가능해진다 (승인 사항)
- **생존편향 경고를 결과에 붙인다.** `backtest_run.note` 에 자동으로 적어
  숫자를 볼 때마다 함께 보이게 한다 (승인 사항)
- **스윙(일봉)만 한다.** 분봉이 나흘치뿐이다. 단타는 Phase 10
- 유니버스는 최소한으로 — 해당 시점 상장 + 관리종목·거래정지 제외 +
  거래대금 상위 N. 확정은 Phase 7 (`config/universe.yaml`)

## 1단계 — DataFeed

- [ ] `DataFeed` ABC (`INTERFACES.md` 3장)
- [ ] `BacktestFeed` — 커서 시각이 `now()` 다
- [ ] **`now()` 이후 데이터를 반환하지 않는 단위 테스트** (CLAUDE.md 필수)
- [ ] `get_candles` 는 조정가를 준다. `get_quote` 는 조정하지 않는다 (3.3)
- [ ] `get_universe` 는 `stock_status` 의 **해당 시점 값**을 읽는다 (3.4)
      현재값을 읽으면 생존편향이 생긴다

## 2단계 — Strategy 규격

- [ ] `Context`, `EntryIntent`, `ExitIntent` 타입 (`INTERFACES.md` 4장)
- [ ] `Strategy` ABC — `scan`, `manage`, `on_start`, `on_day_end`
- [ ] 더미 전략 — 매일 사서 익일 판다. **틀을 검증하는 용도다**
- [ ] 전략은 의도만 반환한다. 주문도 크기 산정도 하지 않는다 (4.2)

## 3단계 — RiskManager

- [ ] `RiskDecision`, `RiskManager.evaluate` (`INTERFACES.md` 5장)
- [ ] **한도 초과 시 거부하는 테스트** (CLAUDE.md 필수)
- [ ] 확인 항목 — 국면별 배분, 건당 최대, 종목당 비중, 동시 보유 수, 일일 손실
- [ ] 한도는 `config/limits.yaml` 에 둔다. 코드에 숫자를 쓰지 않는다

## 4단계 — 체결 시뮬레이터

- [ ] 다음 거래일 시가 체결
- [ ] 수수료·거래세·슬리피지 반영. **미반영 결과는 무의미하다** (PROJECT.md 11장)
- [ ] 보유 중 상장폐지 → 정리매매 마지막 가격으로 청산
- [ ] 금액은 전부 `Decimal`. `float` 를 쓰지 않는다

## 5단계 — 루프와 CLI

- [ ] `backtest/` 패키지
- [ ] `python -m backtest run --strategy swing --from 2025-01-01 --to 2025-12-31`
- [ ] 하루 단위 진행 — 휴장일 건너뛰기, `manage` 는 매일, `scan` 도 매일

## 6단계 — 결과 적재

- [ ] `backtest_run` — 파라미터 전체와 `fee_rate`·`slippage_rate` 를 남긴다
      0 으로 돌린 결과도 그 사실이 남는다
- [ ] `backtest_trade` — 체결 하나하나
- [ ] 지표 — 총수익률, MDD, 승률, 거래수, 샤프
- [ ] `note` 에 생존편향 경고 자동 첨부

## 7단계 — 포털 화면

- [ ] `GET /api/backtest/runs` (`INTERFACES.md` 10장에 이미 있다)
- [ ] 실행 목록과 지표 비교
- [ ] **화면 위치를 정해야 한다.** `PROJECT.md` 6장 탭 목록에 백테스트가 없다.
      새 탭인지 운영·로그 안인지 그때 확인받는다

## Phase 6 완료 기준

- [ ] 더미 전략으로 백테스트가 완주한다
- [ ] 수수료·슬리피지 변경이 결과에 반영된다
- [ ] 미래 참조 방지 테스트가 통과한다

---

## 사용자 조치 필요

- [ ] **수수료율 확인** — 키움 온라인 매매수수료율과 증권거래세율.
      임시값으로 시작하되 실제 값을 받으면 바꾼다. 값이 틀리면 결과가 통째로 틀린다

## 관측 대기 (Phase 4·5 에서 이월)

- [ ] 일봉·지표·국면 heartbeat — 매일 19:00
- [ ] 분봉·수급·플래그 heartbeat — 08-31 16:10 이 첫 발화
- [ ] 분봉 자동 적재 — 지금까지 쌓인 것은 손으로 돌린 결과다
- [ ] `stock_flags` 플래그 생존 — KRX 마스터가 돈 뒤에도 남는지
- [ ] Claude Design 결과물 연결 — 산출물이 나오면 템플릿만 바꾼다
- [ ] 실제 데이터로 키워드 병합을 한 번 해본다

## 받아들이고 닫은 것 (Phase 5)

| 항목 | 왜 |
|---|---|
| 쓰레기 키워드 (`NV`) | 버린다는 표시 자리가 스키마에 없다 (승인) |
| `제우스` 오탐 | 종목명이자 게임 이름. 규칙으로 못 가른다 (승인) |
| 언급의 무게 | 주인공과 스쳐 나온 종목이 같은 무게다 |
| 종목 약칭 | 사람이 사전을 만들어야 한다 |
| 급등 임계값 3.0 | 근거 없는 임시값. 일주일 뒤 조정 |

## 건드리지 않은 것

- 서버 `~/stock-portal/ion` — 정체불명 파일(398바이트, 8/25). 내 작업과 무관하다
- `docs/INTERFACES.md` 코드블록이 `ruff format` 기준에 걸린다. 기존부터 그랬다
