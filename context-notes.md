# context-notes.md — 작업 중 내린 결정과 근거

> 설계 결정(A-1~A-6)의 내용과 이유는 `docs/` 와 커밋 메시지에 있다.
> 이 문서는 **구현하면서 내린 판단**만 기록한다. 새로 읽는 사람이 "왜 이렇게 했나"를
> 다시 캐지 않도록 하는 것이 목적이다.

---

## 2026-08-24 — 마이그레이션 작성

### 순수 SQL + 러너를 골랐다 (Alembic 아님)

`SCHEMA.md` 가 이미 손으로 쓴 SQL 이고 그것이 계약이다. Alembic 을 쓰면 모델에서
스키마를 역산하게 되는데, 월 단위 파티셔닝·부분 인덱스·SCD 제약은 결국 raw SQL 로
써야 해서 이점이 줄어든다. 문서와 코드가 어긋날 여지를 없애는 쪽을 택했다.

부작용으로 `common/db/` 모델 계층이 특정 ORM 에 묶이지 않는다. 모델은 나중에
psycopg 로 직접 짜든 SQLAlchemy 를 얹든 자유롭다.

러너는 `schema_migration` 테이블에 `version`, `checksum`, `applied_at` 을 남긴다.
**checksum 은 적용된 파일이 나중에 수정되는 것을 잡기 위한 것이다.** 이미 적용된
마이그레이션은 고치지 않고 새 파일을 추가한다.

### 시드는 정적 데이터만 넣었다

`002_seed_static.sql` 에 `exchange` 1건과 `indicator` 8종만 넣었다. 외부 조회가
필요 없는 고정값이라 스키마와 함께 버전 관리하는 편이 안전하다.

`stock`, `exchange_holiday`, `account`, `source` 는 외부 데이터나 실제 계좌 정보가
있어야 하므로 별도 적재 스크립트로 미뤘다. 마이그레이션이 외부 API 에 의존하면
재현 가능성이 깨진다.

### CHECK 제약을 넣지 않았다

`side`, `status`, `action_type` 등은 `SCHEMA.md` 에 주석으로만 허용값이 적혀 있다.
CHECK 제약을 걸면 잘못된 값을 DB 가 막아주지만, **설계 문서에 없는 제약을 임의로
추가하는 것**이라 넣지 않았다.

넣을지 여부는 별도 판단이 필요하다. 넣는다면 값 집합이 확정된 뒤가 좋다.
지금 걸면 상태값을 하나 추가할 때마다 마이그레이션이 필요해진다.

### 파티션을 2027-12 까지만 만들었다

`price_minute` 은 `PARTITION BY RANGE (ts)` 이고 2026-08 ~ 2027-12 의 17개 파티션을
`DO` 블록으로 생성한다. **파티션 경계는 UTC 오프셋을 명시했다.** 날짜 리터럴만 쓰면
세션 타임존에 따라 경계가 9시간 밀린다. 저장은 UTC 이므로 경계도 UTC 여야 한다.

DEFAULT 파티션은 두지 않았다. 있으면 나중에 새 파티션을 붙일 때 DEFAULT 를
스캔해야 해서 느려진다. 대신 **월 파티션을 미리 만드는 배치가 Phase 2 에 필요하다.**
2028-01 이 오기 전에 없으면 분봉 INSERT 가 실패한다. `checklist.md` 에 남겼다.

### `KOSPI_MA200_GAP` 의 source 를 'derived' 로 넣었다

`SCHEMA.md` 의 `indicator.source` 주석은 `'kofia'|'krx'|'customs'|'ecos'` 를 예시로
든다. 그런데 이 지표는 수집이 아니라 KOSPI 지수에서 **계산하는 파생값**이다
(`ROADMAP.md` Phase 3 에도 "수집이 아닌 파생"으로 적혀 있다).

`'krx'` 로 넣으면 수집기가 KRX 에서 이 지표를 직접 받아오는 것처럼 읽힌다.
정확성을 택해 `'derived'` 를 썼다. **문서의 예시 목록에 없는 값이므로 확인이 필요하다.**

### 검증 범위와 한계

로컬에 PostgreSQL 도 Docker 도 없어 **실행 검증은 하지 못했다.** sqlglot 으로
PostgreSQL 방언 파싱만 했다.

확인한 것.

- 문법 파싱 (001: 47개 구문, 002: 2개 구문)
- 테이블 32개, 컬럼 중복 없음
- FK 참조 대상이 모두 먼저 선언됨
- 인덱스·INSERT 가 존재하는 테이블/컬럼을 가리킴
- 파티션 DDL 17개의 파싱·경계 연속성·이름 중복
- A-1~A-6 결정이 실제로 반영됐는지 항목별 확인

확인하지 못한 것.

- `DO $$ ... $$` 블록의 plpgsql 문법 (sqlglot 이 다루지 못해 생성 결과만 재현 검증)
- 실제 실행 시점의 오류 (타입 불일치, 예약어 충돌, 권한)
- 파티션 부모-자식 제약이 실제로 성립하는지

**2026-08-24 서버 적용 완료.** PostgreSQL 18.6 에서 `apply` 가 정상 동작했다.
테이블 50개(본체 32 + `schema_migration` + 파티션 17), `exchange` 1건, `indicator` 8종 확인.
위의 "확인하지 못한 것" 중 실행 시점 오류와 파티션 제약은 이걸로 해소됐다.

### 예약어를 검토했다

`position`, `value`, `open`, `close`, `level`, `source`, `signal` 은 PostgreSQL 에서
비예약어라 테이블·컬럼명으로 쓸 수 있다. `order` 는 예약어이므로 `SCHEMA.md` 대로
`order_request` 를 유지했다.

실행 검증을 못 했으므로 이 판단도 서버 적용으로 확인해야 한다.

---

## 2026-08-24 — 서버 구성

### venv 를 쓴다

Ubuntu 26.04 는 PEP 668 로 시스템 파이썬에 직접 설치하는 것을 막는다.
`pip install` 이 `externally-managed-environment` 로 실패한다.

`--break-system-packages` 를 쓰지 않고 `~/stock-portal/.venv` 를 만들었다.
24시간 돌릴 서버의 시스템 파이썬을 건드리는 위험을 감수할 이유가 없다.

**systemd 유닛을 쓸 때 `.venv/bin/python` 을 명시해야 한다.**
시스템 파이썬으로 실행하면 psycopg 를 찾지 못한다.

### 서버는 배포 키로 pull 한다

비공개 저장소라 서버에도 인증이 필요하다. 계정 토큰 대신 **읽기 전용 배포 키**를 썼다.
서버는 pull 만 하므로 쓰기 권한이 필요 없고, 키가 새도 저장소 하나로 피해가 제한된다.

`~/.ssh/config` 의 `github-stockportal` 호스트 별칭으로 접근한다.
암호 없는 키다. systemd 가 자동 pull 할 때 암호를 물으면 멈추기 때문이다.

### GitHub 계정이 두 개다

저장소는 `bhchoi-claude` 소유이고, 개발 PC 는 `bhchoihaikorea` 로 인증돼 있다.
`bhchoihaikorea` 를 협업자로 추가해서 push 한다. 커밋 author 도 이쪽이다.

### 공인 IP 는 문서에 적지 않는다

`.env` 의 `KIWOOM_ALLOWED_IP` 하나만 정본으로 둔다.
비밀값은 아니지만 집 네트워크를 특정하는 정보라 계좌번호·키와 같은 방침을 적용했다.

---

## 2026-08-25 — 키움 REST 실측

모의투자 계좌로 토큰 발급과 시세 조회에 성공했다. Phase 0 완료.
확인한 규격은 `INTERFACES.md` 2.4 에 적었다. 여기에는 판단만 남긴다.

### `stk_cd` 는 순수 종목코드다 — 앞서 한 추정이 틀렸다

검색으로 본 `KRX:039490` 형식 예시를 근거로 "키움도 거래소 접두어를 쓰므로
A-1 결정과 일치한다"고 적었는데 **틀렸다.** `ka10001` 에 `KRX:005930` 을 보내면
`return_code = 5` 로 거부되고, `005930` 만 받는다.

A-1 결정 자체는 유효하다. 근거가 이전상장 대응이었지 키움 규격과의 일치가
아니었기 때문이다. 다만 **어댑터가 `stock_id` 에서 접두어를 떼야 한다.**
`stock.code` 를 별도 컬럼으로 보관해둔 것이 여기서 쓰인다.

이 변환은 **브로커 어댑터 안에만** 둔다. 밖으로 새면 전략이 종목코드 형식을
알게 되고, 의존 방향(`INTERFACES.md` 0장)이 깨진다.

### 실전과 모의는 앱키가 별도다

모의 앱키로 실전 도메인에 접속하면 명시적으로 거부된다.
`.env` 를 `KIWOOM_APP_KEY_PAPER` / `KIWOOM_APP_KEY_LIVE` 로 나눴다.

어댑터는 `account.is_paper` 하나로 **도메인과 키를 함께** 골라야 한다.
둘 중 하나만 바꾸면 인증 오류가 난다.

### HTTP 200 이어도 `return_code` 를 봐야 한다

토큰 발급 실패도 HTTP 200 으로 왔다. 에러는 본문의 `return_code` 에 있다.
어댑터의 에러 분류(`TransientError` / `PermanentError`)를 HTTP 상태로만 하면
실패를 성공으로 읽는다.

### 검증 과정에서 겪은 것

`.env` 에 앱키를 넣을 때 붙여넣기로 `
` 한 글자만 들어간 적이 있다.
`grep` 으로는 "값이 있음"으로 보여서 놓치기 쉬웠다.
값을 다루는 스크립트는 항상 `.strip()` 을 거치게 한다.

셸에서 `"{\"appkey\":\"$VAR\"}"` 식으로 JSON 을 조립하면 값에 특수문자가
하나만 있어도 깨진다. JSON 은 코드로 만든다.

---

## 2026-08-26 — 기준 데이터 출처 확정, `common/db/` 모델 계층

### 기준 데이터 정본은 KRX 다

`stock` 은 `board`·`listed_shares`·`listed_at`·`is_spac`·`is_preferred` 를 요구한다.
DART `corpCode` 에는 시장 구분과 상장주식수가 없고, 키움 종목 리스트 API 는
아직 실측하지 않았다. 확인되지 않은 규격 위에 적재 스크립트를 쌓지 않는다.

DART 고유번호는 Phase 5 정보수집에서 별도로 매핑한다. 지금 섞지 않는다.

휴장일도 KRX 매매거래일정을 쓴다. 공휴일 달력에는 임시휴장과 조기폐장이 없다.

### ORM 을 쓰지 않는다

`psycopg` 위에 함수를 얹었다. SQLAlchemy 를 넣지 않은 이유는 두 가지다.

스키마가 마이그레이션 SQL 로 이미 확정돼 있어 모델이 정의가 아니라 사본이다.
사본이 둘이면 어긋난다. 그래서 사본을 얇게 두고 어긋남을 테스트로 잡는 쪽을 골랐다.

그리고 `price_minute` 처럼 대량 적재가 필요한 테이블이 있다. ORM 계층은
그 지점에서 어차피 벗겨내게 된다.

### 6개 테이블만 만들었다

`exchange`, `exchange_holiday`, `stock`, `stock_status`, `account`, `source`
그리고 `event_log`. Phase 1 적재에 필요한 것뿐이다.

나머지 26개는 쓰는 시점에 추가한다. 지금 만들면 쓰이기 전에 스키마가 바뀐다.

### `load_database_url` 을 `conn.py` 로 옮겼다

`migrate.py` 에 있던 것을 옮기고 `migrate.py` 가 가져다 쓴다.
`.env` 파싱이 두 곳에 있으면 한쪽만 고쳐진다. 특히 `.strip()` 처리가 그렇다.
(2026-08-25 에 앱키에 보이지 않는 문자가 섞였던 건과 같은 종류의 문제다.)

`migrate.py` 는 `psycopg` 확인이 끝난 **뒤에** 이 import 를 한다.
`conn.py` 가 최상단에서 `psycopg` 를 import 하기 때문에, 위로 올리면
"psycopg 가 없습니다" 안내가 뜨기 전에 ImportError 로 죽는다.

같은 이유로 `common/db/__init__.py` 는 비워 두었다. 여기서 재export 하면
`python -m common.db.migrate` 가 패키지 import 단계에서 `psycopg` 를 요구한다.

### upsert 는 `COALESCE` 로 덮는다

`sector`·`listed_shares`·`listed_at`·`delisted_at` 은 `COALESCE` 를 거친다.
KRX 안에서도 목록마다 주는 필드가 다르다. 필드가 없는 출처로 재적재할 때
`EXCLUDED` 를 그대로 쓰면 기존 값이 NULL 로 지워진다.

`delisted_at` 도 마찬가지다. 한 번 채워진 폐지일을 되돌리지 않는다.
행을 지우지 않기로 한 A-6 결정과 같은 방향이다.

### `stock_status` 는 최초 1행만 연다

`open_stock_status` 는 열린 행(`valid_to IS NULL`)이 없을 때만 삽입한다.
변경 감지와 이력 종료는 Phase 2 의 상태 갱신 배치가 맡는다.
Phase 1 에서 이력 관리까지 만들면 수집기 없이 검증할 방법이 없다.

### 스키마 드리프트 테스트

`tests/test_schema_drift.py` 가 `001_initial.sql` 을 파싱해서 dataclass 필드와
대조한다. DB 없이 돈다.

`get_stock` 이 `Stock(*row)` 로 만들기 때문에 `STOCK_COLUMNS` 순서가 어긋나면
값이 조용히 뒤섞인다. 이 순서도 함께 검사한다.

### DB 통합 테스트는 서버에서만 돈다

`DATABASE_URL` 이 없으면 skip 한다. 개발 PC 에는 `.env` 가 없고,
서버 PostgreSQL 은 localhost 만 수신해서 Tailscale 로도 닿지 않는다(5432 거부).

**따라서 pull 한 뒤 서버에서 한 번 돌려야 실제로 검증된 것이다.**
테스트는 커밋 없이 롤백하므로 운영 데이터를 건드리지 않는다.

## 2026-08-26 (2) — KRX 오픈API 로 결정, 텔레그램 알림

### KRX 는 공식 오픈API 를 쓴다

`data.krx.co.kr` 화면이 쓰는 비공식 JSON 엔드포인트가 더 빠른 길이지만
쓰지 않기로 했다. 이 적재는 서버에서 매일 도는 배치가 된다.
문서가 없는 경로는 예고 없이 바뀌고, 바뀌면 조용히 깨진다.

승인 대기 동안 로더를 미리 쓰지 않는다. **응답을 실측한 뒤에 쓴다.**
어제 키움 `stk_cd` 를 검색 결과로 추정했다가 틀렸다. 같은 실수를 반복하지 않는다.

### `.env` 읽기를 `common/env.py` 로 모았다

`conn.py` 에 이어 텔레그램 토큰·chat_id 까지 필요해지면서 세 번째 파서가
생길 참이었다. `load_env` / `require_env` 로 합치고 `load_database_url` 은
그 위의 한 줄이 됐다.

`.env` 는 한 번만 읽고 캐시한다. 값이 바뀌면 프로세스를 재시작한다.
운영 중에 비밀값이 슬며시 바뀌는 쪽이 더 위험하다.

줄 중간의 `#` 는 주석으로 보지 않는다. DB 비밀번호에 들어갈 수 있다.

### 알림은 평문으로 보낸다

Markdown 을 쓰면 종목명과 본문의 `_ * [` 를 매번 이스케이프해야 하고,
빠뜨리면 발송 자체가 400 으로 실패한다. 알림은 꾸미는 것보다 도착하는 게 먼저다.

### `send` 는 예외를 던지지 않고 bool 을 준다

알림 실패가 매매 엔진이나 수집기를 멈추면 안 된다.
실패 이유는 로그로 남기고 호출부는 계속 간다. (`INTERFACES.md` 9장)

### 토큰이 URL 에 들어 있다

텔레그램은 `/bot{토큰}/sendMessage` 형식이라 URL 자체가 비밀값이다.
그래서 예외를 `%s` 로 그대로 찍지 않고 예외 타입 이름만 남긴다.
`urllib` 의 에러 메시지에 URL 이 섞여 들어오기 때문이다.
`tests/test_telegram.py` 가 로그에 토큰이 없는지 확인한다.

### HTTP 200 과 `ok` 는 다르다

키움에서 겪은 것과 같다. 텔레그램도 본문의 `ok` 가 정본이다.
상태코드만 보면 "chat not found" 를 성공으로 읽는다.

### 중복 억제는 아직 만들지 않았다

`INTERFACES.md` 9.1 의 규칙(국면 전환 시에만, 엔진 중단 최초 1회 등)은
발송 주체가 있어야 검증할 수 있다. 지금은 주체가 없다.
`event_log.notified` 를 쓰는 억제 로직은 Phase 3 국면 알림과 함께 만든다.

## 2026-08-26 (3) — `config/` 신설, 계좌·소스 적재

### 공통 타입은 `common/types.py` 에 둔다 (승인 받음)

`PROJECT.md` 4장 구조에 없던 파일이라 확인을 받고 추가했다.

`Candle`·`Quote`·`Position` 은 `Broker` 와 `Strategy` 가 함께 쓴다.
`common/broker/` 안에 두면 전략과 `BacktestFeed` 가 `common.broker` 를
import 하게 되어 의존 방향이 뒤집힌다. 파일은 브로커 작업 때 만든다.

### 적재 스크립트를 `common/db/seed.py` 에 뒀다

`migrate.py` 와 같은 자리다. `python -m common.db.seed` 로 돈다.
새 디렉토리를 만들 이유가 없었다.

**KRX 로더는 여기 넣지 않는다.** 외부 HTTP 호출은 수집기의 일이고,
`common/db/` 가 바깥 API 를 알기 시작하면 경계가 무너진다.
`seed.py` 는 `config/` 에 있는 정적 정의만 다룬다.

### PyYAML 을 추가했다

`config/*.yaml` 이 설계 전제라 불가피했다. 지금까지 psycopg 하나였다.

### YAML 의 `1.0` 은 float 이다

`weight` 를 `Decimal(str(값))` 으로 감싼다. `Decimal(1.1)` 은
`1.100000000000000088…` 이 된다. 금액은 아니지만 NUMERIC 컬럼이라
왕복에서 값이 흔들리면 비교가 어긋난다.

### 계좌번호가 새지 않는 것을 구조로 보장한다

`build_accounts` 는 `account` 테이블에 있는 6개 키만 골라 담는다.
`accounts.yaml` 에 `account_no` 가 섞여 들어와도 모델에 실릴 자리가 없다.
런타임 검사 대신 테스트로 확인한다 (`test_계좌번호는_모델에_담기지_않는다`).

`allocation`(자금 배분)도 같은 이유로 DB 에 가지 않는다.
`account` 테이블에 컬럼 자체가 없고, RiskManager 가 config 에서 직접 읽는다.

### 계좌 활성 상태의 초기값

`paper` 만 `is_active: true` 로 뒀다. `daytrade` 는 Phase 10,
`swing` 실전은 Phase 9 에서 켠다. 지금 켜두면 켜져 있다는 사실을 잊는다.

### 텔레그램 채널은 비워 뒀다

구독 목록을 모른다. 추측해서 채워 넣지 않았다. 형식만 주석으로 남겼다.

## 2026-08-27 — KRX 오픈API 실측 (1차)

### API 호스트는 `data-dbg.krx.co.kr` 다

`openapi.krx.co.kr` 은 포털 웹사이트다. 여기로 API 를 부르면 404 가 오고,
응답도 JSON 이 아니라 에러 HTML 페이지다.

실제 호출은 `https://data-dbg.krx.co.kr/svc/apis/{path}/{apiId}` 로 간다.
명세 화면의 샘플 블록에 `Host: openapi.krx.co.kr` 라고 적혀 있는데
**그 표기가 실제와 다르다.** 테스트 폼의 `apiUrl` 값이 정본이었다.

### 종목기본정보는 시장별로 API 가 나뉜다

| 시장 | apiId |
|---|---|
| 유가증권 | `stk_isu_base_info` |
| 코스닥 | `ksq_isu_base_info` |
| 코넥스 | `knx_isu_base_info` |

경로 앞부분은 `/svc/apis/sto/`, 인증은 요청 헤더 `AUTH_KEY`,
요청 파라미터는 기준일자 `basDd=YYYYMMDD` 하나뿐이다.

`board` 를 어느 API 를 불렀는지로 정할 수 있다. 응답 필드를 해석할 필요가 없다.

### 인증키 발급과 API 이용신청은 별개다

키가 있어도 API 마다 따로 신청해야 한다. 신청 전에는 401 이 온다.

```
{"respMsg":"Unauthorized API Call","respCode":"401"}
```

**401 을 키 문제로 읽으면 안 된다.** 키는 멀쩡하고 권한이 없는 것이다.
어댑터의 에러 분류에서 이 구분이 필요하다. 401 은 `PermanentError` 다.

401 이 JSON 규격대로 왔다는 것 자체가 경로와 헤더가 맞다는 신호였다.
404 HTML 과 401 JSON 을 구분한 덕분에 호스트를 특정할 수 있었다.

### 응답 필드 12개 (명세 기준, 실데이터 미확인)

`ISU_CD` 표준코드 / `ISU_SRT_CD` 단축코드 / `ISU_NM` 한글 종목명 /
`ISU_ABBRV` 한글 종목약명 / `ISU_ENG_NM` 영문 종목명 / `LIST_DD` 상장일 /
`MKT_TP_NM` 시장구분 / `SECUGRP_NM` 증권구분 / `SECT_TP_NM` 소속부 /
`KIND_STKCERT_TP_NM` 주식종류 / `PARVAL` 액면가 / `LIST_SHRS` 상장주식수

**아직 실데이터를 못 봤다.** 이용신청 승인 후 확인하고 매핑을 확정한다.

### 채울 수 없는 컬럼이 있다

`sector`(업종)가 이 API 에 없다. `SECT_TP_NM`(소속부)는 중견기업부·우량기업부
같은 구분이지 업종이 아니다. NULL 로 두고, 업종을 주는 출처가 생기면 채운다.
upsert 를 `COALESCE` 로 짜둔 것이 여기서 쓰인다.

`is_managed`·`is_suspended` 도 없다. Phase 2 상태 갱신 배치가 채운다.
`delisted_at` 은 이전 적재와의 차집합으로 감지해야 한다. 역시 Phase 2 다.

### 휴장일 API 가 없다

서비스 목록에 매매일정·휴장일이 없다. 2026-08-26 에 "휴장일도 KRX
매매거래일정을 쓴다" 고 적은 것은 틀렸다.

일별매매정보에서 역산하는 쪽으로 간다. 거래일이 데이터로 나오므로,
평일인데 없는 날이 휴장일이다. 별도 출처가 필요 없고 임시휴장·조기폐장까지
자동으로 반영된다. `exchange_holiday` 적재는 Phase 2 로 미룬다.
Phase 1 완료 기준에는 `stock` 적재만 있어 영향이 없다.

## 2026-08-27 (2) — 종목 마스터 로더

실데이터를 받아 매핑을 확정했다. 2026-08-26 기준 KOSPI 944 / KOSDAQ 1823 / KONEX 108.

### `name` 은 `ISU_ABBRV` 다

`ISU_NM` 은 `삼성전자보통주`, `삼성전자1우선주` 처럼 정식 명칭이라 화면에 쓸 수 없다.
`ISU_ABBRV` 가 `삼성전자`, `삼성전자우` 로 우리가 아는 이름이다.

### `board` 는 `MKT_TP_NM` 을 그대로 쓴다

값이 정확히 `KOSPI` / `KOSDAQ` / `KONEX` 다. 변환이 필요 없다.
세 API 응답 전체에서 각각 한 종류만 나오는 것을 확인했으므로,
어느 엔드포인트를 불렀는지와 응답이 일치한다.

### 주권 계열만 적재한다 (승인 받음)

`SECUGRP_NM` 에 여섯 종류가 온다. 이 중 셋만 담는다.

| 담는다 | 버린다 |
|---|---|
| 주권, 외국주권, 주식예탁증권 | 부동산투자회사, 사회간접자본투융자회사, 투자회사 |

`stock` 에 증권구분 컬럼이 없어서다. 섞어 넣으면 나중에 리츠를 걸러낼 근거가
DB 안에 남지 않는다. 컬럼을 추가하는 안도 있었으나 스키마 변경을 피했다.

**리츠를 다루게 되면 재적재가 필요하다.** 그때는 컬럼 추가를 함께 검토한다.

### `is_preferred` 는 '보통주가 아님' 이다 (승인 받음)

`KIND_STKCERT_TP_NM` 값은 `보통주` / `구형우선주` / `신형우선주` / `종류주권` 이다.
`종류주권` 은 엄밀히 우선주가 아니지만 참으로 둔다.
이 플래그의 쓰임이 '보통주가 아닌 것을 걸러내기' 이기 때문이다.
거짓으로 두면 종류주권이 보통주처럼 보인다.

### `is_spac` 은 휴리스틱이다

KRX 응답에 스팩 구분값이 없다. 종목명에 `스팩` 이 들어가는지로 판정한다.
관행상 스팩은 전부 이름에 스팩이 들어가지만 **규격이 보장하는 것이 아니다.**
유니버스 필터에서 이 값에만 의존하지 않는 편이 좋다.

### `sector` 는 여전히 NULL 이다

`SECT_TP_NM` 은 KOSPI 에서 전부 빈 문자열이고 KOSDAQ 은 `우량기업부` 다.
업종이 아니라는 것이 데이터로 확인됐다.

### 거래일 파싱에 `datetime` 을 쓰지 않는다

`date.fromisoformat("20150821")` 로 바로 읽는다. `strptime().date()` 은
ruff 의 DTZ007 에 걸리는데, 여기서는 그 경고가 오탐이다. 거래일은 시장 현지
기준 DATE 라서 tz 를 붙이면 오히려 틀린다. (`CLAUDE.md` 절대규칙 5)

경고를 억제하는 대신 `datetime` 을 아예 거치지 않게 바꿨다.
UTC 변환 유혹이 생길 자리를 없애는 쪽이 낫다.

### 위치는 `collectors/market/` 다

외부 HTTP 호출이라 `common/db/` 에 두지 않는다.
`krx.py` 클라이언트는 Phase 2 일별매매정보 수집에서 그대로 쓴다.
`PROJECT.md` 의 `market/` 설명에 '종목 마스터' 를 덧붙였다.

## 2026-08-27 (3) — 일별매매정보 실측, Phase 2 순서 변경

### 같은 이름의 필드가 API 마다 다른 값이다 — 함정

| 필드 | 종목기본정보 | 일별매매정보 |
|---|---|---|
| `ISU_CD` | `KR7005930003` (표준코드/ISIN) | `060310` (**단축코드**) |
| 시장구분 | `MKT_TP_NM` | `MKT_NM` (**이름이 다르다**) |

`ISU_CD` 를 두 API 에서 같은 값으로 믿으면 `stock_id` 매칭이 전부 어긋난다.
일별매매정보의 `ISU_CD` 는 종목기본정보의 `ISU_SRT_CD` 에 대응한다.

**KRX 필드명을 API 간에 재사용해서 추론하지 않는다.** API 마다 실측한다.

### 숫자에 콤마가 없다

`"1108"`, `"48242"`, `"58789416320"` 처럼 순수 숫자 문자열이다.
콤마 제거 전처리가 필요 없다. 다만 문자열이므로 `Decimal(값)` 으로 감싼다.

### 호출 속도 — 회당 약 1.2초

10회 연속 호출이 11.4초. 429 나 지연 증가가 없었다.
3년치가 약 750 영업일이므로 **전 종목 3년 일봉이 시장당 15분 내외**다.
세 시장을 합쳐도 한 시간이 안 걸린다.

키움으로 받으면 종목당 호출이라 2849회 + 페이징이다. KRX 가 압도적이다.

### 휴장일 역산이 실제로 된다

`20260817` 이 0건으로 왔다. 광복절 대체공휴일이다.
평일인데 0건이면 휴장일이라는 규칙이 데이터로 확인됐다.
`exchange_holiday` 를 별도 출처 없이 채울 수 있다.

### `price_daily` 매핑

| 컬럼 | KRX |
|---|---|
| `stock_id` | `KRX:` + `ISU_CD` (단축코드다) |
| `trade_date` | `BAS_DD` |
| `open` / `high` / `low` / `close` | `TDD_OPNPRC` / `TDD_HGPRC` / `TDD_LWPRC` / `TDD_CLSPRC` |
| `volume` | `ACC_TRDVOL` |
| `value` | `ACC_TRDVAL` |

`MKTCAP`·`LIST_SHRS`·`FLUC_RT`·`CMPPREVDD_PRC` 는 쓰지 않는다.
`adj_factor` 는 기본값 1 로 두고 `corporate_action` 에서 따로 계산한다.

**원주가를 그대로 저장한다.** 이 API 는 수정주가가 아니다. 설계와 맞는다.

### FK 때문에 필터가 필요하다

`price_daily.stock_id` 가 `stock(stock_id)` 를 참조한다.
그런데 우리 `stock` 에는 주권 계열 2849건만 있고, 일별매매정보에는
리츠·투자회사도 온다. KOSPI 기준 26건 차이다.

**`stock` 에 없는 종목의 시세는 건너뛴다.** 안 그러면 FK 위반으로 트랜잭션이
통째로 실패한다. 건너뛴 수를 로그로 남겨서 예상 밖으로 커지면 알 수 있게 한다.

### 확인이 남은 것

거래정지 종목의 시가·고가·저가가 `0` 으로 오는지 모른다.
`price_daily` 의 OHLC 는 NOT NULL 이라 0 이 그대로 들어가면 지표가 망가진다.
로더를 쓰기 전에 거래량 0 인 행을 실제로 확인한다.

### Phase 2 순서를 바꿨다

`ROADMAP.md` 는 `KiwoomBroker` 를 먼저 만들고 그 위에 일봉 수집기를 얹는
순서였다. **일봉을 KRX 로 받기로 하면서 그 전제가 사라졌다.**

브로커 없이 일봉 적재를 시작할 수 있다. 이 프로젝트의 첫 번째 제약이
'데이터는 시간이 자산' 이었으므로, 하루라도 빨리 쌓는 쪽을 택한다.

출처를 이렇게 나눈다.

| 용도 | 출처 |
|---|---|
| 과거 일봉, 휴장일 | KRX 오픈API |
| 실시간 시세, 잔고·주문 | 키움 |

분봉은 KRX 에 없으므로 키움이다. 그래서 `KiwoomBroker` 가 여전히 필요하지만
일봉보다 뒤로 간다.

## 2026-08-27 (4) — 일별매매정보 실측 (2차), 거래량 0 처리 결정

`20260826` 로 세 시장을 실제로 불러 확인했다. 401 없이 응답했으므로
일별매매정보 이용신청 3종이 실제로 반영돼 있다.

### `apiId` 를 확정했다

| 시장 | apiId |
|---|---|
| 유가증권 | `stk_bydd_trd` |
| 코스닥 | `ksq_bydd_trd` |
| 코넥스 | `knx_bydd_trd` |

경로는 종목기본정보와 같은 `/svc/apis/sto/` 다.
1차 실측 때 이 값을 기록하지 않아 다시 확인해야 했다.
**엔드포인트 ID 는 실측할 때마다 적는다.**

### 거래량 0 이면 시·고·저가 `0` 이고 종가만 온다

| 시장 | 전체 | 거래량 0 |
|---|---|---|
| KOSPI | 944 | 37 |
| KOSDAQ | 1823 | 97 |
| KONEX | 108 | 29 |

거래량이 있는데 OHLC 에 `0` 이 섞인 행은 세 시장 통틀어 **0건**이었다.
`거래량 0` 과 `시·고·저 0` 이 예외 없이 같이 간다. 종가는 항상 값이 온다.

### 시·고·저를 종가로 채운다 (승인 받음)

거래량 0 인 날을 도지 캔들로 만든다. 근거는 셋이다.

- 종가가 온다는 것은 KRX 가 그날의 기준가를 확정했다는 뜻이다.
  거래가 없었으니 '가격이 움직이지 않은 날' 로 보는 것이 사실에 맞다
- `low <= open, close <= high` 불변식이 지켜진다. `0` 을 그대로 넣으면 깨진다
- **정보 손실이 없다.** `volume = 0` 이 그대로 남으므로 나중에
  '이날은 거래가 없었다' 를 언제든 복원할 수 있다

행을 건너뛰는 안도 있었으나, 장기 거래정지 종목의 시계열에 구멍이 생겨
이동평균이 조용히 틀어진다. 구멍보다 도지가 낫다.

`price_daily` 에 OHLC CHECK 제약이 없어서 `0` 이 와도 그대로 들어간다.
로더가 막지 않으면 아무도 막지 않는다.

### 종목코드에 영문자가 들어간다 — 함정

82건이다. 대부분 우선주의 `K`·`L` 접미다.

```
00104K CJ4우(전환)   37550L DL이앤씨2우(전환)   03473K SK우
0030R0 대신밸류리츠   0126Z0 삼성에피스홀딩스
```

종목 마스터 로더는 이미 정상 적재하고 있다. KOSDAQ·KONEX 의 시세 종목이
`stock` 에 100% 존재하는 것이 근거다.

**종목코드를 `\d{6}` 으로 검증하는 코드를 넣지 않는다.** 지금 멀쩡한 것이
검증을 추가하는 순간 82건을 버리게 된다.

### FK 갭은 KOSPI 26건, 전부 리츠 계열이다

KOSDAQ·KONEX 는 0건이다. 주권 계열만 담기로 한 결정과 정확히 맞아떨어진다.
건너뛰는 수가 이 규모를 크게 벗어나면 `stock` 적재가 밀린 것이므로
로그로 드러나게 한다.

## 2026-08-27 (5) — 일봉 로더 실적재 검증

`20260826` 하루치를 서버 DB 에 실제로 넣고 확인했다. **2849건 적재, 26건 건너뜀.**

| 확인 항목 | 결과 |
|---|---|
| 총 건수 | 2849 (= `stock` 테이블 건수) |
| 거래량 0 | 162 |
| OHLC 에 `0` 이 남은 행 | 0 |
| `low <= open, close <= high` 위반 | 0 |
| `adj_factor <> 1` | 0 |

거래량 0 이 실측 163건인데 적재는 162건이다. 차이 1건은 `KRX:348950`
(리츠) 로, `stock` 에 없어 건너뛴 것이다. 건너뛴 26건 안에 거래량 0 이
하나 섞여 있었을 뿐 누락이 아니다.

**건수 차이는 설명될 때까지 넘어가지 않는다.** 1건 차이가 매핑 버그일
수도 있었다.

### `adj_factor` 는 upsert 에서 갱신하지 않는다

`ON CONFLICT DO UPDATE` 절에 넣지 않았다. `corporate_action` 배치가 계산하는
파생값이라, 일봉을 재적재할 때마다 1 로 되돌아가면 계산 결과가 조용히 사라진다.

수집기가 채우는 컬럼과 배치가 채우는 컬럼을 upsert 절에서 분리한다.

### 서버 작업 환경

개발 PC 에 ssh 키를 만들어 `bh-server@bh-server` 에 등록했다.
이제 개발 PC 에서 서버 명령을 직접 실행할 수 있다.

**이번 검증은 push 없이 scp 로 파일만 올려서 돌렸다.**
서버의 `common/db/models.py` 가 커밋되지 않은 상태로 수정돼 있으므로,
다음 `git pull` 전에 `git checkout -- common/db/models.py` 가 필요하다.

## 2026-08-27 (6) — 3년치 백필, 생존편향 대응

### 현재 `stock` 만 보고 3년치를 받으면 생존편향이 박힌다

3년치 적재 직전에 규모를 쟀다.

| 기준일 | 시세 종목 | `stock` 에 없음 |
|---|---|---|
| 2023-08-29 | 2746 | 242건 (8.8%) |
| 2024-08-27 | 2832 | 202건 (7.1%) |
| 2025-08-26 | 2881 | 126건 (4.4%) |

상시 제외 대상인 리츠 26건을 빼면 3년 전 기준 **약 216건이 그 사이 사라진
종목**이다. DL건설·HD현대인프라코어·HD현대미포(합병), IHQ·KH필룩스 등이다.

그대로 받으면 폐지 종목의 과거 일봉이 통째로 빠진다. 백테스트가 살아남은
종목만 보게 된다. `SCHEMA.md` 가 `stock` 행을 삭제하지 않기로 한 이유와
정면으로 충돌하는 것을, 삭제가 아니라 **애초에 넣지 않는 방식으로** 저지를 뻔했다.

**적재를 시작하기 전에 무엇이 빠지는지 센다.** 다 쌓은 뒤에는 알아채기 어렵다.

### 종목기본정보도 `basDd` 를 받는다 (승인 받음)

과거 날짜로 부르면 그날 상장돼 있던 종목이 `SECUGRP_NM` 과 함께 온다.
주권 계열만 담는 기존 필터를 그대로 쓸 수 있다.

주 1회 스냅샷이면 157주 × 3시장 × 1.2초 ≈ 10분이다. 일봉 50분에 비하면 싸다.
간격을 좁힐 이유는 '상장 후 한 주 안에 폐지되는 종목' 인데 사실상 없다.

**순서를 뒤집었다.** checklist 는 일봉을 먼저 받고 종목 상태 갱신을 나중에
두었으나, 그 순서로는 3년치를 두 번 받아야 한다.

### 폐지일은 마지막 관측일의 다음 날이다 (승인 받음)

스냅샷을 시간순으로 돌며 종목별 마지막 관측일만 기억한다.
마지막 스냅샷에 없으면 폐지로 보고 `관측일 + 1일` 을 `delisted_at` 으로 넣는다.
스냅샷 간격만큼(최대 6일) 오차가 있다.

마지막 관측일만 보기 때문에 **사라졌다 다시 나타나는 종목이 자연스럽게 처리된다.**
증분으로 폐지를 표시했다면 되돌리는 코드가 따로 필요했다.

`stock_status` 는 건드리지 않는다. 폐지 종목에 열린 상태 행(`valid_to IS NULL`)이
생기면 틀린 이력이 된다. 상태 이력은 종목 상태 갱신 배치가 온전히 맡는다.

### 종목 수 급감을 중단 사유로 삼는다

스냅샷이 직전보다 `max_shrink_ratio` 넘게 줄면 예외를 던진다.
API 가 빈 응답이나 부분 응답을 주면 **전 종목이 폐지된 것으로 기록된다.**
되돌리기 어려운 사고라 진행보다 중단을 택했다.

임계값은 `config/collect.yaml` 에 둔다 (`CLAUDE.md` 절대규칙 1).

### 일봉 백필은 이미 받은 날을 건너뛴다

`price_daily` 에 있는 거래일을 먼저 읽고 빼고 시작한다. 중단해도 다시 돌리면
이어진다. 별도의 진행 상태 파일이 필요 없다. **DB 가 이미 진행 상황을 안다.**

토·일은 부르지 않는다. 공휴일은 불러야 알 수 있지만 주말은 확실하다.
하루가 실패해도 나머지를 계속 받고, 실패한 날은 다시 돌릴 때 자동으로 재시도된다.

## 2026-08-27 (7) — 백필 204만건을 잃은 트랜잭션 버그

일봉 백필이 `2045820건 적재, 실패 0일` 로 끝났는데 **DB 에는 한 건도 없었다.**

### 원인

`price_daily_backfill.main` 만 루프 전에 커서로 직접 SELECT 를 돌렸다.

```python
with connect() as conn:
    with conn.cursor() as cur:      # 여기서 암묵적 트랜잭션이 열린다
        known = known_stock_ids(cur)
    for day in days:
        with transaction(conn) as cur:   # 최상위가 아니라 세이브포인트가 된다
            upsert_price_daily(cur, kept)
```

psycopg 는 autocommit 이 아니면 첫 `execute()` 에서 트랜잭션을 연다.
그 상태에서 `conn.transaction()` 은 **세이브포인트**를 만들 뿐이다.
블록을 빠져나와도 커밋되지 않는다. `connect()` 의 `conn.close()` 가 전부 롤백했다.

실측으로 확인했다. `conn.info.transaction_status` 가 커넥션 직후 `IDLE(0)`,
커서로 SELECT 한 뒤 `INTRANS(2)`, `transaction()` 을 빠져나온 뒤에도 `INTRANS(2)` 다.
읽기를 `transaction()` 안에서 하면 매번 `IDLE(0)` 로 돌아온다.

다른 로더들이 멀쩡했던 이유는 `with connect() as conn, transaction(conn) as cur:`
로 커넥션 직후 바로 들어가서다. `migrate.py` 는 커서로 먼저 읽지만
`conn.commit()` 을 명시적으로 불러 넘어갔다.

### 배운 것

**"적재 완료" 로그는 커밋의 증거가 아니다.** `executemany` 는 성공했고
건수도 맞았다. 로더를 만들면 반드시 다른 커넥션으로 다시 읽어 확인한다.
이번에 잡힌 것도 로그가 아니라 `SELECT COUNT(*)` 덕분이었다.

**하나만 다른 코드를 의심한다.** 같은 일을 하는 네 개의 로더 중 하나만
구조가 달랐고, 그 하나가 틀렸다.

### `transaction()` 에 가드를 넣었다

이미 트랜잭션이 열려 있으면 `RuntimeError` 를 던진다.
조용한 데이터 유실은 가장 나쁜 실패 방식이라 진행보다 중단을 택했다.
기존 호출부는 모두 최상위라 영향이 없다 (`migrate.py` 는 이 헬퍼를 쓰지 않는다).

## 2026-08-28 — 하루 실측으로 '예외 없다' 고 단정한 것이 틀렸다

3년치를 넣고 나서 OHLC 에 `0` 이 남은 행이 4건 나왔다. 불변식도 4건 깨졌다.

| 종목 | 거래일 | 거래량 | 종가 |
|---|---|---|---|
| 동양2우B | 2024-03-28 | 2 | 11480 |
| 지에이이노더스 | 2024-12-30 | 35536 | 5590 |
| 다이나믹디자인 | 2025-03-21 | 1015 | 1126 |
| 라피치 | 2026-05-08 | 11000 | 6900 |

**거래량이 있는데 시·고·저만 `0` 이다.** 정리매매·시간외 거래로 보인다.
정규장 체결이 없으면 시·고·저가 안 오지만 거래량·거래대금은 남는 것이다.

2026-08-27 에 `20260826` 하루를 재고 '거래량 0 과 시·고·저 0 이 예외 없이
같이 간다' 고 적었다. 그날은 맞았고 3년으로 늘리니 틀렸다.

**하루치 표본으로 '예외 없다' 를 주장하지 않는다.** 2849건이 일치했다는 것은
그날 그 현상이 없었다는 뜻이지 규칙이 성립한다는 뜻이 아니다.
205만건 중 4건이라 하루를 봐서는 나올 수 없는 빈도였다.

### 판정 기준을 값으로 바꿨다

`if volume == 0` 에서 `if not open_ or not high or not low` 로 바꿨다.
**막으려는 것이 `0` 인데 `0` 을 직접 보지 않고 다른 값으로 추론했다.**
파생 조건이 아니라 문제 그 자체를 본다.

종가가 `0` 인 행은 205만건에 하나도 없다. 채울 값이 항상 있다.

## 2026-08-28 (2) — 3년치 적재 완료, `delisted_at` 이 일봉과 어긋난다

| 항목 | 값 |
|---|---|
| 총 행수 | 2,048,669 |
| 거래일 | 727 (2023-08-29 ~ 2026-08-26) |
| 종목 | 3,083 (폐지 234 포함) |
| OHLC `0` / 불변식 위반 / FK 고아 / 중복키 | 전부 0 |

### 스냅샷으로 역산한 폐지일이 일봉과 어긋난다

`delisted_at - 마지막 거래일` 이 -5 ~ +6일, 평균 -1.3일이다.
**234건 중 183건이 자기 폐지일 이후에도 거래된 것으로 남아 있다.**

주 1회 스냅샷의 오차라 예상된 것이지만, 단순한 부정확을 넘어 데이터가
자기모순이다. `trade_date < delisted_at` 같은 조건으로 거르면 실제 거래를 버린다.

이제 `price_daily` 가 있으므로 종목별 마지막 거래일에서 정확히 계산할 수 있다.
스냅샷 역산은 **일봉을 받기 전에 FK 를 풀기 위한 임시값**이었던 셈이다.

## 2026-08-28 (3) — 폐지일을 일봉으로 정밀화 (승인 받음)

`delisted_at` 을 **마지막 거래일 + 1일**로 맞췄다. 185건이 갱신됐다.

| | 정밀화 전 | 후 |
|---|---|---|
| `delisted_at - 마지막 거래일` | -5 ~ +6일 | **1 ~ 1일** |
| 폐지일 이후에도 거래된 종목 | 183건 | **0건** |

상장중 2849건은 그대로다. 이미 폐지로 표시된 종목만 손댄다.
**새로 폐지를 표시하지 않는다.** 그건 종목 상태 갱신 배치의 일이고,
상장중인 종목의 마지막 거래일은 그냥 마지막으로 적재한 날이라 의미가 없다.

### 마지막 날까지 거래된 종목은 갱신에서 뺀다

폐지로 표시됐는데 마지막 거래일이 적재 마지막 날이면, 폐지가 아니라 종목
마스터에서만 빠진 것일 수 있다. 그대로 매기면 미래 날짜가 들어간다.

처음에는 경고만 찍고 갱신은 그대로 하도록 짰다가 고쳤다.
**경고만 하고 틀린 값을 쓰면 가드가 아니다.** UPDATE 조건에서 제외한다.
지금 데이터에는 해당 종목이 없지만 매일 도는 배치에서는 생길 수 있다.

### 스냅샷 역산의 역할

주 1회 스냅샷 역산은 결국 **일봉을 받기 전에 FK 를 풀기 위한 임시값**이었다.
정확한 값은 일봉이 알고 있었다. 순서상 임시값이 필요했을 뿐이다.

## 2026-08-28 (4) — 일 1회 배치. 조용한 무동작을 두 번 피했다

### KRX 는 당일 데이터를 다음 날 공개한다

`2026-08-28 00:48` 기준으로 `20260827`(목) 이 0건이었다. `20260826` 은
`20260827 23:05` 에 정상 조회됐다. **당일 마감 직후에는 받을 것이 없다.**

처음 설계한 `평일 16:10` 타이머는 매일 '휴장일' 로 끝나면서 종료코드 0 을
돌려줬을 것이다. 알림도 안 가고 데이터는 8-26 에 멈춘 채였을 것이다.

### '오늘' 을 받지 않는다 (승인 받음)

`price_daily` 에 없는 최근 거래일을 찾아 채운다. 백필의 `missing_dates` 를
그대로 쓴다. **공개 시각을 몰라도 되고, 늦어지면 다음 날 따라잡는다.**
실행 시각에 둔감한 설계가 외부 일정에 대한 가정을 없앤다.

### 휴장일과 미공개는 순서로 구분한다 (승인 받음)

둘 다 0건이라 한 날짜만 봐서는 구분할 수 없다.
**공개가 날짜순이라는 성질을 쓴다.** 가진 데이터보다 앞에 있는 미적재일은
휴장일이고, 뒤에 있는 것만 지연이다. 지연이 `max_delay_days` 를 넘으면 알린다.

이 판정이 없으면 배치가 매일 조용히 아무것도 안 하는 상태를 아무도 모른다.
**아무 일도 일어나지 않는 것이 정상인 배치는 이상을 스스로 말해야 한다.**

### 종목기본정보는 휴장일에도 응답한다 — 함정

휴장일 판정을 이 API 로 하려다 발견했다. 2026-08-15(토)에 2846건이 왔다.
그 전에 이미 `stock` 과 `stock_status` 에 휴장일 스냅샷을 써버렸다.

`price_daily` 와 `stock` 은 무사했고 `stock_status` 에 폐지 종목 3건의
열린 행(`valid_to IS NULL`)이 생겼다. 지웠다.

근본 원인도 막았다. `open_stock_status` 가 `delisted_at IS NULL` 인 종목에만
행을 연다. 과거 날짜로 마스터를 돌리면 그때 살아 있던 폐지 종목이 들어오는데,
호출부가 조심하는 것보다 함수가 거부하는 편이 낫다.

**두 API 의 성질이 다르면 하나로 다른 하나를 추론하지 않는다.**
1차 실측 때 `ISU_CD` 로 같은 실수를 했고, 그때 적어둔 교훈을 또 어겼다.

### 남은 부채 — 폐지 감지

배치는 신규 상장은 반영하지만 상장폐지를 감지하지 못한다.
`delisted_refine` 은 이미 폐지로 표시된 종목만 다듬는다.
checklist 의 `종목 상태 갱신` 항목이 이것을 맡는다. **매일 부채가 쌓인다.**

## 2026-08-28 (5) — 종목 상태 갱신

`collectors.market.stock_status` 를 만들었다. 일 1회 배치에서 `stock_master`
자리를 대신한다. 마스터 upsert 를 겸하므로 둘 다 돌릴 이유가 없다.

### 감지와 적용

| 감지 | 판정 | 적용 |
|---|---|---|
| 상장 | 이번에 있고 상장중 목록에 없음 | 상태 행을 연다 |
| 폐지 | 상장중 목록에 있고 이번에 없음 | 이력을 끊고 `delisted_at` |
| 이전상장 | 양쪽에 있고 `board` 가 다름 | 끊고 새 시장으로 다시 연다 |
| 재등장 | 이번에 있고 폐지로 표시돼 있음 | 표시를 지우고 다시 연다 |

`valid_to` 가 배타적이라(`SCHEMA.md`) 변경일을 그대로 넣는다.
롤백 검증에서 이력에 겹침도 구멍도 없는 것을 확인했다.

### 재등장을 자동으로 되돌린다

한 번의 잘못된 스냅샷으로 200건이 폐지로 잡혀도 다음 날 스스로 풀린다.
수동 복구가 필요한 상태를 만들지 않는 편이 낫다고 봤다. 대신 경고를 남긴다.
**잦으면 폐지 감지가 오탐을 내고 있다는 신호다.**

### 종목기본정보의 공개 범위를 실측했다

| 날짜 | 마스터 | 시세 |
|---|---|---|
| 08-14 (금) | 942 | 942 |
| 08-15~17 (휴장) | 942 | 0 |
| 08-26 (수) | 944 | 944 |
| 08-27~28 (미공개) | 0 | 0 |

**마스터는 휴장일에도 직전 상태를 돌려주고, 공개 경계는 시세와 같다.**
그래서 미공개 날짜로 돌리면 0건이 오고, 휴장일로 돌리면 과거 상태가 온다.

### 과거 날짜로 시간을 되돌리지 못하게 막았다

`20260815` 로 상태 갱신을 돌리면 그날 이후 상장한 종목이 전부 폐지로 잡힌다.
급감 가드는 10% 기준이라 몇 건 차이를 못 잡는다.

`stock_status` 의 최대 `valid_from` 보다 이른 날짜는 거부한다(종료코드 2).
**API 를 부르기 전에 본다.** 어차피 못 쓸 응답을 받아올 이유가 없다.

이 함정은 dry-run 에서 `detect` 를 직접 부르다 발견했다. `20260827` 이
0건이라 2849 종목 전부가 폐지로 잡혔다. 운영 경로는 `is_holiday` 가 시세로
먼저 막아서 안전했지만, **막아준 것이 우연이면 다음에는 안 막힌다.**

### 채우지 못하는 것 — `is_managed` / `is_suspended`

KRX 종목기본정보에 관리종목·거래정지 필드가 없다. 2026-08-27 에
'Phase 2 상태 갱신 배치가 채운다' 고 적었지만 **채울 데이터가 없다.**

거래정지를 시세로 추정하는 안(시·고·저 0)은 쓰지 않는다.
거래가 없어서 0 인 종목과 구분되지 않는다. 코넥스에는 그런 종목이 흔하다.

출처를 찾아야 한다. 그때까지 두 컬럼은 계속 FALSE 다.
**유니버스 필터에서 이 값에 의존하면 안 된다.**

## 2026-08-28 (6) — 휴장일 역산 적재

`price_daily` 에서 역산해 55일을 넣었다. 백필 로그가 센 휴장일 수와 같다.

실제 달력과 대조했다.

```
2025-12-25 성탄절 / 2025-12-31 연말 휴장 / 2026-01-01 신정
2026-02-16~18 설 연휴 / 2026-03-02 삼일절 대체공휴일
```

연도별 2023년 7일(8월말부터) / 2024년 18일 / 2025년 19일 / 2026년 11일(8월까지).
주말 혼입 0건, 거래일과 겹침 0건이다.

### 주말은 담지 않는다

토·일은 변하지 않는 규칙이다. 데이터로 남기면 같은 지식이 두 곳에 생기고,
한쪽만 고치면 어긋난다. 코드가 이미 `weekday() < 5` 로 거르고 있다.
**조회하는 쪽이 요일을 함께 본다.**

`SCHEMA.md` 에 주말 포함 여부가 적혀 있지 않아 판단했다.

### 일봉이 있는 구간 안에서만 역산한다

구간 밖의 공백은 '아직 안 받은 날' 이지 휴장일이 아니다.
특히 최근 며칠은 KRX 가 공개하지 않은 것이다. `20260827` 을 휴장일로
적었다면 그날 데이터가 올라와도 영원히 휴장일로 남는다.

**없는 것에 의미를 부여할 때는 관측 범위를 먼저 정한다.**

### 이름은 채우지 않는다

역산으로는 알 수 없다. `upsert_holidays` 가 `COALESCE` 로 바뀌어서,
나중에 이름을 손으로 넣어도 배치가 지우지 않는다.

## 2026-08-28 (7) — `corporate_action` 출처 조사

`SCHEMA.md` 미결 사항이던 '키움 / DART / KRX 중 확인 필요' 를 조사했다.

### 문제 규모를 먼저 쟀다

가격제한폭이 ±30% 이므로 그것을 넘는 하루 변동은 조정 이벤트이거나
거래정지 관련이다. 3년치에서 **788건 / 560종목(전체의 약 20%)** 이 나왔다.
지금 이 종목들의 이동평균·수익률은 전부 틀려 있다.

### 정본은 KRX 일별매매정보의 `LIST_SHRS` 다

이미 받고 있는 응답에 상장주식수가 들어 있다. 실측 결과다.

| 종목 | 상장주식수 | 종가 |
|---|---|---|
| 쌍방울 2024-11-11 | 262,592,129 → 5,251,842 (÷50) | 269 → 13,450 (×50) |
| 광림 2024-10-22 | 90,706,696 → 3,023,556 (÷30) | 1,006 → 30,200 (×30) |
| 녹원씨엔아이 2024-07-18 | 16,440,776 (불변) | 11,540 → 235 |

**새 인증키도 새 API 이용신청도 필요 없다.** 일 단위라 권리락일이 정확하다.
`ratio` 는 상장주식수 비의 역수로 바로 나온다.

### 가격 갭만으로 판정하면 오탐이 난다

녹원씨엔아이는 상장주식수가 그대로인데 종가가 1/49 이 됐다.
거래정지 해제 후 재개다. 조정 대상이 아니다.
12건 표본에서 4건이 이런 경우였다.

### 시총 유지는 판별 기준이 될 수 없다 — 가설이 반증됐다

'조정 이벤트는 시가총액이 보존된다' 고 보고 `가격비 × 주식수비 ≈ 1` 로
거르려 했다. 표본에서 세 건이 걸러졌는데 전부 **실제 감자**였다.

```
040610  주식수비 0.5  가격비 1.75  시총비 0.876
365590  주식수비 0.2  가격비 6.55  시총비 1.309
049470  주식수비 0.2  가격비 3.54  시총비 0.708
```

감자와 동시에 시장이 재평가하면 시총비가 1에서 벗어난다.
**기계적 조정과 시장 변동이 같은 날 겹친다.** 둘을 분리하는 것이
조정계수의 목적이지, 겹치지 않는다고 가정할 근거가 아니었다.

판정은 `LIST_SHRS` 변화 유무만으로 한다. 비율도 거기서 나온다.
**가격은 결과이지 근거가 아니다.**

### KRX 로 안 되는 것

- **현금배당** — 일별매매정보에 없다. `adjusts_price=FALSE` 라 조정계수에는
  영향이 없지만 기록은 남겨야 한다
- **유상증자와 무상증자 구분** — 둘 다 상장주식수가 는다.
  `adjusts_price` 가 갈리므로(무상 TRUE / 유상 FALSE) 구분이 필요하다.
  액면분할은 종목기본정보의 `PARVAL` 변화로 구분된다

DART 가 필요한 지점이 여기다. `DART_API_KEY` 는 비어 있다.

### 저장 문제

`price_daily` 에 `LIST_SHRS` 컬럼이 없다. 2026-08-27 에 '쓰지 않는다' 고
결정했는데, 그때는 조정 이벤트의 정본인 줄 몰랐다.

앞으로는 `stock.listed_shares` 가 직전 값을 들고 있으므로 상태 갱신 배치가
비교할 수 있다. 스키마 변경이 필요 없다.
과거 3년치는 일별매매정보를 다시 받아야 한다 (약 50분).

## 2026-08-28 (8) — 조정 이벤트 판정, 가설을 두 번 고쳤다

`corporate_action` 감지를 만들면서 판정 기준을 두 번 틀렸다. 기록해 둔다.

### 1차 가설 — 시총 보존 (틀림)

'조정 이벤트는 시가총액이 보존된다' 고 보고 `가격비 × 주식수비 ≈ 1` 로
거르려 했다. 12건 표본에서 이 필터에 걸린 3건이 전부 실제 감자였다.

```
040610  주식수비 0.5  가격비 1.75  시총비 0.876
365590  주식수비 0.2  가격비 6.55  시총비 1.309
```

감자와 동시에 시장이 재평가한다. **기계적 조정과 시장 변동은 같은 날 겹친다.**

### 2차 가설 — 주식수 변화만 보기 (역시 틀림)

'가격은 결과이지 근거가 아니다' 라며 주식수 변화만으로 판정했다.
6거래일 시험 실행에서 오탐이 나왔다.

| 종목 | 주식수 | 가격비 | 실체 |
|---|---|---|---|
| 264450 | -3% | 0.9986 | 자기주식 소각 |
| 035720 | 증가 | 0.9576 | 전환사채·스톡옵션 |
| 102280 | ÷50 | 50.00 | 감자 (진짜) |

소각·전환·행사는 주식수만 바뀌고 가격은 연속이다. 하루 5~9건씩 나오므로
3년이면 수천 건이 잘못 들어갈 뻔했다.

### 확정 — 두 신호가 모두 있어야 한다

| 신호 | 역할 | 단독으로 쓰면 |
|---|---|---|
| 상장주식수 변화 | 이벤트 후보 + **정확한 비율** | 소각·전환이 섞인다 |
| 가격제한폭 초과 | **가격 조정 대상인지** 판별 | 거래정지 해제가 섞인다 |

권리락일에는 조정 후 기준가로 제한폭이 적용되므로 기계적 조정은 반드시
±30% 를 넘는다. 소각·전환은 절대 넘지 않는다. 시장 규칙이라 휴리스틱이 아니다.

**비율은 가격이 아니라 주식수에서 얻는다.** 가격에는 시장 변동이 섞여 있고,
그 둘을 분리하는 것이 조정계수의 목적이다.

### 배운 것

세 번의 판정 기준이 전부 그럴듯했고 앞의 둘은 데이터가 반증했다.
**작은 구간으로 먼저 돌려 실제 행을 눈으로 본 것이 매번 잡아냈다.**
6거래일 시험 실행이 없었다면 45분을 돌리고 수천 건의 오탐을 넣었을 것이다.

큰 배치 전에 작은 구간을 돌리고 결과를 읽는다.

### 감자를 병합과 묶었다 (승인 사항)

`SCHEMA.md` 의 `action_type` 에 감자가 없다.
주식수가 줄면 전부 `merge` 로 넣고 `detail` 에 전후 주식수를 남긴다.
`adjusts_price` 는 어느 쪽이든 TRUE 라 조정계수에는 영향이 없다.

### 주식수 증가는 보류한다 (승인 사항)

무상증자(조정 대상)와 유상증자(조정 대상 아님)가 섞인다.
액면분할은 종목기본정보의 `PARVAL` 변화로 구분되지만 아직 받지 않는다.
DART 키가 생기면 함께 처리한다.

## 2026-08-28 (9) — 3년 스캔 결과, 판정 기준을 세 번째로 고쳤다

**377건 / 335종목 / 2023-09-12 ~ 2026-08-26.** 후보 788건 중 377건만 남았다.
주식수가 그대로인 거래정지 해제가 걸러진 것이다.

비율 분포가 교과서적이었다.

```
0.2(5:1) 158건  0.1(10:1) 70건  0.5(2:1) 52건  0.3333 15건  0.25 11건
```

### 3차 수정 — 임의 비율은 조정 대상이 아니다

1에 가까운 비율 4건을 열어 보다 발견했다. 전체를 훑으니
**354건은 단순 분수이고 23건은 임의 비율**이었다.

| 종목 | 1/비율 | 실체 |
|---|---|---|
| 삼성바이오로직스 2025-11-24 | 1.5375 | 인적분할 |
| 삼양홀딩스 2025-11-24 | 1.1063 | 인적분할 (같은 날) |
| SK디앤디 / 태영건설 / 유니퀘스트 | 1.30 / 3.39 / 1.20 | 분할 |
| 코스온 / 큐에이드 | 1.0664 / 1.0952 | 자기주식 소각 |

**주식수 비가 이들의 가격 조정 비율이 아니다.** 인적분할은 분할 가치 비율로
조정해야 하고, 소각은 애초에 가격을 조정하지 않는다.
그대로 적용하면 시계열이 지금보다 더 틀어진다.

감자·액면병합은 `1/2`, `1/5`, `2/5` 처럼 분모가 작은 분수다.
주식수 비의 역수가 그렇게 떨어지는지로 가른다. 23건은 기록은 남기되
`adjusts_price = FALSE` 로 둔다.

### 괴리(시총비)로는 못 가른다 — 다시 확인

`가격비 × 주식수비` 는 377건 중 185건만 0.9~1.1 안에 있다.
실제 20:1 감자(`057880`)도 괴리가 0.50 이다. 감자와 시장 재평가가 겹쳐서다.
**1차 가설이 왜 틀렸는지 전체 데이터로 재확인됐다.**

### 판정 기준의 최종 형태

| 조건 | 역할 |
|---|---|
| 상장주식수 감소 | 이벤트 후보 + 비율 |
| 가격제한폭 초과 | 가격 조정을 동반했는가 |
| 비율이 단순 분수 | 주식수 비가 곧 조정 비율인가 |

세 번 다 '그럴듯한 단일 기준' 으로 시작했다가 데이터에 반증됐다.
**작은 표본을 열어 실제 행을 눈으로 본 것이 매번 잡아냈다.**

### 남은 구멍

인적분할 종목의 시계열은 여전히 끊겨 있다. 분할 가치 비율이 필요한데
KRX 일별매매정보에 없다. DART 가 필요하다.

## 2026-08-28 (10) — 키움 브로커와 분봉 수집기

### 분봉 가격에 전일대비 부호가 붙는다 — 함정

`ka10080` 응답의 `open_pric` 이 `-257000` 으로 온다. 음수 가격이 아니라
하락 표시다. 900건 3600개 값 중 `-` 1520개, `+` 2076개, 부호 없음 4개였다.

**`Decimal("-257000")` 을 그대로 쓰면 음수 가격이 저장된다.**
`price_minute` 에 가격 CHECK 제약이 없어 아무도 막지 않는다.

일봉(`ka10081`)은 `cur_prc` 에 부호가 없다. **API 마다 다르다.**
KRX 에서 `ISU_CD` 로 같은 실수를 했었다. 필드별로 실측한다.

### 유량이 1 이다

같은 `api-id` 를 연속 호출하면 즉시 429 다. 응답에
`허용된 API 요청 개수를 초과하였습니다. 유량=1` 이 온다.

| 간격 | 결과 |
|---|---|
| 0.0초 | 2회째 429 |
| 0.5초 | 8회 중 2회 429 |
| 1.0초 | 8회 모두 성공 |

한도는 `api-id` 별이다. 다른 `api-id` 를 섞으면 걸리지 않았다.

### 스로틀은 호출이 끝난 시각부터 잰다

처음에는 호출 **시작** 시각을 기록했다. 응답 시간만큼 간격이 줄어
연속조회(`cont-yn`)에서 429 가 났다. 실측은 응답을 받은 뒤 `sleep` 했으므로
끝-시작 간격 1초였는데, 코드는 시작-시작 1초였다.

**측정 조건과 구현 조건이 같은지 확인한다.** 같은 '1초' 가 다른 1초였다.

### 분봉은 일봉과 타이머를 나눴다

KRX 일봉은 다음 날 공개되지만 **키움 분봉은 당일 바로 온다.**
한 타이머에 묶으면 분봉이 하루 늦어진다. 출처의 공개 주기가 다르면
수집 주기도 나눈다.

### 관심종목은 거래대금 상위에서 뽑는다

별도의 관심종목 표를 두지 않았다. 최근 거래일 거래대금 상위 200개를 쓴다.
단타 대상은 유동성이 있어야 하고, 폐지 종목은 최근 거래일에 없으므로
자연히 빠진다. 표를 만들면 갱신 주체가 하나 더 생긴다.

200종목 18만봉에 3분 47초다. 유량이 1 이라 종목당 1초가 하한이다.

### 조회 재시도만 넣었다

`TransientError` 에 한해 지수 백오프로 다시 건다 (`INTERFACES.md` 2.3).
`RateLimitError` 가 `TransientError` 를 상속하므로 429 도 여기서 풀린다.
**주문은 이 경로를 쓰지 않는다.** 주문 재시도는 중복 주문이 된다.

### 아직 없는 것

`get_balance` / `get_positions` 는 `NotImplementedError` 다.
주문과 웹소켓도 Phase 8 이다. 이번 단계의 목표는 분봉 수집이었다.

## 2026-08-28 (11) — `trading_flow` 수집

### KRX 오픈API 에 투자자별 거래실적이 없다 — 문서 기재가 틀렸다

`PROJECT.md` 지표 표에 '외국인 순매수 / 일 / KRX' 로 적혀 있었으나
KRX 오픈API 서비스 목록에는 지수·주식·파생·채권의 일별매매정보뿐이다.
투자자 유형별 데이터가 없다.

**휴장일 때와 같은 종류의 오류다.** 2026-08-26 에 '휴장일도 KRX 매매거래일정을
쓴다' 고 적었다가 서비스 목록에 없어 뒤집었다. 설계 시점의 '있을 것이다' 가
문서에 사실처럼 남았다.

키움 `ka10059`(`/api/dostk/stkinfo`) 로 간다. `PROJECT.md` 표를 고쳤다.

### 값은 백만원 단위 금액이다

`amt_qty_tp` 와 `unit_tp` 를 바꿔도 모의투자에서는 응답이 같았다.
파라미터로 확인할 수 없어 값 자체로 갈랐다.

| 종목 | 종가 | 거래량 | 개인 순매수 원값 |
|---|---|---|---|
| KRX:368600 | 1,050원 | 18,943,094주 | **98** |

98주는 1,894만주 거래에서 말이 안 된다. 98백만원(9,800만원)은 거래대금
210억의 0.5% 로 타당하다. `acc_trde_prica` 도 같은 백만원 단위였다.

`price_daily.value` 가 원 단위이므로 원으로 바꿔 저장한다.

**파라미터 설명을 못 믿을 때는 값의 크기로 판정한다.** 가격대가 크게 다른
종목 둘을 비교하면 수량과 금액이 갈린다.

### 부호를 벗기면 안 된다 — 분봉과 반대다

분봉 가격은 `-257000` 이 하락 표시라 `abs()` 로 벗겼다.
수급은 `-862106` 이 순매도라는 뜻이다. **같은 제공자라도 필드마다 다르다.**

같은 날 두 API 에서 정반대 처리를 해야 했다. 한쪽 규칙을 다른 쪽에
옮기면 조용히 틀린다.

### 검산이 되는 데이터다

개인 + 외국인 + 기관 + 기타법인 + 내외국인 = 0 이었다. 매수한 만큼 누군가 팔았다.
기관 세부 8개 합이 `orgn` 과 정확히 맞았다. 테스트로 고정했다.

**합이 0 이라는 성질은 파싱이 맞는지 스스로 말해준다.**

### 한 번에 100 거래일이 온다

매일 돌리면 과거분이 함께 메워진다. 별도 백필이 필요 없다.
200종목 19,439행에 3분 30초다.

### 드리프트 테스트가 컬럼을 빠뜨리고 있었다

`foreign_net` 을 `FOREIGN KEY` 로 오인해 건너뛰고 있었다.
`startswith(("PRIMARY","FOREIGN",...))` 가 컬럼명에도 걸렸다.
`"FOREIGN KEY"` 처럼 뒤 토큰까지 보도록 고쳤다.

**새 테이블을 붙이지 않았으면 드러나지 않았을 버그다.**

## 2026-08-28 (12) — 분봉 월 파티션 자동 생성

마이그레이션이 2026-08 ~ 2027-12 를 만들어 두고 '이후 월분은 운영 배치가
미리 생성해야 한다' 고 적어 둔 것을 구현했다. 18개월치를 확보한다.

### DDL 에는 파라미터를 쓸 수 없다

`FOR VALUES FROM (%s) TO (%s)` 로 짰다가 `there is no parameter $1` 로 실패했다.
`psycopg.sql.Literal` 로 박아 넣어야 한다.

**개발 PC 에서는 통과했다.** DB 통합 테스트가 `DATABASE_URL` 이 없어 skip 되고,
DDL 은 실행해 봐야 알 수 있는 오류였다. 2026-08-25 에 '서버에서 한 번
돌려야 실제로 검증된 것' 이라고 적어둔 것이 그대로 맞았다.

### 파티션 생성은 실패하면 멈춘다

키움 유닛의 `ExecStart` 중 파티션만 `-` 를 붙이지 않았다.
파티션이 없으면 분봉 적재가 통째로 실패하므로, 뒤를 돌릴 이유가 없다.
분봉과 수급은 서로 의존하지 않으니 `-` 를 붙였다.

### 경계는 UTC 다

`psql` 이 세션 타임존(KST)으로 보여주므로 `2028-01-01 09:00:00+09` 로 표시된다.
UTC 자정과 같은 시각이다. **표시 형식을 값의 오류로 오해하지 않는다.**

마이그레이션이 만든 파티션과 형식이 이어지는지 확인했다.
`202712` 의 끝과 `202801` 의 시작이 정확히 같다.

## 2026-08-28 (13) — Phase 3 시작, 수집기 플러그인 구조

### Phase 3 는 DART 를 쓰지 않는다

사용자가 DART 키를 확보했다고 알렸으나 Phase 3(시장분석)의 출처는
KRX·금융투자협회·관세청·환율이다. DART 는 Phase 2 잔여분(인적분할·배당)과
Phase 5(정보수집)에서 쓴다.

**키는 아직 서버 `.env` 에 없다.** 값을 받아 적지 않는다. 사용자가 직접 넣는다.

### 막혀 있는 것을 먼저 셌다

`indicator` 8종 중 지금 받을 수 있는 것이 거의 없다.

| 지표 | 출처 | 상태 |
|---|---|---|
| `VKOSPI` / `KOSPI_MA200_GAP` | KRX 지수 API | **401 미신청** |
| `DEPOSIT` / `CREDIT_BALANCE` | 금융투자협회 | 출처 미조사 |
| `USDKRW` | ECOS 등 | 출처 미확정 |
| `EXPORT_YOY` / `EXPORT_SEMI_YOY` | 관세청 | 출처 미조사 |
| `FOREIGN_NET` | `trading_flow` | 상위 200종목만 있다 |

KRX 지수는 `idx/kospi_dd_trd` 로 401 이 왔다. **경로는 맞다.**
401 JSON 이 온다는 것이 경로가 맞다는 신호라는 것을 2026-08-27 에 배웠다.

**막힌 것을 먼저 세면 사용자가 병렬로 신청할 수 있다.** 코드를 다 짜고
나서 "그런데 키가 없습니다" 라고 하면 그 시간이 낭비된다.

### 실패 알림은 한 번으로 울리지 않는다

`PROJECT.md` 10장이 수집기 실패를 '1시간 내 반복 시' 로 규정한다.
한 번 실패로 울리면 일시 장애마다 알림이 온다.

`event_log` 의 최근 ERROR 수를 세서 판단한다. 별도의 실패 카운터를 두지 않는다.
**이미 남기는 기록이 판단 근거가 되면 상태가 하나 줄어든다.**

### `change_rate` 는 시계열 전체를 다시 센다

증분으로 계산하면 값이 나중에 정정될 때 뒤따르는 변화율이 틀린 채로 남는다.
지표 표는 작으므로 해당 지표 전체를 `LAG` 로 다시 센다.

### 파이프가 린트 실패를 가렸다

`ruff check ... | tail -2 && git commit` 으로 묶었더니 `tail` 의 종료코드가 0 이라
린트 5건이 남은 채로 커밋됐다. **파이프 뒤의 `&&` 는 앞 명령의 성공을 보장하지 않는다.**

## 2026-08-28 (14) — RegimeEngine

### 계층 이름은 `indicator.layer` 를 따랐다

`INTERFACES.md` 8.1 규칙 파일 예시는 계층이 `fundamental` / `daily` 두 개인데,
`SCHEMA.md` 의 `indicator.layer` 는 `sentiment` / `risk` / `position` /
`fundamental` 네 개다.

예시는 형식을 보이려는 것이고 '# ... 나머지 5종' 으로 끝난다.
**`layer_scores` 가 지표 정의와 맞아야 화면에서 대조할 수 있으므로**
네 계층을 썼다. `daily` 는 계층이 아니라 갱신 주기다.

### 점수는 두 임계값 사이의 선형 보간이다

`danger` 에서 -1, `safe` 에서 +1, 밖은 잘라낸다.
**`danger` 가 `safe` 보다 커도 된다.** VKOSPI 처럼 값이 클수록 위험한 지표는
분모가 음수가 되어 방향이 저절로 뒤집힌다. 지표마다 부호 규칙을 따로 두지
않아도 된다.

### 결측을 0 으로 넣으면 안 된다

`INTERFACES.md` 8.2 가 명시한다. 0 은 '중립' 이라는 판단인데 결측은 판단이 아니다.

가중평균을 남은 항목만으로 계산하면 정규화가 저절로 된다.
계층 안이 전부 비면 그 계층도 빼고, 계층 가중치도 남은 것으로 다시 나눈다.

테스트로 고정했다. 지표 하나가 빠져도 나머지가 같으면 점수가 같아야 한다.

### 쓸 수 있는 지표가 없으면 판정하지 않는다

`InsufficientData` 를 던진다. 중립으로 적으면 **'판단했다' 는 거짓 기록**이
`market_regime` 에 남는다. 나중에 규칙을 검증할 때 그 행이 노이즈가 된다.

### 묵은 값에 유효기간을 뒀다

규격에 없지만 넣었다. `max_age_days` 가 없으면 죽은 소스의 마지막 값이
영원히 판정에 남는다. 월간 지표는 60일, 일간은 5일로 뒀다.

### 구조를 문서에 맞췄다

`collectors/indicator/` 를 만들었다가 `collectors/market/` 으로 옮겼다.
`PROJECT.md` 4장이 `collectors/` 아래에 `news/` 와 `market/` 만 두고
'시장지표, 국면 판정' 을 `market/` 에 넣는다.

**코드가 쌓이기 전에 고쳤다.** 나중에 옮기면 import 경로가 사방에 퍼진다.

## 2026-08-28 (15) — 국면 판정 적재와 전환 알림

네 경로를 실제 DB 로 확인하고 되돌렸다.

| 상황 | 결과 |
|---|---|
| 첫 판정 (직전 없음) | `safe`, 알림 0건 |
| 같은 국면 유지 | 알림 0건 (무음) |
| `safe` -> `danger` | 알림 1건 |
| `is_override = TRUE` | 덮어쓰지 않고 종료 |

### 판정 기준일은 최신 거래일이다

오늘로 잡으면 휴장일에 판정이 실패한다. `price_daily` 의 최대 `trade_date`
를 쓴다. 이미 휴장일을 걸러 넣은 표라 별도 달력이 필요 없다.

### 지표가 없으면 보류하고 종료코드 0 이다

실패가 아니다. 지표 수집기가 아직 없으니 정상 상태다.
1 을 돌려주면 systemd 가 실패로 보고 알림이 매일 울린다.
`event_log` 에 WARN 만 남긴다.

### 스냅샷의 `Decimal` 을 문자열로 넣는다

JSON 이 `Decimal` 을 직렬화하지 못한다. `float` 로 바꾸면 값이 미세하게
달라진다. **스냅샷은 판정 근거이므로 보이는 그대로 남긴다.**

### `metric` 은 SQL 에 그대로 들어간다

설정 파일의 `metric` 으로 컬럼명을 고르므로 문자열 보간이 불가피하다.
`METRICS` 집합으로 먼저 거르고, 아니면 `ValueError` 를 던진다.
테스트에 `"value; DROP TABLE stock"` 을 넣어 거부되는 것을 고정했다.

**설정 파일도 신뢰 경계다.** 사람이 쓰는 파일이라 오타가 SQL 로 샌다.

## 2026-08-28 (16) — 지표 8종 출처 조사

### KRX 지수 이용신청을 하지 않아도 된다

사용자에게 'KRX 지수 API 신청이 가장 급하다' 고 말한 뒤,
**신청 전에 키움에 있는지부터 확인했다.** 다 있었다.

```
ka20003 /api/dostk/sect   전업종지수 31건
  001 종합(KOSPI) / 603 변동성지수 / 604 코스피고배당50 ...

ka20006 /api/dostk/chart  업종일봉 600건 (약 2.4년)
  001 20260828  678888  -> 6788.88
  603 20260828    5008  ->   50.08
```

`VKOSPI`, `KOSPI_MA200_GAP`, 익일 `kospi_return` 이 한 번에 풀렸다.
600건이면 200일 이평에 충분하다.

**신청을 부탁하기 전에 이미 가진 것을 뒤진다.** 하루 이틀이 걸리는 절차를
안 밟아도 됐다.

### 지수 값은 100배로 온다 — 배율 함정

`ka20006` 이 `678888` 을 주고 `ka20003` 은 같은 값을 `-6788.88` 로 준다.
**같은 제공자의 두 API 가 배율이 다르다.**

분봉 가격의 부호, 수급의 단위(백만원)에 이어 세 번째다.
키움은 API 마다 표현이 다르다고 보고 매번 실측한다.
`ka20003` 과 대조해서 확정했다. 한쪽만 봤으면 지수가 100배로 들어갔을 것이다.

### `ka20007` 을 일봉으로 착각할 뻔했다

`ka20007` 이 300건을 주길래 일봉인 줄 알았는데, 고가·저가 폭이 한 주치였다.
주봉이다. `ka20008` 은 월봉(`inds_mth_pole_qry`), 일봉은 `ka20006`
(`inds_dt_pole_qry`)이다.

**건수만 보고 주기를 단정하지 않는다.** 응답 필드 이름과 값의 폭이 말해준다.

### 나머지는 두 곳뿐이다

| 지표 | 출처 | 승인 |
|---|---|---|
| `DEPOSIT` / `CREDIT_BALANCE` | data.go.kr 금융위원회_금융투자협회종합통계정보 | 자동 |
| `EXPORT_YOY` / `EXPORT_SEMI_YOY` | data.go.kr 관세청_품목별 수출입실적(GW) | 개발단계 자동 |
| `USDKRW` | 한국은행 ECOS | 가입 시 자동 |

금융투자협회 **자체** 오픈API(`openapi.kofia.or.kr`)는 심의 승인이고
제공 데이터가 Do-not-call·교육원 위주다. **data.go.kr 쪽이 맞는 길이다.**

### 아직 확인 못 한 것 둘

- 투자자예탁금이 '증시 자금 흐름' 오퍼레이션에 있는지. 신용공여 잔고는 확인됐다
- `PROJECT.md` 는 수출 **20일 잠정치**를 쓴다고 적었는데, 조사한 품목별
  수출입실적은 월 단위(매월 15일경 전월 확정)다. 다른 자료다

**문서가 요구하는 것과 찾은 것이 다르면 적어 둔다.** 구현할 때 이 차이가
드러나면 그때는 이미 코드를 짠 뒤다.

## 2026-08-28 (17) — 키움 지수 기반 지표 3종

VKOSPI 254일, KOSPI 200일 이격도 254일을 넣고 국면 판정이 처음으로 돌았다.

```
2026-08-27 neutral 점수 -0.200
  layer_scores {'risk': '-1', 'position': '1'}
  indicators   {'VKOSPI': '53.01', 'KOSPI_MA200_GAP': '15.535859'}
  kospi_return 1.5299  kosdaq_return 1.3037
```

### 결측 정규화가 실전에서 확인됐다

USDKRW·DEPOSIT·CREDIT_BALANCE·FOREIGN_NET·EXPORT 가 전부 없는 상태다.

- `risk` 계층은 VKOSPI 만 남아 그 값이 곧 계층 점수가 된다 (-1)
- `position` 계층은 이격도 하나뿐이다 (+1)
- 계층 가중치 0.30 / 0.20 을 남은 둘로 정규화하면 0.6 / 0.4
- `0.6 x (-1) + 0.4 x (+1) = -0.2`

**손으로 센 값과 저장된 점수가 정확히 같았다.** 단위 테스트가 맞다는 것과
실제로 맞게 도는 것은 다른 문제라서 한 번은 손으로 검산했다.

### 값 검증은 다른 API 와 대조했다

`ka20006` 으로 넣은 VKOSPI 최신값 50.08 이 `ka20003` 현재가 `-50.08` 과 같다.
**배율을 잘못 잡았으면 5008 이 들어갔을 것이고, 그래도 테스트는 통과했을 것이다.**
같은 값을 주는 다른 경로가 있으면 그것으로 대조한다.

### 지표 수집이 하루에 두 번 돈다

시장 등락률은 판정 행이 있어야 채울 수 있고, 판정은 지표가 있어야 한다.

```
지표 수집 -> 국면 판정 -> 지표 수집(등락률 채우기)
```

한 번에 하려면 순서를 뒤섞거나 모듈이 서로를 알아야 한다.
**같은 배치를 두 번 돌리는 편이 단순하다.** 지표는 덮어써도 같은 값이다.

### 이평 구간이 모자라면 예외가 아니라 실패다

`CollectResult(success=False)` 로 돌려준다. 예외를 던지면 실행기가 잡긴 하지만,
'데이터가 모자람' 은 예상된 상태이지 사고가 아니다.
200일 이평은 상장 초기 종목이나 신설 지수에서 늘 겪는다.

## 2026-08-28 (18) — 수급을 전 종목으로, `FOREIGN_NET` 추가

상위 200종목 합계는 시장 전체가 아니다. 상장 2849종목 전부로 넓혔다.
키움 유량이 1 이라 약 48분이다.

### 규모가 커지자 두 장치가 필요해졌다

**실패 비율 임계값.** 200종목일 때는 실패 0건이었지만 전 종목을 돌면
거래정지·신규상장으로 몇 건은 늘 실패한다. 한 건에 종료코드 1 을 돌려주면
systemd 가 매일 실패로 보고 알림이 울린다. `max_failure_ratio` 를 넘을 때만
실패로 끝낸다.

**커버리지 하한.** 일부만 수집된 날의 합계는 '시장 전체 수급' 이 아니라
'그날 받은 것들의 합' 이다. 그 값이 지표로 들어가면 **다른 날과 비교할 수 없다.**
상장 종목 수의 90% 이상이 모인 날만 집계한다.

기존 200종목 데이터가 그대로 남아 있으므로 이 장치가 없으면
과거 100일이 200종목 합으로 들어갔을 것이다. 값은 그럴듯해 보이고
단위도 맞아서 **눈으로는 잡히지 않는다.**

### 테스트가 실제 데이터와 충돌했다

`indicator_value` 에 실제 VKOSPI 254건이 들어온 뒤 DB 통합 테스트 2개가 깨졌다.
테스트가 `VKOSPI` 코드로 행을 넣고 단정을 세웠는데, 실제 행이 섞여
`value_as_of` 가 다른 날짜를 돌려줬다.

롤백하는 트랜잭션 안이라 흔적은 남지 않았지만, **읽기는 이미 있는 행까지 본다.**
격리는 쓰기에만 적용된다.

테스트 전용 코드(`TEST_IND`)를 트랜잭션 안에서 만들어 쓰도록 고쳤다.
**공유 테이블이 비어 있다고 가정하지 않는다.**

어제 `stock`·`price_daily` 테스트가 무사했던 것은 `999990` 같은 없는 종목코드를
쓴 덕분이었다. 지표는 코드가 8개로 정해져 있어 같은 방법을 못 썼다.

## 2026-08-29 — 전 종목 수급 완료, 임계값이 포화돼 있다

**2849종목 283,137행, 실패 0건.** 거래일 100일 전부 커버리지 98.9% 이상이라
집계에서 빠진 날이 없다. `FOREIGN_NET` 값은 `trading_flow` 직접 합계와 일치한다.

국면 판정이 세 계층으로 돌았다.

```
2026-08-27 neutral 0.294
  {'risk': '-1', 'position': '1', 'sentiment': '1'}
  (0.35x1 + 0.30x(-1) + 0.20x1) / 0.85 = 0.294   손계산과 일치
```

`fundamental` 이 통째로 빠져 계층 가중치 합이 0.85 로 정규화됐다.

### 임시 임계값이 85~99% 포화다

| 지표 | 실제 범위 | 임계값 | 포화 |
|---|---|---|---|
| `FOREIGN_NET` | -7.7조 ~ +7.5조 | ±0.3조 | 87% |
| `VKOSPI` | 18.4 ~ 96.9 | 25 / 15 | 85% |
| `KOSPI_MA200_GAP` | -2.2 ~ 82.8 | -10 / 5 | 99% |

**포화된 지표는 ±1 이진 신호다.** 선형 구간을 안 쓰므로 값의 크기가 판정에
반영되지 않는다. `ROADMAP.md` 가 '맞을 리가 없다' 고 한 그대로였지만,
얼마나 어긋났는지는 데이터를 넣어 봐야 알았다.

관측 분위수는 이렇다.

```
FOREIGN_NET(조원)  10% -4.45  25% -2.72  50% -0.78  75% 0.58  90% 2.03
VKOSPI             10% 21.0   25% 30.5   50% 49.7   75% 67.8  90% 84.3
KOSPI_MA200_GAP    10% 16.9   25% 25.2   50% 36.4   75% 50.1  90% 66.3
```

### 합성 데이터를 의심했다가 아니었다

VKOSPI 중앙값 49.7 과 이격도 중앙값 36% 가 비현실적이라 모의투자 서버가
가짜 값을 준다고 의심했다.

**우리가 가진 KRX 일봉으로 시총가중 등락률을 내어 대조했다.**

| 거래일 | KRX 시총가중 | 키움 지수 |
|---|---|---|
| 2026-08-27 | 1.4476 | 1.5299 |
| 2026-08-24 | -3.2779 | -3.1244 |
| 2026-08-20 | 5.9917 | 5.8940 |

거의 일치한다. 지수는 진짜고 이 장이 실제로 극단적이다.
하루 ±3~6% 가 나오는 시장이면 VKOSPI 50 이 맞다.

**직관이 데이터를 의심할 때는 독립된 출처로 대조한다.** 의심을 근거로
데이터를 버렸으면 멀쩡한 지표를 버릴 뻔했다.

### 임계값은 지표 8종이 다 붙은 뒤 조정한다 (승인 사항)

지금 조정하면 두 번 하게 된다. 남은 4종(`DEPOSIT`·`CREDIT_BALANCE`·
`USDKRW`·`EXPORT` 계열)이 붙으면 계층 가중치 정규화도 달라진다.

**포화 상태를 알고 두는 것과 모르고 두는 것은 다르다.**
지금 판정은 세 지표의 부호만 반영한다. `market_regime` 이력에 이 시기가
남으니, 나중에 조정할 때 `rule_version` 으로 전후를 가를 수 있다.

## 2026-08-29 (2) — 실패 격리 검증에서 알림 결함이 드러났다

Phase 3 완료 기준인 '소스 하나를 강제로 실패시켜도 나머지가 정상 동작' 을
실제 수집기로 확인했다. 5개 중 2개를 강제 실패시켰다.

| 수집기 | 결과 |
|---|---|
| `kofia.BrokenCollector` (예외) | 실패 |
| `kiwoom.VkospiCollector` | 성공 22건 |
| `customs.RejectingCollector` (실패 반환) | 실패 |
| `kiwoom.KospiMaGapCollector` | 성공 22건 |
| `kiwoom.ForeignNetCollector` | 성공 22건 |

예외가 밖으로 새지 않았고 두 실패 모두 `event_log` 에 ERROR 로 남았다.

### 그런데 알림이 0건이었다 — 죽은 소스가 영원히 조용하다

`PROJECT.md` 10장의 '수집기 실패는 1시간 내 반복 시 알림' 을 그대로 구현했는데,
**그 표현은 자주 도는 수집기를 전제한 것이다.**

지표 수집기는 하루 한 번 돈다. 1시간 창 안에서는 실패가 최대 1회다.
임계값 2 에 영원히 닿지 못한다. **완전히 죽은 소스가 알림 없이 조용해진다.**

일 1회 배치에서 '조용한 무동작' 을 막으려고 `max_delay_days` 를 넣었는데,
같은 실수를 알림 쪽에서 다시 했다.

관찰 창을 `주기 x 임계값` 으로 잡아 '최근 N 번 연속 실패' 를 잡도록 고쳤다.
하루 주기면 48시간이다. 실증했다.

```
1회째 실패 -> 알림 0건
2회째 실패 -> 알림 1건 (48시간 내 2회)
```

**문서의 수치를 그대로 옮기기 전에 그 수치가 어떤 주기를 전제하는지 본다.**
'1시간' 은 값이 아니라 '몇 번 연속 실패했는가' 의 대용이었다.

## 2026-08-29 (3) — 지표 8종 완성, 임계값 재조정

### 인증키 신청을 브라우저로 대신했다

data.go.kr 세 건을 양식까지 채웠다. **약관 동의와 제출은 사용자가 눌렀다.**
비밀번호 입력·약관 동의·최종 제출은 대신하지 않는다.

ECOS 는 '인증키 신청' 이 곧 회원가입이라 별도로 신청할 API 가 없었다.
사이트 안내의 `02 OPEN API 인증키 신청` 단계를 보고 "별도 절차가 있다" 고
정정했다가, 로그인 화면의 안내문("인증키 신청 시 기입했던 이메일")을 보고
처음 판단이 맞았음을 알았다. **화면의 문구 하나가 문서보다 정확했다.**

### 서비스키가 Encoding 형태로 온다

data.go.kr 이 주는 '일반 인증키' 는 `%` 가 섞인 Encoding 형태다.
`urlencode` 하면 `%` 가 `%25` 로 이중 인코딩돼 403 이 난다.

`unquote` 로 한 번 푼 뒤 인코딩하면 Encoding·Decoding 어느 값이 들어와도 된다.
**사용자가 어느 쪽을 넣을지 코드가 정할 수 없다.** 내가 "Decoding 을 넣으세요"
라고 안내했지만, 안내에 기대는 대신 코드가 둘 다 받게 하는 편이 낫다.

값을 노출하지 않고 판별했다. 키에 `%` 가 있는지, 인코딩 방식별로 200/403 이
갈리는지만 보면 된다.

### `indicator` 표의 단위를 안 보고 만들어 두 번 틀렸다

| 지표 | 정의된 단위 | 넣은 것 |
|---|---|---|
| `DEPOSIT`·`CREDIT_BALANCE`·`FOREIGN_NET` | 억원 | 원 |
| `EXPORT_YOY`·`EXPORT_SEMI_YOY` | % (증가율) | 달러 금액 |

첫 번째는 `NUMERIC(20,6)` 오버플로로 드러났다. 예탁금 100조가 컬럼 한계
(10^14) 를 넘었다. **스키마가 막아줬다.**

두 번째는 아무 오류 없이 통과했다. 적재 후 `indicator.unit` 열과 대조하다
발견했다. **막아주는 것이 없으면 대조해야 한다.**

`indicator` 표는 지표의 정본 정의다. 수집기를 만들기 전에 읽는다.

### 관세청은 조회 구간이 1년이다

36개월을 한 번에 요청해 `오류 99` 로 거부당했다. 12개월씩 쪼개 여러 번 부른다.
품목별은 10자리 세부코드로 쪼개져 오므로 달별 합산도 필요하다.
응답 마지막의 `총계` 행을 거르지 않으면 존재하지 않는 달이 생긴다.

### 임계값 재조정 — 포화 85~99% -> 20~25%

`ROADMAP.md` 가 '임시값이고 맞을 리 없다' 고 한 그대로였다.
실측 분포의 10/90 분위수로 잡으니 80% 의 날이 선형 구간에 들어왔다.

**포화된 지표는 값의 크기를 버린다.** ±1 이진 신호가 되어 30 과 90 이 같아진다.

`rule_version` 을 `2026-08-29` 로 올려 이전 판정과 구분된다.

### 분위수를 기계적으로 적용하면 이상한 규칙이 나온다

`KOSPI_MA200_GAP` 은 p10 이 16.8, p90 이 66.9 다. 그대로 쓰면
**이격도 67% 가 '안전'** 이 되는데 실제로는 과열 신호다.

과열과 과랭을 함께 위험으로 보려면 선형이 아닌 규칙이 필요하다.
지금은 분위수를 적용하되 규칙 파일에 주석으로 남겼다.
**전략 판단은 별도 논의 대상이라 임의로 정하지 않는다** (`CLAUDE.md`).

### 테스트를 임계값에 묶지 않는다

`VKOSPI 30`, `USDKRW 1500` 을 최악값으로 박아 둔 테스트가 재조정하자 깨졌다.
규칙 파일에서 danger/safe 를 끌어와 '어느 쪽이 위험인가' 만 검증하도록 바꿨다.

**앞으로도 임계값은 계속 바뀐다.** 바뀔 값을 테스트에 박으면 안 된다.

### 표본의 한계를 규칙 파일에 적었다

일간 254일, 월간 11~12개월이다. 그 구간이 이례적인 강세장이다
(이격도 중앙값 +36%, VKOSPI 중앙값 50). 평범한 장이 오면 다시 어긋난다.

**지금 맞춘 값이 언제 틀릴지를 함께 적어 둔다.**

## 2026-08-29 (4) — 주식수 증가 58건을 DART 로 분류

보류했던 증가 이벤트를 가렸다. **58건 전부 분류, 미매칭 0건.**

| 종류 | 조정 | 출처 | 건수 |
|---|---|---|---|
| `split` | O | DART | 18 |
| `split` | O | 비율 판정 | 34 |
| `rights` | X | DART | 2 |
| `rights` | X | 비율 판정 | 4 |

조정 대상이 359건에서 411건으로 늘었고, 조정 대상 종목의 가격 불연속이
**477건에서 81건**으로 줄었다.

### 날짜로는 맞출 수 없다

DART 의 `isu_dcrs_de` 는 **발행일**이고 우리가 감지한 것은 **상장주식수 변경일**이다.

```
삼양홀딩스  DART 2025.11.06  <->  우리 2025-11-24   18일 차이
```

수량이 유일하게 신뢰할 수 있는 키였다. 그래서 스캔을 다시 돌려(45분)
증가 건의 수량을 저장했다. **처음에 수량을 안 남긴 것이 재스캔을 불렀다.**
보류할 때도 판단 근거가 될 값은 남긴다.

### DART 가 규칙을 검증해줬다 — 이번 작업의 핵심

수량 매칭으로 15건만 맞았다. 그 15건을 늘어놓으니 규칙이 보였다.

```
주식분할  ->  5.0, 2.0, 2.5      단순 분수
유상증자  ->  1.5862, 1.2295     임의 비율
```

**15건 전부 예외가 없었다.** 감소 이벤트에 쓰던 단순 분수 판정이 증가에도
그대로 통한다는 뜻이다. DART 는 독립된 근거로 그것을 확인해준 셈이다.

DART 를 우선 쓰고 근거가 없으면 비율로 판정한다. **무엇이 판정했는지
`source` 에 남긴다** (`dart` / `krx`). 나중에 규칙을 의심할 때 근거가 갈린다.

### 매칭 실패 세 가지를 각각 고쳤다

- **DART 가 분할에서 총수를 적는다.** 129890 은 증가분 40,514,728 이 아니라
  발행 후 총수 50,643,410 이었다. 두 수량을 모두 받아들인다
- **우선주는 DART 목록에 없다.** 회사 단위로 등록되기 때문이다.
  마지막 자리를 0 으로 바꾼 보통주 코드로 다시 찾는다 (001465 -> 001460)
- **사업보고서에 증자·감자 표가 없는 종목이 있다** (001460). 비율 판정이 받는다

### DART 는 자기 고유번호를 쓴다

종목코드로는 조회가 안 된다. `corpCode.xml` 이 3.6MB zip 이고 118,804건 중
상장사는 3,988건이다. 매핑을 먼저 받아야 아무것도 할 수 있다.

### `jsonb_build_object` 는 파라미터 타입을 추론하지 못한다

`IndeterminateDatatype: could not determine data type of parameter $3` 이 났다.
`%s::text` 로 캐스트해야 한다. 서버에서만 드러나는 종류의 오류다.

### 인적분할 4건은 그대로 둔다 (승인 사항)

`irdsSttus` 에 제대로 잡히지 않는다. 삼성바이오로직스 2025년은 이벤트가
1건이고 발행형태가 `-` 다. 분할 가치 비율은 주요사항보고서를 파싱해야 한다.

기록은 남아 있고 `adjusts_price = FALSE` 라 **잘못된 조정은 일어나지 않는다.**
해당 4종목의 시계열이 끊긴 채로 남을 뿐이다.

## 2026-08-29 (5) — 재부팅 검증

Phase 2 완료 기준 중 마지막으로 남아 있던 '서버 재부팅 후 수집기 자동 기동' 을
확인했다. 재부팅 전 값을 먼저 적어두고 대조하는 방식으로 했다.

| 항목 | 재부팅 전 | 재부팅 후 |
|---|---|---|
| `stock-portal-daily.timer` | enabled/active, next 08-30 19:00 | 동일 |
| `stock-portal-kiwoom.timer` | enabled/active, next 08-31 16:10 | 동일 |
| `postgresql` | enabled/active | 동일 |
| `price_daily` | 2,054,367행 | 동일 |
| `adj_factor <> 1` | 204,914행 | 동일 |
| `price_minute` | 200종목 180,000행 | 동일 |

실패 유닛 0개. 손으로 올린 것은 없다.

### `ssh host "sudo ..."` 는 TTY 가 없어 실패한다

이 서버는 무암호 sudo 가 막혀 있다 (`sudo -n true` 로 확인). 그래서
`ssh bh-server@bh-server "sudo systemctl reboot"` 처럼 명령을 인자로 넘기면
비밀번호를 물어볼 터미널이 없어 거부된다. `ssh -t` 로 TTY 를 강제하거나
접속한 뒤에 친다. **처음에 `-t` 없는 명령을 안내했다가 정정했다.**

### 놓친 실행이 없어서 따라잡기는 돌지 않았다

두 타이머 모두 `Persistent=true` 다. 서버가 꺼져 있어 놓친 실행은 부팅 후
따라잡는다. 이번에는 daily 가 당일 19:00 에 이미 돌았고 kiwoom 은 평일
16:10 이라 다음이 월요일이었다. **놓친 것이 없으니 안 도는 것이 맞는 동작이다.**
따라잡기 자체는 이번 검증으로 확인되지 않았다. 평일에 서버가 꺼져 있는
상황이 와야 확인된다.

### 키움 타이머의 `LAST` 가 재부팅 후에 생겼다

재부팅 전에는 `-` 였는데 재부팅 후 `Fri 2026-08-28 22:43:13` 이 찍혔다.
타이머를 08-29 에 등록해서 그 systemd 세션에는 발화 이력이 없었고,
재부팅 후에는 `/var/lib/systemd/timers` 의 스탬프 파일을 읽기 때문이다.
**`LAST` 가 `-` 라고 해서 한 번도 안 돈 것은 아니다.**

### `ROADMAP.md` 의 `adj_factor` 항목이 실제와 다르다 (아직 안 고침)

`price_daily.adj_factor 계산 배치` 가 미체크인데 실제로는 구현돼 있고
`daily.py` 에서 매일 호출되며 204,914행에 반영돼 있다. 체크박스만 밀렸다.
이번 작업 범위가 아니라서 손대지 않았다.

## 2026-08-29 (6) — ROADMAP 을 실측과 대조했다

`adj_factor` 하나가 밀린 줄 알았는데 Phase 1 이 통째로 미체크였다.
**문서가 작업을 따라오지 못한 구간이 두 단계였다.** 코드와 DB 를 직접 보고
하나씩 맞췄다.

### 실측 근거

| 항목 | 확인한 값 |
|---|---|
| 일봉 | 2023-08-29 ~ 2026-08-28, 3,083종목 2,054,367행 |
| `exchange_holiday` | 55건 |
| 분봉 | 200종목 180,000행 |
| `trading_flow` | 2,849종목 283,137행 |
| `stock` | 3,083종목, 폐지 234건 |
| `stock_status` | 2,849행 |
| `adj_factor <> 1` | 204,914행 |
| PostgreSQL | 18.6 |

### 체크하지 않은 것이 셋 있다

문서를 고치는 김에 다 체크하고 싶은 유혹이 있었다. **실제로 안 되는 것은
안 된다고 적었다.**

- **`KiwoomBroker.get_balance` / `get_positions`** — `NotImplementedError` 다.
  ROADMAP 이 이 단계의 조회 항목으로 둘을 명시했는데 구현이 없다.
  매매가 붙기 전에는 쓸 데가 없어서 미룬 것으로 보인다
- **`is_managed` / `is_suspended`** — 2,849종목 전부 FALSE 다. 출처가 없어서
  못 채운다. **컬럼이 있다고 값이 있는 것이 아니다.** 관리종목·거래정지
  변경은 지금 구조로 감지되지 않는다
- **분봉 자동 적재** — 08-27·08-28 이 각 76,000행대로 온전해서 수집기 자체는
  된다. 그러나 타이머를 08-29 에 등록해서 **첫 자동 발화가 08-31 16:10 이다.**
  지금까지는 손으로 돌린 결과다. '매일 자동으로 쌓임' 은 월요일에 확인된다

### 배터리 임계값은 경로가 달라서 없는 줄 알았다

`/sys/class/power_supply/BAT*/charge_control_end_threshold` 를 봤더니 없었다.
설정이 안 된 줄 알았는데, 이 기기는 배터리가 `BAT0` 이 아니라 `CMB0` 이고
임계값은 LG 전용 경로에 있었다.

```
/sys/devices/platform/lg-laptop/battery_care_limit  ->  80
```

**표준 경로에 없다고 설정이 없는 것이 아니다.** 재부팅 후에도 80 이라
영속성도 같이 확인됐다.

### 공유기·BIOS 는 확인하지 못했다

DHCP 예약과 BIOS 전원복구 자동부팅은 서버 밖이라 SSH 로 볼 수 없다.
확인 못 했다고 적고 미체크로 뒀다.

## 2026-08-29 (7) — `is_managed` / `is_suspended` 출처 조사

출처를 찾았다. **키움 `ka10099` (종목정보 리스트)** 다.
`/api/dostk/stkinfo`, 본문 `{"mrkt_tp": ...}`. 모의투자 도메인에서도 나온다.

| `mrkt_tp` | 응답 | 우리 종목과 |
|---|---|---|
| `0` | 거래소 2,481건 (ETF·ETN 포함) | KOSPI 주권 944건 전부 매칭 |
| `10` | 코스닥 1,823건 | 1,823 = 1,823, 차집합 0 |
| `50` | 코넥스 108건 | 108 = 108 |

세 번 부르면 전 종목이 덮인다. `3`(ELW), `8`(ETF), `30`(OTCBB), `60`(ETN),
`5`(신주인수권), `4`(뮤추얼펀드) 는 우리 대상이 아니다.

### 필드 두 개를 함께 써야 한다. 하나로는 안 된다

```
is_suspended  <-  auditInfo == "거래정지"
is_managed    <-  "관리종목" in state
```

**엇갈려 쓰면 둘 다 놓친다.** 처음엔 `auditInfo` 하나로 될 줄 알았다.

`auditInfo` 는 단일값이라 서로 덮어쓴다. 코스닥에서 KRX 가 관리종목이라고
한 123건 중 `auditInfo` 로는 66건만 관리종목이고 나머지 57건은 거래정지로
표시된다. **관리종목이면서 거래정지면 거래정지가 이긴다.**

`state` 는 `|` 로 나뉜 다중값이라 가려지지 않는다. KRX 관리종목 123건을
100% 포함하고, KRX 소속부가 놓친 7건까지 잡는다 (우선주 1건, 투자주의환기에
가려진 6건).

반대로 `state` 의 거래정지는 부분집합이다. 코스닥 31 ⊂ `auditInfo` 93,
KOSPI 17 ⊂ 34. 왜 일부만 적는지는 모른다.

### 거래량으로 검증했다

거래정지면 그날 거래량이 0 이어야 한다. 이것으로 어느 필드가 맞는지 갈렸다.

| 시장 | `auditInfo` 거래정지 | 거래량 0 | 결과 |
|---|---|---|---|
| KOSPI | 34 | 34 | **양방향 완전 일치** |
| KOSDAQ | 93 | 94 | 93건 일치. 나머지 1건은 스팩 |
| KONEX | 11 | 32 | 검증 불가 |

**KONEX 는 이 방법으로 검증할 수 없다.** 유동성이 없어 정상 종목도 거래량 0
인 날이 흔하다. 표시는 오지만 대조할 방법이 없다. KONEX 에는 `state` 관리종목이
0 건인데, 코넥스에 관리종목 지정 제도가 없어서인지 키움이 안 주는 것인지
구분하지 못했다.

### KRX 로도 절반은 됐다. 안 쓰고 있었다

코스닥 `SECT_TP_NM` 에 `관리종목(소속부없음)` 123건이 있다.
**이미 매일 받고 있는 응답이다.** `stock_master.to_stock` 이 이 필드를 버린다.

그런데 KRX 만으로는 안 된다.

- KOSPI 는 `SECT_TP_NM` 이 944건 전부 빈 값이다
- 소속부는 값이 하나뿐이라 투자주의환기와 겹치면 관리종목이 가려진다
- 거래정지는 어느 KRX API 에도 없다. 종목기본정보 12개 필드와
  일별매매정보 15개 필드를 다 확인했다

### 한계 — 과거는 못 받는다

`ka10099` 는 **현재 상태만** 준다. 기준일자 파라미터가 없다.
`stock_status` 를 소급해 채울 수 없고 오늘부터 앞으로만 쌓인다.
이미 지나간 관리종목 지정·해제는 복구되지 않는다.

### 곁가지로 나온 것

`ka10099` 에 지금 DB 에 없는 값이 더 있다. 필요해지면 여기서 가져온다.

- `auditInfo` 의 투자주의·투자경고·투자위험·단기과열·투자주의환기종목
- `state` 의 증거금률(20~100%)·신용가능·담보대출
- `upName`(업종), `upSizeName`(대/중/소형주), `nxtEnable`(NXT 거래가능)

### 아직 구현하지 않았다

`stock_status.py` 는 지금 KRX 한 곳만 본다. 키움을 넣으면 **종목 상태 갱신이
브로커 API 에 의존하게 된다.** 설계상 판단이 필요해 사용자에게 확인받기로 했다.

## 2026-08-29 (8) — `stock_flags` 수집기 (별도 분리, 승인 사항)

`is_managed` / `is_suspended` 를 키움 `ka10099` 로 채운다.
`stock_status` 는 KRX 그대로 두고 플래그만 따로 갱신한다 (사용자 선택).

### 분리하려면 소유권부터 옮겨야 했다

`upsert_stocks` 가 `is_managed = EXCLUDED.is_managed` 로 무조건 덮어썼다.
KRX 경로는 이 값을 채우지 않으니 **항상 FALSE 로 되돌린다.**

키움 유닛은 16:10, 데일리는 19:00 이다. 그대로 두면 오후에 채운 플래그가
저녁에 지워진다. **분리 자체가 성립하지 않는다.**

`is_managed = stock.is_managed` 로 바꿔 갱신 시 손대지 않게 했다.
이제 이 두 컬럼의 주인은 `stock_flags` 하나다.

### 길이 0 인 구간을 만들지 않는다

플래그가 바뀌면 이력을 끊고 새로 연다. 그런데 그 행이 **오늘 열렸다면**
끊고 다시 열 수 없다. `valid_to = valid_from` 인 구간이 생기고,
`PRIMARY KEY (stock_id, valid_from)` 에 걸려 새 행이 들어가지도 못한다.
`ON CONFLICT DO NOTHING` 이라 조용히 사라져 **열린 행이 없는 종목**이 된다.

오늘 열린 행은 제자리에서 고치고, 어제 이전 것만 끊고 연다.

### 이전상장이 플래그를 흘렸다 — 내 변경이 만든 구멍

`stock_status.apply` 는 이전상장 시 이력을 끊고 새로 연다. 그 새 행을
기본값으로 열고 있었다. 전에는 플래그가 늘 FALSE 라 티가 안 났는데,
값이 생기고 나니 **시장을 옮긴 종목의 관리종목이 풀린 것으로 남는다.**

끊기 전에 읽어 이어받게 고쳤다. 신규 상장과 재상장은 FALSE 가 맞다.

### 첫 실행 결과

```
대조 2849건, 변경 224건 (제자리 0, 새 구간 224)
현재 관리종목 165, 거래정지 137
stock_status 2849 -> 3073행
```

무결성을 네 가지로 확인했다. 종목당 열린 행 정확히 1개, 길이 0·역전 구간 0건,
`stock` 캐시와 열린 행의 불일치 0건, 재실행 시 변경 0건(멱등).

조사 때 센 값과 하나씩 어긋난다 (관리종목 166 -> 165, 거래정지 138 -> 137).
KRX 응답에는 있지만 `stock` 에는 없는 리츠가 한 건씩 빠진 것이다.

### 남은 것

- **유닛 반영이 사용자 조치다.** `ExecStart` 한 줄이 저장소에만 있다
- KRX 마스터를 실제로 돌려 플래그가 살아남는 것은 아직 못 봤다.
  토요일이라 KRX 가 0건을 준다. DB 테스트로는 확인했고 월요일 19시에 드러난다
- KONEX 는 `state` 관리종목이 0건이다. 제도가 없는 것인지 키움이 안 주는
  것인지 구분하지 못했다

## 2026-08-29 (9) — 조정가 불연속 81건의 원인

`adjusts_price=TRUE` 를 가진 종목의 조정 후 종가가 직전 거래일 대비
가격제한폭(0.65~1.45) 밖으로 튄 날을 셌다. **81건 / 53종목.**

겹치지 않게 갈랐다.

| 원인 | 건수 | 종목 | 결함인가 |
|---|---|---|---|
| 거래정지 경계 | 45 | 40 | 아니다 |
| 정리매매 | 23 | 14 | 아니다 |
| 권리락 미감지 | 13 | 13 | **그렇다** |

**81건 중 68건은 조정계수 문제가 아니다.** 불연속으로 세면 안 되는 것을
세고 있었다.

### 거래정지 기간의 종가는 시세가 아니다

KRX 는 정지 기간에도 매일 행을 준다. **마지막 종가를 그대로 반복하고
거래량이 0 이다.**

```
녹원씨엔아이  7/08~7/17  11540원 거래량 0  (정지)
              7/18       235원 거래량 912만  (재개)
```

이 구간을 연속된 시세로 읽으면 재개일에 반드시 큰 불연속이 생긴다.
가격이 틀린 것이 아니라 **정지 구간을 시세로 취급한 착시다.**

불연속을 셀 때 직전 거래일을 '거래량 > 0 인 마지막 날' 로 잡아야 한다.

### 정리매매에는 가격제한폭이 없다

폐지 20일 안의 종목이 23건이다. 하루 -80% 가 정상이다.
±30% 를 기준으로 삼는 것 자체가 이 구간에는 맞지 않는다.

```
바이온  32 -> 24 -> 14 -> 7 -> 3
```

### 진짜 결함은 13건 — 권리락을 못 잡는다

우리 감지 기준은 **상장주식수 변화 + 가격제한폭 초과** 이고 둘을 같은 날에
요구한다. 액면분할·병합은 같은 날 함께 바뀌어서 잡힌다.

**무상증자·유상증자는 두 날이 갈라진다.** 권리락일에 가격만 떨어지고
주식수는 몇 주 뒤 신주 상장일에 늘어난다. 그래서 양쪽 다 안 잡힌다.

```
피씨엘  09-13  11,976,236주  9,190원
        09-14  11,976,236주  3,980원   <- 권리락. 주식수 그대로
        10-05  17,176,236주            <- 신주 상장. 가격 안 움직임
        10-13  51,528,708주
```

`corporate_action` 에 이 사건 자체가 없다.

### 13건을 주식수 추이로 검증했다

권리락이면 `가격비 × 주식수배율` 이 1 근처여야 한다. 45일 뒤 주식수와 비교했다.

10건이 1 근처로 맞았다 (앱튼 0.334×3.0, 프롬바이오 0.515×2.0,
비엘팜텍 0.433×3.0, 코미코 0.534×1.96 …).

3건(피씨엘·베셀·텔콘RF제약)은 곱이 1.9~2.6 으로 넘쳤다. 45일 창 안에
**유상증자 신주 상장이 겹쳐** 주식수 배율이 과다하게 잡힌 것이다.
권리락이 아니라는 뜻은 아니다. 창이 너무 넓었다.

### 고치지 않았다

권리락을 잡으려면 감지 기준을 바꿔야 한다. '주식수 변화' 를 전제에서 빼고
가격 급변만으로 후보를 잡으면 시장 급등락과 섞인다. 별도 출처(DART 무상증자
결정, KRX 권리락 공시)가 필요하다. **설계 판단이라 사용자 확인을 받는다.**

## 2026-08-29 (10) — 불연속 측정 기준을 고쳤다

세는 방법이 틀려서 결함이 아닌 것을 결함으로 세고 있었다.
`collectors/market/adj_check.py` 로 옮겼다. 적재하지 않고 보기만 한다.

### 앞선 판단을 하나 정정했다

"직전 거래일을 거래량 > 0 인 날로 잡으면 정지 건이 사라진다" 고 했는데
**틀렸다.** 거래량 0 행을 빼도 정지 전후 두 실제 체결가의 격차는 남는다.
81 -> 63 으로 줄 뿐이다.

거래량 0 을 빼는 것은 **반복된 가짜 종가를 비교 대상에서 없애는 것**이지
정지 구간을 없애는 것이 아니다. 정지를 사이에 둔 격차는 따로 표시해야 한다.

### 기준 셋

- 체결된 날만 본다 (`volume > 0`)
- 정리매매를 뺀다 (폐지 20일 안). 가격제한폭이 없어 하루 -80% 가 정상이다.
  10일과 30일 사이에서 결과가 안 바뀐다
- 정지 구간을 사이에 둔 것은 `halt_days` 로 표시한다. 연속 거래일과 같이 셀 수 없다

### 결과 — 81건이 13건이 됐다

| 분류 | 건수 | 종목 |
|---|---|---|
| 연속 거래일 | 13 | 13 |
| 정지 구간 포함 | 18 | 16 |
| 정리매매 (제외) | 32 | 15 |

정지 구간 18건도 갈렸다.

- 14건 — 조정은 이미 적용됐고 남은 것은 재개 후 실제 재평가다 (비율 1.45~1.53).
  17일 정지 뒤 +50% 는 결함이 아니다
- 3건 — 임의 비율이라 `adjusts_price=FALSE` 로 둔 감자다 (케이피엠테크 8.35,
  KR모터스 3.08). **조정이 필요했을 수 있다.** 단순 분수 규칙이 너무 좁다
- 1건 — 정지 구간에 이벤트 기록 자체가 없다

### 남은 결함은 13건뿐이다

전부 권리락 미감지다. 나머지 68건은 세는 방법의 문제였다.

`adj_check` 는 데일리에 넣지 않았다. 매일 알릴 값이 아니고,
`corporate_action` 을 건드린 뒤 손으로 확인하는 용도다.

## 2026-08-29 (11) — Phase 2 를 닫았다

권리락 미감지 13종목을 **남긴 채로 닫기로 했다 (승인 사항).**

### 받아들이고 닫은 것 셋

| 항목 | 종목 | 왜 못 잡는가 |
|---|---|---|
| 권리락 미감지 | 13 | 권리락일과 신주 상장일이 갈라진다 |
| 인적분할 | 4 | `irdsSttus` 에 안 잡힌다 |
| 임의 비율 감자 | 3 | 단순 분수 규칙(분모 ≤ 4)이 좁다 |

셋 다 `adjusts_price = FALSE` 다. **잘못된 조정은 일어나지 않는다.**
해당 종목의 시계열만 끊긴 채로 남는다. 조용히 틀린 값이 되는 것보다
끊긴 채로 남는 편이 낫다.

### 관측이 안 된 채로 넘긴 것 둘

닫으면서 '확인했다' 고 적지 않았다. 설정상 동작하고 첫 자동 실행이
08-31 일 뿐이다. 체크리스트의 이월 항목으로 남겼다.

- 분봉 자동 적재 (08-31 16:10)
- `stock_flags` 플래그 생존 (08-31 19:00)

**돌 것이라고 믿는 것과 도는 것을 본 것은 다르다.** 문서에 그대로 적었다.

### `get_balance` / `get_positions` 는 Phase 8 로 넘겼다

`NotImplementedError` 인 채로 있다. 주문이 붙기 전에는 쓸 데가 없다.
`kiwoom.py` 주석이 이미 Phase 8 이라고 적고 있어 그쪽에 맞췄다.
Nginx 도 같은 이유로 Phase 4 로 넘겼다. 띄울 화면이 없다.

**닫는다는 것은 다 했다는 뜻이 아니라, 남은 것을 어디로 보냈는지
적었다는 뜻이다.**

## 2026-08-29 (12) — Phase 4 시작, 시작 전에 정한 것

화면이 처음 나오는 단계다. 코드를 쓰기 전에 문서와 코드를 대조했고
갈리는 지점 다섯을 물어 확인받았다.

### 프레임워크는 Flask

`ROADMAP.md` 가 "Flask/FastAPI" 로 열어둔 것을 닫았다.

포털이 하는 일은 DB 를 읽어 JSON 과 HTML 로 내보내는 것뿐이다.
비동기가 필요한 곳이 없고, 엔진과는 HTTP 가 아니라 DB 로 통신한다
(`PROJECT.md` 8.1). FastAPI 의 이점인 스키마 검증은 Phase 8 제어 API 에서나
값을 하는데, 그때 요청 본문이라고 해봐야 `{"confirm": "LIQUIDATE"}` 한 줄이다.
지금 pydantic·uvicorn·starlette 를 들이는 값이 없다.

Jinja2 가 Flask 에 딸려 오는 것도 이유다. 화면을 직접 써야 한다.

### 화면은 최소 HTML 을 직접 쓴다

`PROJECT.md` 6장이 참조하는 `SCREENS.md` 가 없다. Claude Design 산출물이
아직 나오지 않았다는 뜻이다.

기다리면 Phase 4 를 닫을 수 없다. 완료 기준이 "휴대폰에서 시장분석 화면이
보임" 이라 화면 없이는 기준을 충족하지 못한다. 수수한 템플릿을 먼저 쓰고,
디자인이 나오면 템플릿만 갈아끼운다. API 는 그대로 쓸 수 있다.

### `heartbeat` 에 쓰는 코드가 없었다

`SCHEMA.md` 6장에 표는 있고 `CLAUDE.md` 가 "통신은 DB(`heartbeat`, `command`)
로만 한다" 고 적어놨는데, 실제로 쓰는 코드가 프로젝트 전체에 없다.
Phase 2·3 이 전부 oneshot 배치라 필요가 없었기 때문이다.

Phase 4 완료 기준이 "수집기 상태가 화면에 반영됨" 이라 여기서 메운다.

### oneshot 배치의 heartbeat 는 무슨 뜻인가

`SCHEMA.md` 는 "`NOW() - last_beat_at` 이 임계값을 넘으면 죽은 것" 이라고
적었다. 상시 프로세스를 전제한 문장이다. 수집기는 하루 한 번 돌고 끝난다.

의미를 이렇게 정했다.

| 상태 | 언제 |
|---|---|
| `running` | 배치가 시작할 때 |
| `idle` | 종료코드 0 으로 끝났을 때 |
| `error` | 종료코드가 0 이 아니거나 예외로 죽었을 때 |

임계값은 프로세스마다 다르다. 하루 한 번 도는 배치에 '1시간' 을 쓰면
언제나 죽어 보인다. `config/portal.yaml` 에 프로세스별로 둔다.
Phase 3 에서 반복 실패 알림의 관찰 창을 수집기 주기에 맞췄던 것과 같은 이유다.

`running` 인 채로 임계 시간을 넘긴 것도 죽은 것으로 본다.
배치가 죽으면 `error` 를 남길 기회조차 없기 때문이다.

### `process_name` 은 모듈별

`PROJECT.md` 프로세스 표에는 `collector-market` 하나로 적혀 있다.
실제로는 systemd 유닛 둘이 모듈 일곱을 따로 돌린다.

하나로 묶으면 늦게 도는 모듈의 성공이 먼저 실패한 모듈을 덮어쓴다.
`price_minute` 이 죽어도 뒤이어 도는 `trading_flow` 가 성공하면 화면은
초록이 된다. 어떤 수집이 멈췄는지 구분할 수 없다.

`event_log.process_name` 이 이미 모듈 단위(`daily`, `price_minute` …)라
같은 이름을 쓴다. 두 표가 이름으로 연결된다.

### detail 에는 종료코드만 남긴다

`SCHEMA.md` 는 detail 에 "처리 건수 등" 이라고 적었다. 건수를 넣으려면
각 수집기의 `main()` 반환형을 바꿔야 한다. 일곱 군데를 고치는 값이 없다.

건수는 이미 `event_log` 에 있고 화면에서 `/api/events` 로 같이 보인다.
heartbeat 은 '살아 있는가' 만 답하게 뒀다.

### `/api/events` 를 추가한다

`PROJECT.md` 6장의 운영·로그 탭은 "프로세스 상태, 에러, 주문 이력,
API/LLM 사용량" 인데 `INTERFACES.md` 10장 조회 표에 `event_log` 를 읽는
엔드포인트가 없다. 표에서 빠진 것으로 보고 한 줄 더한다. 조회 전용이다.

### 커넥션 풀은 두지 않는다

요청마다 `connect()` 로 열고 닫는다. DB 가 같은 서버의 localhost 이고
사용자가 한 명이다. 풀이 필요해지면 그때 넣는다.

## 2026-08-29 (13) — 포털 구현, 표가 휴대폰에서 두 번 잘렸다

코드는 다 됐다. 배포와 검증이 남았다.

### 표본 데이터로 화면을 먼저 봤다

개발 PC 에는 `.env` 가 없어 DB 에 붙지 못한다. 조회 함수를 표본값으로
바꿔치기한 임시 서버를 띄워 375px 폭에서 화면을 봤다. 스크래치에만 두고
저장소에는 넣지 않았다.

**보길 잘했다. 두 군데가 잘려 있었다.**

| 무엇 | 어떻게 고쳤나 |
|---|---|
| 지표표의 '값' 이 화면 밖 | 열 순서를 지표·값·전기대비·계층·기준일 로 바꿨다 |
| 에러 메시지가 잘림 | 표를 버리고 한 건을 두 줄로 쓰는 목록으로 바꿨다 |

폭이 좁으면 표는 답이 아니다. 가로 스크롤은 있는데 사람은 스크롤하지 않는다.
중요한 값이 첫 화면에 보이거나, 표가 아니어야 한다.

프로세스표의 '비고' 도 열에서 빼 이름 아래에 붙였다. 네 열이 세 열이 됐다.

### 화면과 API 가 같은 함수를 쓴다

`portal/queries.py` 가 DB 를 읽어 JSON 으로 낼 수 있는 dict 를 만들고,
`api.py` 는 그것을 그대로 내보내고 `views.py` 는 템플릿에 넣는다.

둘이 갈라지면 화면에는 보이는데 API 에는 없는 값이 생긴다.
Claude Design 산출물이 나와도 바뀌는 것은 템플릿뿐이다.

### heartbeat 의 status 와 화면의 state 는 다르다

`status` 는 프로세스가 스스로 적은 것이고 `state` 는 '지금 이것을 어떻게
봐야 하는가' 다. 넷으로 낸다.

| state | 언제 |
|---|---|
| `ok` | 정상 종료했고 신호가 임계 시간 안에 있다 |
| `error` | 스스로 실패를 적었다 |
| `stale` | 신호가 임계 시간을 넘었다 |
| `unknown` | heartbeat 행이 아예 없다. 한 번도 실행되지 않았다 |

**신호가 끊긴 `running` 은 죽은 것으로 본다.** 배치가 죽으면 `error` 를
남길 틈이 없다. 살아 있다고 적힌 마지막 말을 믿으면 영원히 초록이다.

키움 유닛은 평일에만 돌아 금요일부터 월요일까지 72시간이 빈다.
임계값을 74시간으로 뒀다. 26시간으로 두면 주말마다 화면이 빨개진다.

### Decimal 은 문자열로 낸다

API 응답에서 `NUMERIC` 을 float 로 바꾸지 않는다. 값이 미세하게 달라진다.
화면 쪽 `num` 필터가 소수 6자리 뒤의 0 을 떼고 천 단위를 끊는다.

### 시각은 UTC 로 저장하고 화면에서 KST 로 바꾼다

API 는 저장된 그대로 ISO 문자열로 낸다. `kst` 필터가 화면에서만 바꾼다.
저장 규칙(`CLAUDE.md` 5)을 표시 편의로 흔들지 않는다.

### 남은 것

- 서버 배포 (Flask·gunicorn 설치, systemd, Nginx)
- 완료 기준 둘은 배포 뒤에야 확인된다
- `common/db/heartbeat.py` 의 DB 테스트는 로컬에서 건너뛴다.
  서버에서 `pytest tests/test_heartbeat.py` 로 한 번 돌려야 한다

### 포트 8000 은 이미 쓰이고 있었다 (2026-08-29 배포)

`PROJECT.md` 3장은 포털을 8000 으로 적어놨는데 서버에서는 도커 컨테이너
`muhan-maesu` 가 그 자리를 쓰고 있었다. gunicorn 이 붙지 못하고 3초마다
재시작하는 중이었는데, `systemctl status` 는 `active (running)` 이었다.

**서비스가 살아 있다는 표시를 믿고 넘어갈 뻔했다.** `curl` 이 준
`{"detail": "Not Found"}` 가 우리 형식(`{"error": ...}`)이 아니라는 것이
단서였다. 다른 앱이 대신 응답하고 있었다.

포털을 **8001** 로 옮겼다 (승인 사항). 돌고 있는 다른 서비스를 건드리지
않는 쪽이다. Nginx 가 80 으로 받으므로 휴대폰에서는 포트를 볼 일이 없다.

이 서버의 도커 점유 현황을 `PROJECT.md` 에 적었다.

| 포트 | 무엇 |
|---|---|
| 8000 | 무한매수 |
| 5000 | kavita |
| 9000 | portainer |

**5000 은 문서가 리치프랜즈랩 자리로 적어둔 곳이다.** Phase 11 에서 다시
정해야 한다. 지금 손대지 않고 표시만 남긴다.

### 배포 확인 (2026-08-29 23:35)

| 확인 | 결과 |
|---|---|
| 서버 전체 테스트 | 265건 통과. DB 통합분까지 돌았다 |
| gunicorn (8001) | `/api/processes` 가 실제 DB 를 읽는다 |
| heartbeat 경로 | `partitions` 를 손으로 돌려 `unknown` → `ok` 확인 |
| Nginx (80) | `/` 와 `/api/indicators` 가 포털로 간다 |

지표 8종이 실제 값으로 나온다. 나머지 수집기 6종은 heartbeat 을 붙인 뒤로
아직 돌지 않아 `unknown` 이다. 08-30 19:00 과 08-31 16:10 에 채워진다.

**`systemctl status` 의 `active (running)` 을 믿지 않는 법을 하나 배웠다.**
`Restart=always` 가 붙어 있으면 3초마다 죽고 살아나는 프로세스도 방금 뜬
것처럼 보인다. 봐야 할 것은 `restart counter` 와 로그다.

### 등락률이 -178% 로 나왔다 (2026-08-29)

휴대폰 화면에서 국면 이력의 KOSPI 등락률이 `-178.65%` 였다.
지수 등락률이 100% 를 넘을 수 없으니 틀린 값이다.

내 필터 버그였다. 두 열의 단위가 다른데 같은 필터를 썼다.

| 열 | 단위 | 예 |
|---|---|---|
| `indicator_value.change_rate` | 비율 | `-0.0250` 이 -2.50% |
| `market_regime.kospi_return` | 퍼센트 | `-1.7865` 가 -1.79% |

`daily_returns()` 가 넣을 때 이미 `* 100` 을 한다
(`collectors/market/indicators.py`). 화면에서 또 곱했다.

필터를 `ratio_pct` 와 `percent` 로 갈랐다. 이름만 봐도 어느 단위를 받는지
알게 했다. `SCHEMA.md` 의 `kospi_return` 주석에도 단위를 적었다.
설계를 바꾼 것이 아니라 이미 그런 값을 적어둔 것이다.

**표시 계층에서만 틀렸고 저장된 값은 옳다.** API 는 저장된 그대로 낸다.

### 곁에서 본 것 — VKOSPI 50.08

같은 화면에 VKOSPI 가 50.08 로 떠 있다. 보통 15~25 이고 50 은 위기 수준이다.
그런데 같은 날 KOSPI 200일 이격도는 +13.2% 로 과열 쪽이다. 둘이 어긋난다.

Phase 3 에서 닫은 지표라 임의로 손대지 않는다. 확인이 필요하면
`ka20006` 의 변동성지수 코드(`603`)가 맞는지부터 본다.

## 2026-08-29 (14) — Phase 5 시작, 시작 전에 정한 것

`PROJECT.md` 9장의 처리 순서를 그대로 구현한다. 순서 자체가 비용 설계다.
사전으로 걸러낸 뒤 남은 것만 LLM 에 보낸다. 사전이 커질수록 비용이 준다.

### 집계는 달력일(KST) 그대로

메시지는 24시간 오는데 `keyword_daily` 는 하루 한 행이다.
'15:30 이후는 다음 거래일' 로 미는 방법도 있었지만 달력일을 택했다 (승인 사항).

미는 쪽의 이점은 '밤사이 소문이 다음날 시장에 반영된다' 는 관점과 맞고
휴장일 행이 안 생겨 `ma7` 이 고르다는 것이다. 대신 집계 경계가 복잡해지고
휴장일 판정이 끼어든다.

**급등 감지는 알림이 즉시 나가고, 집계는 사후 비교용이다.** 즉시성이
중요한 쪽은 이미 실시간 경로에 있으므로 집계는 단순한 편이 낫다.

주말에는 메시지가 줄어 `ma7` 이 낮아지고, 월요일 `surge_ratio` 가 과하게
뜰 수 있다. **알고 받아들인다.** 실측한 뒤 문제가 되면 요일 보정을 넣는다.

### 종목명 사전은 걸러서 쓴다

3,000종목 이름을 그대로 넣으면 오탐이 섞인다. 짧은 이름과 일반명사와
겹치는 이름이 문제다. 우선주·스팩을 빼고, 제외 목록을 `config/` 에 둔다.

**놓치는 것보다 잘못 잡는 것이 더 해롭다.** 언급이 없는 종목에 언급이
있다고 기록되면 그 뒤의 모든 집계가 조용히 틀어진다. 놓친 것은 사전을
고치면 원문에서 다시 뽑을 수 있다. `raw_message` 에 원문을 남기는 이유다.

### LLM 상한은 하루 $1

Haiku 기준으로 하루 수천 건을 분석하고도 남는다. 상한은 성능 조절이 아니라
**폭주 방지**다. 무한 루프로 같은 배치를 반복 호출하는 사고를 막는다.

호출 **전에** `api_usage` 의 당일 누적을 확인한다. 호출한 뒤에 세면 이미 쓴
돈이다. 상한에 닿은 날은 사전 매칭만으로 동작하고 알림을 보낸다.
분석되지 않은 메시지는 `analyzed_at` 이 비어 있어 다음 날 다시 잡힌다.

### 1단계를 먼저 닫는다

텔레그램 수집만 먼저 붙여 원문을 쌓기 시작한다. 분석은 나중에 다시 돌릴 수
있지만 **받지 못한 메시지는 되찾을 수 없다.** Phase 2 에서 분봉을 먼저
쌓기로 한 것과 같은 이유다.

### Telethon 로그인은 사용자가 한다

봇 토큰이 아니라 사용자 계정 세션이 필요하다. 채널을 구독한 계정으로
읽어야 하기 때문이다. 최초 로그인은 전화번호로 코드를 받는 절차라
**사용자가 서버에서 직접 한다.** 세션 파일은 `.gitignore` 에 넣는다.

### Telethon 로그인 스크립트 (2026-08-29)

`python -m collectors.news.login` 하나로 끝난다. 이미 세션이 있으면 로그인을
묻지 않고 채널 목록만 다시 찍는다.

**로그인 뒤에 구독 채널을 찍는 것은 확인 절차다.** 세션 파일이 생겼다는 것과
그 세션으로 실제로 읽힌다는 것은 다르다. 겸사겸사 `source` 에 넣을 채널
식별자(`channel_id`, `@username`)가 그 출력에서 나온다.

세션 파일에는 로그인 자격이 들어간다. 봇 토큰과 달리 재발급으로 무효화할 수
없어서 `.gitignore` 에 넣고 권한을 600 으로 낮춘다.

`telethon>=1.41` 을 requirements 에 넣었다. 개발 PC 에서는 1.44.0 이 붙었다.
서버는 Python 3.14 라 설치가 되는지 봐야 안다.

### 1단계 텔레그램 수집 (2026-08-29)

채널 3개로 시작한다. 급등일보, 가치투자클럽, 특징주 속보(그로쓰리서치).
가중치는 전부 1.0 이다. **가중치는 상한이 1 인 확률이 아니라 곱셈 계수다.**
2.0 이면 두 배로 센다. 근거가 없으니 지금은 전부 기준값이다.

식별자는 `@사용자명` 이 아니라 숫자 채널 ID 다. 사용자명은 바뀌고, 비공개
채널에는 아예 없다. 고른 셋 중 하나가 그렇다.

#### 따라잡기에는 개수 제한을 두지 않는다

재시작하면 마지막으로 받은 메시지 번호 뒤를 **전부** 받는다. 며칠 꺼져
있었으면 그만큼 받는다. 개수를 제한하면 조용히 구멍이 남는다.
처음 붙는 채널만 최근 200건으로 시작한다. 채널의 전체 역사는 필요 없다.

메시지 번호는 `external_id` 에 TEXT 로 저장되는데 비교는 정수로 한다.
문자열로 비교하면 `'9'` 가 `'10'` 보다 뒤가 된다.

#### 본문 없는 메시지는 버린다

사진·파일만 있는 메시지는 `message.message` 가 비어 있다. 분석할 것이 없다.
캡션이 달려 있으면 그것이 본문으로 들어온다.

#### 상시 프로세스라 정지 판정 단위를 분으로 바꿨다

`config/portal.yaml` 의 `stale_after_hours` 를 `stale_after_minutes` 로 고쳤다.
배치는 1560분(26시간)이고 텔레그램 수집은 10분이다. 시간 단위로는 60초마다
신호를 남기는 프로세스의 정지를 한 시간 안에 알아챌 수 없다.

화면에는 `duration` 필터가 60분 이상이면 시간으로, 아니면 분으로 보여준다.

#### 세션이 없으면 서비스가 뜨지 않게 했다

로그인을 물어야 하는데 systemd 아래에는 물을 곳이 없다. 그대로 두면 프롬프트
앞에서 멈춘 채 `active` 로 보인다. 세션 파일을 먼저 확인하고 없으면 종료한다.

유닛에 `StartLimitBurst=5` 를 뒀다. 5분에 5번 실패하면 포기하고 `failed` 로
멈춘다. **무한 재시작은 로그만 채우고 화면에는 살아 있는 것처럼 보인다.**
포털 8000번 포트 사고에서 겪은 그대로다.

### 상시 프로세스는 pull 만으로 안 바뀐다 (2026-08-30)

`git pull` 뒤 포털을 재시작하지 않아 `/api/processes` 가 500 을 냈다.
설정 키를 `stale_after_hours` 에서 `stale_after_minutes` 로 바꿨는데,
새 YAML 을 **옛 코드가** 읽었기 때문이다.

배치는 매번 새로 떠서 문제가 없지만 gunicorn 과 수집기는 시작할 때 읽은
코드로 계속 돈다. `deploy/README.md` 맨 앞에 적었다.

설정만 바꾸는 경우가 더 위험하다. 코드가 그대로라 '재시작할 이유' 를
떠올리지 못한다.

### 첫 적재 588건

| 채널 | 건수 | 구간 |
|---|---|---|
| 특징주 속보 | 200 | 08-27 ~ 08-28 |
| 급등일보 | 194 | 08-26 ~ 08-30 |
| 가치투자클럽 | 194 | 08-14 ~ 08-29 |

200건이 아니라 194건인 둘은 본문 없는 사진 글이 6건씩 걸러진 것이다.
같은 200건이라도 글이 뜸한 채널은 더 먼 과거까지 닿는다.

## 2026-08-30 — 2단계 규칙 분석, 표본이 규칙을 정했다

원문 589건을 먼저 뽑아봤다. **상상해서 짰으면 세 군데를 틀렸다.**

### 링크를 먼저 지워야 한다

589건 중 455건(77%)에 링크가 있다. 가치투자클럽은 `https://naver.me/...`
25자만 던지는 채널이다. 링크를 남긴 채 길이를 재면 이 글이 '25자짜리 알찬 글'
로 보인다. 지우고 나면 0자다.

### 여섯 자리 숫자 391건은 대부분 종목코드가 아니다

`newsId=04454246645551912` 같은 기사 ID 다. 링크를 지우기 전에 종목코드를
찾으면 거의 전부 오탐이다. 정규화 뒤에만 찾고, 앞뒤에 숫자가 더 붙으면
종목코드로 보지 않는다.

### 짧은 글을 자르면 키워드가 농축된 글을 잃는다

가장 짧은 축이 `원전 2Q`, `ESS 2Q`, `변압기 2Q`, `건설기계 2Q` 다.
7자짜리에 테마 키워드가 하나씩 들어 있다. '최소 길이 미달 제거' 를 세게 걸면
이것부터 사라진다.

최소 길이를 **4자**로 정했다 (승인 사항). 거의 안 자른다.
필터의 목적은 LLM 비용 절감인데 짧은 글은 원래 싸다. 링크만 있는 글은
정규화 후 0자라 어차피 걸러진다.

### 채널 꼬리표를 지운다

`📌그로쓰리서치 실시간 특징주 받기` 가 모든 글 끝에 붙는다.
놔두면 '그로쓰리서치' 가 매일 급등 키워드가 된다. 채널별 꼬리는
`config/collect.yaml` 에 둔다.

### 약칭과 미국 종목은 그대로 둔다 (승인 사항)

`삼전·닉스` 같은 약칭이 실제로 쓰인다. 정식 명칭만으로는 절반만 잡힌다.
다만 약칭 사전은 사람이 만들 수밖에 없고, 589건으로 짐작해 넣으면 오탐만
늘린다. 실측으로 놓친 것을 모은 뒤에 만든다.

미국 종목 뉴스도 그대로 둔다. 국내 종목명 사전에 안 잡혀 `stock_mention` 은
비지만 반도체·AI 같은 테마 키워드는 국내장에도 옮는다.

### `analyzed_at` 의 의미

파이프라인이 끝나면 채운다. 지금은 사전 매칭까지가 끝이라 `dict` 로 닫는다.
LLM 이 붙으면 `llm` 이 된다.

사전을 고친 뒤 다시 돌리려면 `analyzed_at` 을 비우면 된다. **원문을 남기는
이유가 이것이다.** 프롬프트나 사전이 바뀌어도 처음부터 다시 분석할 수 있다.

### 첫 매칭에서 오탐이 무더기로 나왔다 (2026-08-30)

589건을 분석하고 상위를 봤더니 22건이 `이닉스` 였다. 실제로는 전부
`SK하이닉스` 안에 걸린 것이다.

| 잡힌 이름 | 실제 글 | 건수 |
|---|---|---|
| 이닉스 | SK**하이닉스** | 22 |
| 컴투스 | **컴투스**홀딩스 | 11 |
| 타이드 | 펩**타이드** | 8 |
| HD현대 | **HD현대**일렉트릭 | 8 |
| 휴온스 | **휴온스**글로벌 | 7 |

**한국어에는 낱말 경계가 없다.** 부분 문자열이 그대로 걸린다.

#### 제외 목록으로 막을 문제가 아니다

`이닉스`(226360)도 `HD현대`(267250)도 실재하는 종목이다. 목록에서 빼면
진짜 언급을 놓친다. 규칙 자체를 고쳤다.

두 가지를 함께 쓴다.

- **앞 글자 검사** — 이름 앞에 글자가 붙어 있으면 다른 낱말이다.
  `하이닉스` 의 `이닉스`, `펩타이드` 의 `타이드` 가 걸러진다
- **긴 이름부터 보고 지운다** — `HD현대일렉트릭` 을 먼저 지우면 `HD현대` 가
  남지 않는다. 둘 다 실제로 나온 글에서는 둘 다 잡힌다

**뒤쪽은 검사할 수 없다.** `삼성전자가` 의 조사와 `HD현대일렉트릭` 의 이어짐이
같은 모양이기 때문이다. 그래서 뒤쪽은 '지우기' 로 처리한다.

#### 남은 것

`제우스` 11건은 아직 확인하지 못했다. 실재하는 반도체 장비주라 진짜일 수도
있고 다른 낱말 안일 수도 있다. 고친 규칙으로 다시 돌린 뒤 본다.

### 고친 뒤 재분석 결과 (2026-08-30)

589건 전체를 다시 돌렸다. 58건이 규칙 필터에 걸리고 434건의 종목 언급이 남았다.

| 이름 | 고치기 전 | 고친 뒤 | 판정 |
|---|---|---|---|
| 이닉스 | 22 | 7 | **남은 7건은 진짜다** |
| 타이드 | 8 | 0 | 사라짐 |
| HD현대 | 8 | 0 | 사라짐 |
| 휴온스 | 7 | 7 | 진짜. 휴온스글로벌과 정확히 갈렸다 |

`이닉스` 7건은 채널 형식 때문이었다. 글 머리에
`✅특징주 종목 : 이닉스(452400)` 를 붙이고 본문에는 다른 종목 기사를 싣는다.
머리글에서 제대로 잡은 것이다.

**오탐인 줄 알았던 것이 진짜였다.** 숫자만 보고 제외 목록에 넣었으면
정상 동작을 망가뜨릴 뻔했다. 표본을 봐야 하는 이유다.

### 받아들이고 넘어가는 것 — `제우스`

`제우스`(079370)는 실재하는 반도체 장비주인데 컴투스 신작 게임 이름이기도
하다. 표본 11건이 전부 게임 뉴스다.

**그대로 두기로 했다 (승인 사항).** 게임 화제가 식으면 줄어든다.
그동안의 집계에는 게임 뉴스가 종목 언급으로 섞인다는 것을 알고 받아들인다.

부분 문자열 오탐과 달리 규칙으로는 가릴 수 없다. 같은 글자, 다른 뜻이다.

### 나중에 다듬을 것 — 언급의 무게

한 글 안에서 '머리글의 주인공 종목' 과 '본문에 스쳐 나온 종목' 이 같은
무게로 기록된다. 위의 이닉스 글이 `삼성전자`·`SK하이닉스` 언급으로도 세어진다.

`특징주 종목 : X` 라는 형식이 그 글의 주인공을 명시하고 있다. 채널별 형식에
기대는 규칙이라 지금 넣지 않는다. 집계가 쌓여 실제로 문제가 되는지 본 뒤에
판단한다.

## 2026-08-30 (2) — 3단계 LLM

### 모델 ID 가 기억과 달랐다

`claude-haiku-4-5-20251001` 로 알고 있었는데 실제로는 **`claude-haiku-4-5`** 다.
날짜 접미어를 붙이지 않는다. 단가는 1M 토큰당 입력 $1 / 출력 $5 다 (2026-06 기준).

`config/limits.yaml` 에 모델명과 단가를 함께 뒀다. 모델을 바꾸면 단가도 바뀐다.

### 키워드 굵기는 물어볼 필요가 없었다

`SCHEMA.md` 가 이미 답을 적어놨다. "'반도체 120회' 는 정보가 아니고
'유리기판 평소 4회 -> 오늘 30회' 가 정보다."

**잘게 뽑고 동의어는 사람이 병합하는 구조다.** 프롬프트에 "좁은 표현을
우선한다. '반도체' 보다 'HBM', '유리기판'" 이라고 적었다.

### JSON 은 프롬프트가 아니라 형식으로 강제한다

`output_config` 의 `json_schema` 로 응답 형식을 못박는다. 전문이나 코드펜스가
섞일 자리가 없어진다. 프롬프트로 "JSON만 답해" 라고 부탁하는 것과 다르다.

그래도 **개수는 검사해야 한다.** 입력 3건에 출력 2건이 오면 어느 글의
키워드인지 알 수 없다. 그때는 배치 전체를 버린다.

### 상한은 호출 전에 확인한다

호출한 뒤에 세면 이미 쓴 돈이다. 배치마다 `api_usage` 의 당일 누적을 먼저
읽고, 상한에 닿았으면 그 자리에서 멈춘다.

멈출 때 **남은 원문을 표시하지 않는다.** `analyzed_at` 이 비어 있으면 다음
주기가 다시 가져간다. 실패한 배치도 같다. 이것이 재시도 장치 전부다.
따로 재시도 큐를 두지 않는다.

### 상한 $1 의 뜻

성능 조절이 아니라 폭주 방지다. Haiku 로 하루 수천 건을 분석하고도 남는다.
무한 루프로 같은 배치를 반복 호출하는 사고를 막는 값이다.
모자라면 설정에서 올리면 된다.

### 첫 호출이 400 으로 막혔다 (2026-08-30)

```
anthropic-workspace-id is required when authenticating with an
identity-linked API key
```

조직 계정에 묶인 키는 어느 워크스페이스에서 쓰는지 함께 보내야 한다.
SDK 클라이언트의 `default_headers` 로 붙인다. 값은 `.env` 의
`ANTHROPIC_WORKSPACE_ID` 에서 읽고, **없으면 헤더를 붙이지 않는다.**
개인 키는 이 값이 없어야 정상이다.

**실패가 설계대로 처리됐다.** 배치가 `analyzed_at` 을 채우지 않아 20건이
그대로 남았고, 다음 실행이 다시 가져간다. 따로 손댈 것이 없었다.

`anthropic` 은 1.2.0 이 설치됐다. `default_headers` 와 `output_config` 모두
있다.

## 2026-08-30 (3) — 4단계 집계와 급등 알림

### 기준선에서 당일을 뺀다

`ma7` 은 **당일을 뺀 직전 7일**의 하루 평균이다. 당일을 넣으면 오늘의 급등이
제 기준선을 스스로 끌어올려 신호가 약해진다. 30회 터진 날의 평균에 그 30회가
들어가면 배수가 확 준다.

### 언급이 없던 날도 0회로 센다

윈도를 `ROWS` 가 아니라 `RANGE ... INTERVAL '7 days'` 로 잡았다.

행이 있는 날만 평균 내면 **어쩌다 한 번 나오는 표현의 기준선이 1.0 이 된다.**
반년 전에 한 번, 오늘 30번 나온 말이 '평소 1회 → 오늘 30회' 가 아니라
'평소 1회 → 30배' 로 계산되어야 하는데, 전자로 세면 기준선이 부풀어 묻힌다.

날짜 구간으로 세면 기준선이 1/7 = 0.14 가 되고 배수가 제대로 뜬다.

### 첫 등장을 버리지 않는다

`ma7 = 0` 이면 비율이 무한대다. `surge_ratio` 는 NULL 로 둔다.

**그렇다고 후보에서 빼면 가장 강한 신호를 버리는 셈이다.** 처음 보는 말이
하루에 열 번 나오는 것이 바로 찾던 신호다. 비율 대신 **건수**로 잡는 규칙을
따로 뒀다 (`new_min_count`).

알림에도 구분해서 적는다. '평소 4.3회, 7.0배' 와 '처음 등장' 은 다른 이야기다.

### 잡음 하한

기준선이 0.2회인 표현이 1회 나오면 5배다. 급등이라 부를 수 없다.
`min_baseline` 으로 하루 1회 미만은 비율 규칙에서 뺀다.

### 되풀이 알림 막기

배치가 10분마다 도는데 같은 급등을 매번 알리면 도배가 된다. 그날 이미 알린
표현은 `event_log` 를 보고 거른다. **별도 표를 만들지 않았다.** 알림은 어차피
이력으로 남겨야 하고, 남긴 것을 읽으면 그만이다.

### 임계값은 임시값이다

`min_ratio: 3.0`, `min_baseline: 1.0`, `new_min_count: 5`. 근거가 없다.
Phase 3 국면 임계값과 같은 취급으로, 데이터가 쌓이면 조정한다.

## 2026-08-30 (4) — 정보수집 탭

### 같은 실수를 또 했다

키워드 표에서 **배수 열이 휴대폰 폭 밖으로 밀렸다.** 지표표에서 값이 밀렸던
것과 똑같다. 급등 판단의 핵심 수치인데 스크롤해야 보였다.

열 순서를 키워드·건수·배수·평소로 바꿨다. **좁은 화면에서는 열 순서가
곧 중요도다.** 표를 만들 때마다 무엇이 첫 화면에 보여야 하는지 정해야 한다.

탭이 넷이 되면서 마지막 탭도 잘렸다. 420px 아래에서 글자를 줄였다.

### 병합은 집계를 지운다

동의어를 묶으면 묶인 쪽의 `keyword_daily` 행을 지운다. 다시 집계하면
대표어 아래로 합쳐지는데, 지우지 않으면 옛 숫자가 화면에 남는다.

숫자가 바로 바뀌지 않고 다음 집계(10분)에 반영된다는 것을 화면에 적었다.
사용자가 '병합이 안 됐나' 하고 다시 누르는 것을 막으려는 것이다.

### 버릴 키워드는 자리가 없다 (승인 사항)

`NV` 같은 쓰레기를 표시할 컬럼이 `keyword` 에 없다. `is_confirmed` 는
'확인했다' 이지 '버린다' 가 아니다.

**스키마를 늘리지 않고 두기로 했다.** 프롬프트를 고쳐 새로 들어오는 것은
줄였고, 기존 쓰레기는 빈도가 낮아 목록 아래쪽에 조용히 남는다.
정말 거슬리면 그때 컬럼을 더한다.

## 2026-08-30 (5) — 5단계 DART 공시

### 유형 코드가 응답에 없다

`SCHEMA.md` 는 "공시 유형 코드가 이미 분류를 제공하므로 LLM 분류가 불필요하다"
고 적었다. 맞는 말인데, **목록 API 응답에는 유형 코드가 없다.**
`pblntf_ty` 는 요청 파라미터일 뿐이다.

그래서 유형별로 나눠 묻고, 무엇을 물었는지로 유형을 안다.
A 정기공시·B 주요사항보고·C 발행공시·D 지분공시·I 거래소공시 다섯을 받는다.
E 기타·F 외부감사·G 펀드·H 자산유동화·J 공정위는 받지 않는다.

호출이 다섯 배가 되지만 DART 한도가 하루 2만 건이라 문제되지 않는다.

### 시각이 없다

`rcept_dt` 는 날짜(YYYYMMDD)뿐이다. `submitted_at` 은 `TIMESTAMPTZ` 라
그날 0시(KST)로 채운다. **공시가 몇 시에 올라왔는지는 이 표로 알 수 없다.**

스키마를 `DATE` 로 바꾸는 쪽도 있었지만 건드리지 않았다. 나중에 상세 API 로
시각을 채울 여지를 남긴다. 대신 지금 값이 자정이라는 것을 코드와 여기에 적는다.

### 종목 연결은 있으면 하고 없으면 비운다

`stock_id` 에 외래키가 걸려 있다. 우리 종목 표에 없는 회사(신규 상장 직전,
코넥스 등)를 넣으면 적재가 통째로 실패한다. 비워 두고 `corp_name` 은 남긴다.

### `/api/disclosures` 를 더했다

`INTERFACES.md` 10장 표에 없던 것이다. `/api/events` 때와 같은 이유다 —
`PROJECT.md` 6장이 정보수집 탭에 DART 결과를 요구하는데 조회 경로가 없었다.
표에 한 줄 더했다. 조회 전용이다.

### 배치가 통째로 버려지고 있었다 (2026-08-30)

전체를 돌리자 20건 배치에서 출력이 17·19·19·6·19·18·18 건으로 계속 모자랐다.
**7개 배치가 연달아 버려졌다.**

JSON 은 온전했다. 잘린 것이 아니라 **모델이 키워드 없는 글의 항목을 통째로
생략한** 것이다. "없으면 빈 배열" 이라고 적었는데 아예 건너뛰었다.

#### 진짜 문제는 순서에 기댄 것이었다

출력이 배열이라 어느 항목이 어느 글인지 **순서로만** 알 수 있었다.
그래서 개수가 하나만 어긋나도 전부 버려야 했다. 19건이 멀쩡해도 다 버렸다.

각 결과에 **글 번호(`index`)** 를 달게 했다. 이제 몇 건이 빠지든 나머지를
쓴다. 빠진 글은 표시하지 않아 다음 주기에 다시 간다.

`minItems`/`maxItems` 로 개수를 못박아 보려 했으나 **거부당했다.**

```
For 'array' type, 'minItems' values other than 0 or 1 are not supported
```

구조화 출력은 배열 길이를 강제하지 못한다. 번호 방식만 남겼다.
스키마로 막을 수 있는 것과 없는 것의 경계를 하나 알았다.

**형식을 강제해도 내용의 개수까지 보장되지는 않는다.** `output_config` 는
JSON 이 유효하다는 것만 보장한다. 그 위에서 무엇이 어긋날 수 있는지는
따로 생각해야 했다.

### 상한 차단 확인 (2026-08-30)

상한을 $0.001 로 낮춰 돌렸더니 `$0.090581 / $0.001` 에서 호출 없이 멈췄다.
남은 145건은 표시되지 않아 다음 주기로 넘어갔다. Phase 5 완료 기준 하나를
실측으로 채웠다.

### 번호를 요구하니 누락이 사라졌다 (2026-08-30)

재실행에서 7배치 140건이 **전부** 답을 받았다. 무응답 0건이다.

번호는 부분 응답을 살리려고 넣은 것인데, 넣고 나니 **누락 자체가 없어졌다.**
"몇 번 글의 답인가" 를 명시하게 하면 모델이 건너뛰지 않는다.
방어 장치가 문제를 예방하기도 한다.

비용은 140건에 $0.044, 건당 $0.0003 이다. 하루 200건이면 $0.06 이라
상한 $1 은 여전히 넉넉하다.

`INTERFACES.md` 7장의 규격을 고쳤다. "입력 순서와 출력 순서가 일치해야
한다" 는 전제 자체가 문제였다. **일치를 요구하는 대신 일치하지 않아도
되게 만드는 쪽**이 맞았다.

## 2026-08-30 (6) — Phase 4·5 를 닫았다

타이머가 14:00:04 에 저절로 돌고 정보수집 분석·키워드 집계·전자공시 수집이
모두 정상으로 떴다. 마지막 완료 기준이었다.

### 하루 만에 두 단계를 닫았다

Phase 4 는 어젯밤에 코드가 끝나 있었고 오늘 배포·검증만 남아 있었다.
Phase 5 는 오늘 여섯 단계를 이어서 만들었다.

빨랐던 이유는 **매 단계 실측을 먼저 했기 때문**이다.

| 단계 | 무엇을 먼저 봤나 | 그래서 피한 것 |
|---|---|---|
| 2단계 | 원문 589건의 길이 분포와 표본 | 링크·기사 ID·짧은 글 함정 셋 |
| 2단계 | 매칭 상위 25개 | `이닉스` 22건이 오탐인 줄 알았는데 7건은 진짜였다 |
| 3단계 | 20건만 먼저 호출 | 400 오류 두 번을 20건 값으로 겪었다 |
| 4단계 | 집계 결과 표 | 임계값이 실제로 어떻게 걸리는지 |

**상상해서 만들었으면 세 군데 이상 틀렸다.**

### 오늘 겪은 실패 넷

전부 기록에 남겼다. 되풀이하지 않기 위해서다.

1. **`systemctl status` 의 `active` 를 믿을 뻔했다** — 포트가 막혀 3초마다
   죽고 있었다. `Restart=always` 가 재시작 루프를 정상처럼 보이게 한다
2. **`git pull` 후 재시작을 빠뜨렸다** — 새 설정을 옛 코드가 읽어 500 이 났다
3. **순서에 기댄 LLM 응답** — 20건 중 19건이 멀쩡해도 통째로 버렸다
4. **테스트가 실제 저장값과 달랐다** — `NUMERIC(10,2)` 반올림을 몰랐다

넷 다 **'되는 줄 알았는데 아니었다'** 는 모양이다. 돌 것이라고 믿는 것과
도는 것을 본 것은 다르다. Phase 2 를 닫을 때 적은 문장이 오늘도 그대로 맞았다.

### 다음은 Phase 6 백테스트다

전략 내용 없이 틀만 만든다. 미결 사항이 하나 있다 — **체결 모델**(종가 체결이냐
다음 봉 시가냐, 단타와 스윙에 다른 기준을 쓸지). `ROADMAP.md` 가 전략 논의와
함께 정하자고 적어놨다.

## 2026-08-30 (7) — Phase 6 계획

전략 없이 틀만 만든다. 검증 없는 매매가 위험하다는 것이 `ROADMAP.md` 의
순서를 정한 두 제약 중 하나였다. 백테스트 환경이 전략보다 먼저 있어야 한다.

### 체결은 다음 봉 시가 (승인 사항)

`ROADMAP.md` 가 미결로 남겨둔 항목을 닫았다.

종가로 신호를 계산하고 **다음 거래일 시가에 체결**한다.
당일 종가 체결도 후보였지만 그것은 현실에서 불가능하다. 종가를 보고 나서
그 종가에 살 수는 없다. 낙관 편향이 구조적으로 들어간다.

다음 봉 시가는 갭상승·갭하락이 그대로 결과에 들어온다. **그것도 현실이다.**
밤사이 뉴스로 5% 갭상승한 종목을 시가에 사는 것이 실제로 일어나는 일이다.

미래 참조가 **구조적으로** 불가능해지는 것이 가장 큰 이점이다.
신호 계산 시점(종가)과 체결 시점(다음 시가) 사이에 커서가 하루 넘어간다.

단타는 이번 범위 밖이다. 분봉이 나흘치뿐이다. Phase 10 에서 체결 모델을
다시 정한다. 분봉이면 '다음 봉' 의 뜻이 달라진다.

### 생존편향 경고를 결과에 붙인다 (승인 사항)

`PROJECT.md` 11장이 이미 경고하고 있지만, 문서에만 적어두면 몇 달 뒤
숫자만 떼어 볼 때 잊는다. `backtest_run.note` 에 그 구간의 폐지 종목 수와
데이터 유무를 자동으로 적고 화면에도 띄운다.

**편향을 없앨 수는 없다.** Phase 2 에서 받은 '일봉 3년치' 는 오늘 살아있는
종목의 3년치다. 이미 폐지된 종목은 처음부터 없다. 지울 수 없는 낙관이
결과에 섞여 있다는 것을 숫자 옆에 적어두는 것이 할 수 있는 전부다.

### 수수료율은 사용자 확인이 필요하다

`fee_rate` 와 `slippage_rate` 를 `NOT NULL` 로 둔 것은 0 으로 돌린 결과도
그 사실이 남게 하려는 것이다 (`SCHEMA.md` 7장).

**값이 틀리면 결과가 통째로 틀린다.** 스윙은 회전이 낮아 덜하지만,
단타로 가면 수수료가 손익의 부호를 바꾼다. 임시값으로 시작하되 실제
키움 수수료율과 증권거래세율을 받아 바꾼다.

### 유니버스는 최소한으로

`config/universe.yaml` 확정은 Phase 7 이다. 지금은 틀을 돌리는 데 필요한
만큼만 둔다 — 해당 시점 상장, 관리종목·거래정지 제외, 거래대금 상위 N.

중요한 것은 **`stock` 의 현재값이 아니라 `stock_status` 의 해당 시점 값**을
읽는 것이다 (`INTERFACES.md` 3.4). 현재값을 읽으면 '지금 관리종목인 회사는
3년 전에도 관리종목이었다' 는 잘못된 과거를 만든다.

### 화면 위치가 문서에 없다

`ROADMAP.md` 는 "포털에서 결과 조회 화면" 을 요구하는데 `PROJECT.md` 6장의
탭 목록에는 백테스트가 없다. 7단계에 가서 확인받는다.

### 1단계 DataFeed 구현 (2026-08-30)

`common/feed/` 를 새로 만들었다. `PROJECT.md` 4장 디렉토리 목록에 없던 자리라
한 줄 더했다. 전략 밑에 두면 전략이 DB·브로커를 import 하게 되어 의존 방향이
뒤집힌다 (`INTERFACES.md` 0장과 같은 이유).

#### 커서는 그날 장 마감이다

`now()` 가 거래일의 15:30(KST)을 UTC 로 낸다. **종가가 확정된 시점**이라는 뜻이다.
신호는 여기서 계산되고 체결은 다음 거래일 시가에 일어난다. 둘 사이에 커서가
하루 넘어가므로 미래 참조가 구조적으로 막힌다.

#### 조정가와 원주가를 나눈 자리

`get_candles` 는 `close * adj_factor` 로 조정가를, `get_quote` 는 저장된
원주가를 그대로 준다. 지표는 이어져야 하고 체결 대조는 실제 가격이어야 한다.
테스트로 둘 다 고정했다.

#### 없는 것을 지어내지 않는다

- 과거 호가는 남아 있지 않다. `Quote.bid`/`ask` 는 `None` 이다
- 국면 이력은 2026-08 부터다. 그 이전 구간은 전부 중립으로 본다
- `signal` 은 Phase 8 에서 엔진이 채운다. 지금은 빈 목록이다

셋 다 그럴듯한 값을 만들어 넣는 편이 쉬웠지만, 백테스트에서 지어낸 값은
결과를 조용히 낙관으로 기울인다.

### 2단계 Strategy 규격 (2026-08-30)

`EntryIntent` 에 **수량 필드를 두지 않은 것**이 이 규격의 핵심이다.
수량을 넣을 자리가 있으면 전략이 채우게 되고, 그러면 리스크 한도가 전략마다
흩어진다. 크기 산정은 `RiskManager` 한 곳에서만 한다 (`INTERFACES.md` 4.2).

`manage` 가 `scan` 과 독립인 것도 같은 이유다. 신규 진입을 막은 상태에서도
청산은 돌아야 한다. 비상 중단이 '진입 차단' 과 '전량 청산' 으로 갈리는
설계와 짝을 이룬다.

더미 전략은 `common/strategy/dummy.py` 에 뒀다. **백테스트 전용 코드가
아니다.** 실전 엔진에 물려도 그대로 돌아간다. 다만 판단이 없어서 결과의
좋고 나쁨에 뜻이 없다는 것을 파일 첫머리에 적었다.

전략 테스트는 DB 없이 돈다. 스텁 피드가 `DataFeed` 규격만 만족하면 되기
때문이다. **피드를 경계로 둔 값이 여기서 나온다.**

### 3단계 RiskManager (2026-08-30)

한도 다섯을 한곳에서 강제한다. 가장 작은 값이 이긴다.

#### 국면 배분에서 보유분을 뺀다

`regime_allocation` 은 **총 투입 한도**다. 이미 들고 있는 금액을 빼지 않으면
위험 국면에서도 종목 수 한도까지 계속 살 수 있다. 배분이 이름값을 못 한다.

테스트로 고정했다. 위험 30%(300만) 중 250만을 이미 들었으면 50만만 남는다.

#### 일일 손실 한도는 전략 밖이다

`start_day()` 로 그날 시작 자산을 기억하고 `evaluate` 마다 견준다.
전략이 무엇을 하든 한도에 닿으면 그날은 더 사지 않는다.
`INTERFACES.md` 5장이 "전략과 무관한 계층에서 강제한다" 고 적은 부분이다.

#### 테스트 이름이 단정과 어긋났다

`위험 국면에서는 덜 산다` 라고 이름 붙였는데 실제 단정은 두 국면의 수량이
같았다. 종목당 비중 상한이 먼저 걸려서다. 단정은 옳았고 이름이 틀렸다.

**이름이 거짓말하는 테스트는 나중에 읽는 사람을 속인다.**
`한도 넷 중 가장 작은 것이 이긴다` 로 고쳤다.

#### 국면 배분은 임시값이다 (승인 사항)

`danger 0.3 / neutral 0.7 / safe 1.0`. 근거가 없다. 백테스트를 돌려야
생긴다. Phase 3 국면 임계값과 같은 취급이다.

### 4단계 체결 시뮬레이터 (2026-08-30)

받은 실제 요율을 넣었다.

| 항목 | 값 | 어디에 |
|---|---|---|
| 위탁수수료 | 0.015% | 매수·매도 양쪽 |
| 증권거래세 | 0.20% | **매도에만** |
| 슬리피지 | 0.1% | 임시값 |

수수료에 유관기관 제비용이 포함돼 있어 따로 더하지 않는다. 수수료 할인
이벤트 계좌라면 제비용이 별도 부과되어 실효 요율이 달라진다는 것을 설정에
적어뒀다.

#### 세금은 손익과 무관하다

매도금액 기준이라 손실을 봐도 낸다. 테스트로 고정했다.

#### 왕복하면 가격이 그대로여도 손해다

10,000원에 사서 10,000원에 팔면 왕복 비용이 남는다. `PROJECT.md` 11장이
"수수료와 슬리피지를 반드시 반영한다. 미반영 시 결과가 무의미하다" 고 적은
이유가 이것이다. 테스트로 못박았다.

#### 모르는 시장은 가장 높은 세율로

`board` 가 `KONEX` 처럼 설정에 없는 값이면 가장 높은 세율을 쓴다.
**모를 때 낙관 쪽으로 틀리지 않는다**는 원칙이다. 백테스트에서 비용을
낮게 잡는 실수는 결과를 좋아 보이게 만든다.

#### 호가단위는 모사하지 않는다

슬리피지를 곱한 단가가 실제 호가단위에 맞지 않는다. 근사로 받아들이고
적어둔다. 실전과의 차이는 Phase 8 모의투자에서 체결가를 대조해 본다.

#### 설정값 자체를 테스트로 고정했다

`config/limits.yaml` 의 요율이 실제 값과 같은지 보는 테스트를 넣었다.
**값이 틀리면 결과가 통째로 틀리는데, 코드는 멀쩡히 돈다.** 조용히 틀리는
것을 막으려면 값도 단정해야 한다.

## 2026-08-30 (8) — 5단계 루프와 CLI

1~4단계의 조각을 하루 단위로 이었다. 새 개념은 하나도 없다. **순서가 전부다.**

### 하루의 순서가 미래 참조를 막는다

```
1. 전일 종가에 정한 주문을 오늘 시가에 체결한다 (매도 먼저)
2. 커서를 오늘 장 마감으로 옮긴다
3. 보유 중 폐지된 종목을 정리매매 마지막 가격으로 청산한다
4. 오늘 종가로 평가한다
5. manage 로 청산을, scan 으로 진입을 정한다
```

1이 2보다 앞인 것이 핵심이다. 체결이 커서 이동보다 먼저 일어나므로 **오늘 종가를
보고 정한 것은 아무리 빨라도 내일 시가에야 체결된다.** 코드로 막는 것이 아니라
순서로 막는다. 나중에 누가 조건을 하나 더해도 이 뼈대는 안 무너진다.

### 체결측 조회를 `Market` 으로 뺐다

`DataFeed` 는 설계상 커서 이후를 보여주지 않는다. 그런데 체결은 다음 거래일
시가에 일어나니 그 값을 누군가는 봐야 한다. **피드에 `get_next_open` 같은 것을
더하면 전략도 그것을 볼 수 있게 된다.** 규격에 구멍을 내는 셈이다.

그래서 `backtest/market.py` 에 `Market` 프로토콜을 따로 뒀다. 루프만 들고
전략에는 넘기지 않는다. 타입으로 "이건 전략이 볼 것이 아니다" 를 적어둔 것이다.

부수 효과로 루프 테스트가 DB 없이 돈다. 2단계에서 스텁 피드로 전략을 시험한
것과 같은 구조다.

### 거래일은 `price_daily` 에 행이 있는 날이다

`exchange_holiday` 를 보지 않았다. 그 명단이 완전하다는 보장이 없고, 임시
휴장이나 조기 폐장이 빠져 있으면 없는 날에 체결이 일어난다. **데이터가 있는
날만 도는 편이 안전하다.** 휴장일은 행이 없어 자연히 빠진다.

### 매도를 먼저 체결한다

같은 날 매도와 매수가 겹치면 매도부터 낸다. 국내는 매도대금으로 당일 재매수가
되므로 그것이 실제다. 순서를 뒤집으면 현금이 있는데도 못 사는 날이 생겨
결과가 실제보다 나빠진다.

더미 전략은 사는 날과 파는 날이 갈려서 이 순서가 드러나지 않는다. 그래서
루프 전체가 아니라 `_settle` 한 단계만 떼어 테스트했다.

### 현금이 음수가 되지 않게 막았다

종가 기준으로 수량을 정하는데 체결은 다음 날 시가다. 갭상승하면 정한 수량을
살 돈이 모자란다. 그대로 두면 **있지도 않은 레버리지가 결과를 좋게 만든다.**
살 수 있는 만큼으로 줄이고, 한 주도 못 사면 취소한다.

부족분 비율로 줄이되 최소 한 주씩은 반드시 줄인다. 수수료 절사 때문에 비율
계산이 제자리에 머무는 경우가 있어서다.

### 일일 손실 한도의 기준은 전일 종가 평가액이다

`RiskManager.start_day()` 에 무엇을 넣느냐가 한도의 뜻을 정한다. 당일 종가
평가액을 넣으면 `evaluate` 가 같은 값과 비교하게 되어 손실이 항상 0이 된다.
**한도가 있는데 절대 안 걸리는 상태**가 된다.

전일 종가 평가액을 넣어야 "오늘 하루 몇 % 잃었나" 가 된다.

### 평단가에 수수료를 넣었다

현금이 준 만큼이 원가다. 수수료를 빼면 손익이 그만큼 낙관 쪽으로 틀어지고,
`RiskManager` 가 보는 `invested` 도 실제보다 작아져 한도가 헐거워진다.

### 그날 시가가 없으면 주문을 취소한다

거래정지나 폐지로 시가가 없으면 다음 날로 미루지 않고 버린다. 미루면 정지가
풀린 날 며칠 전 판단으로 체결된다. 진입 의도는 `scan` 이 다음 날 다시 만든다.

### 폐지 청산은 마지막 거래일에 일어난 것으로 적었다

체결일을 '폐지를 알아챈 날' 이 아니라 **마지막으로 거래된 날**로 뒀다.
그날이 실제로 팔 수 있었던 마지막 날이기 때문이다. 6단계에서 `backtest_trade`
의 `exit_at` 이 이 값을 받는다.

### `close_time` 과 `universe_size` 를 설정으로 뺐다

`BacktestFeed` 가 요구하는 값인데 넣을 자리가 없어 `config/limits.yaml` 의
`backtest` 아래에 더했다. 유니버스 크기는 **틀을 돌리는 최소값**이고 확정은
Phase 7 (`config/universe.yaml`) 이다.

### 서버 실데이터로 완주했다

2025-03 한 달, 20 거래일, 체결 122건, -0.74%. 테스트는 365건 전부 통과했다.

셋을 봤다.

- **휴장일.** 3/1 토요일과 3/3 대체공휴일이 빠져 03-04 에 시작했다.
  `exchange_holiday` 를 안 보고 일봉 유무로만 판단한 것이 실데이터에서 맞았다
- **체결 수.** 19일 × 5 = 95 보다 많다. 유니버스 상위 5종목이 거래대금에 따라
  매일 바뀌어서 **매도와 매수가 같은 날 겹친 날이 있었다.** 매도를 먼저 내는
  순서가 실제로 쓰였다는 뜻이다
- **비용.** 왕복 0.43% × 61회면 자본 대비 2.6% 다. 실제 손실은 0.74% 이므로
  그 구간 시세가 1.9% 쯤 올라 비용을 덮었다. 비용과 시세가 둘 다 들어간다

**수익률 숫자에는 뜻이 없다.** 더미는 판단이 없다.

### 아직 없는 것

- 결과 DB 적재와 지표(MDD·승률·샤프) — 6단계
- 생존편향 경고 자동 첨부 — 6단계

## 2026-08-30 (9) — 6단계 결과 적재

지표는 한번 숫자가 나오면 **정의를 잊는다.** 그래서 정의를 먼저 정하고 시작했다.

### FIFO 로 짝짓는다 (승인 사항)

`backtest_trade` 는 `entry_at`/`exit_at` 한 쌍이라 매수와 매도를 짝지어야 한다.
분할 매수·부분 청산이 있으면 어느 매수를 판 것인지 정할 방법이 없다. 먼저 산
것을 먼저 판 것으로 본다. 세법과 같은 규칙이라 나중에 실제 손익과 대조하기도 쉽다.

더미는 전량 매매라 지금은 짝이 하나뿐이다. **Phase 7 전략이 분할을 쓰면 이
규칙이 실제로 일한다.** 그때 규칙을 새로 정하지 않도록 지금 못박았다.

### 샤프는 무위험 0, 연 252거래일 (승인 사항)

근거가 아니라 관례다. 값을 바꾸면 숫자가 달라지므로 `days_per_year` 를 설정에
두고 `params` 에 통째로 남긴다. 몇 달 뒤 다른 숫자를 볼 때 어떤 가정이었는지
결과 안에서 확인할 수 있다.

표본이 둘 미만이거나 표준편차가 0이면 `None` 이다.

### 계산할 수 없는 것은 0 이 아니라 NULL 이다

거래가 없는데 승률 0% 를 적으면 **'다 졌다' 로 읽힌다.** 없는 것과 나쁜 것은
다르다. `win_rate` 와 `sharpe` 는 `NULL` 을 허용하고 CLI 는 '계산 불가' 로 찍는다.

### 손익은 현금 증감으로 계산한다

`entry_price` 와 `exit_price` 는 체결 단가라서 **그 둘의 차이에 수량을 곱해도
`pnl` 이 나오지 않는다.** 매수 원가에는 수수료가, 매도 대금에는 수수료와 세금이
들어 있기 때문이다. 그 차이가 곧 거래비용이다.

단가를 그대로 남긴 것은 나중에 실제 체결과 대조할 때 필요해서다. 비용까지
녹인 값을 적으면 어느 가격에 체결됐는지 알 수 없게 된다.

### 미청산 포지션도 남긴다

구간이 끝날 때 들고 있던 것은 `exit_at` 이 `NULL` 인 행으로 남는다. 빼면 손익이
실제와 달라지고, 마지막에 대량 매수한 전략이 좋아 보인다.

`trade_count` 는 미청산을 포함한 전체 행 수다. 승률만 청산된 것으로 센다.

### `Execution` 을 만들었다 — 체결에 이유가 없었다

5단계 루프는 `Fill` 만 모았다. 그런데 `backtest_trade` 에는 `exit_reason` 과
`signal_payload` 가 있다. **체결만 남기면 왜 팔았는지가 사라진다.**

`Fill` 에 넣지 않은 것은 체결 시뮬레이터가 이유를 모르기 때문이다. 수수료
계산에 사유는 필요 없다. 아는 쪽인 루프가 붙이는 것이 맞다.

폐지 청산은 `reason` 이 `'delisted'` 다. `SCHEMA.md` 주석의 목록
(`target|stop|timeout|signal`)에 없는 값이지만 주석이지 제약이 아니다.
**있었던 일을 그대로 적는 편이 목록에 맞추는 것보다 낫다.**

### 적재는 실행과 같은 트랜잭션이다

`insert_run` 과 `insert_trades` 가 갈라지면 매매 없는 실행 기록이 남는다.
지표만 있고 근거가 없는 행이다. CLI 가 `transaction()` 하나로 감싼다.

### 체결 시각은 장 시작 시각이다

체결이 시가에 일어나므로 `backtest_trade` 의 시각은 09:00 KST 를 UTC 로 낸
값이다. `exchange` 시드에 이미 있는 값인데 `close_time` 을 설정에 둔 것과
맞추려고 `config/limits.yaml` 에 뒀다.

**폐지 청산만 예외다.** 정리매매 마지막 **종가**로 체결되는데 시각은 장 시작으로
찍힌다. 거래일은 맞고 시분이 근사다. 일봉 백테스트에서 시분은 정보를 담지
않으므로 받아들였다.

### 생존편향 경고를 자동으로 붙였다 (승인 사항)

구간에 폐지된 종목 수와 **그중 일봉이 남아 있는 수**를 세어 `note` 에 적는다.
둘의 차이가 결과에서 빠진 손실이다.

문장으로 적은 것은 숫자만 적으면 무슨 뜻인지 다시 캐야 해서다. 편향을 없앨
수는 없고, 숫자를 그만큼 낮춰 봐야 한다는 것을 결과 옆에 써 둔다.

### 아직 없는 것

- 포털 화면 — 7단계. **`PROJECT.md` 6장 탭 목록에 백테스트가 없다.** 확인이 필요하다
- 서버에서 실제 적재 확인 — 로컬에 DB 가 없다

## 2026-08-30 (10) — 7단계 포털 화면

### 새 탭을 만들지 않았다 (확인 사항)

`PROJECT.md` 6장 탭 목록에 백테스트가 없어 물었고, **운영·로그 안**으로 정했다.
매일 보는 화면이 아니라 가끔 돌려보고 견주는 것이라 성격이 맞는다. 문서에도
그 이유를 적었다.

### 화면에서 백테스트를 돌리지 않는다

실행은 CLI 로만 한다. 포털은 조회 전용이고 파라미터를 바꾸는 요소를 두지
않는다는 규칙 (`CLAUDE.md` 1) 이 여기에도 걸린다. 화면에 '실행' 버튼을 두면
기간·자본·수수료를 화면에서 고르고 싶어지고, 그 순간 파라미터가 설정 파일
밖으로 새어나간다.

### 경고를 숫자 바로 아래 붙였다

`note` 를 표 아래 한 번만 띄우는 안도 있었지만, 실행마다 구간이 달라 폐지
종목 수도 다르다. **한 줄로 뭉치면 어느 실행의 경고인지 알 수 없다.**
행마다 붙이면 스무 번 반복되지만, 반복되는 것이 이 경고의 목적이다.

행 사이 선을 지워 숫자와 경고가 한 덩어리로 보이게 했다. 선이 있으면 경고가
다음 실행에 딸린 것처럼 읽힌다.

### `ratio_plain` 필터를 더했다

MDD 에 `ratio_pct` 를 쓰면 `+2.63%` 가 된다. **낙폭에 `+` 를 붙이면 오른 것처럼
읽힌다.** 승률과 요율도 방향이 정해진 값이라 부호가 필요 없다.

자릿수를 셋까지 두고 뒤의 0 을 뗀다. 수수료 0.00015 가 `0.015%` 로 보여야 하는데
둘째 자리에서 끊으면 `0.01%` 가 되어 값이 달라진다.

이 프로젝트는 백분율 필터가 이미 둘이었다 (`ratio_pct`, `percent`). 단위가 다른
값이 한 화면에 있어 2026-08-29 에 등락률이 -178% 로 나온 적이 있다. 셋째를
더하면서 이름으로 갈라 두는 규칙을 그대로 따랐다.

### `params` 는 화면에 내지 않는다

`recent_runs` 가 `params` 를 뽑지 않는다. 크고 화면에서 읽지 않는다.
**다만 `fee_rate` 와 `slippage_rate` 는 뽑는다.** 어떤 비용으로 돌린 결과인지
모르면 두 실행의 지표를 견줄 수 없다. 그것이 두 값을 컬럼으로 따로 둔 이유다.

### 아직 없는 것

- 서버에서 화면 확인. 휴대폰 폭을 봐야 한다 (2026-08-29 에 두 번 잘린 적이 있다)
- `backtest_trade` 를 화면에서 보는 것. `ROADMAP.md` 는 요구하지 않는다.
  전략이 생긴 뒤 무엇을 보고 싶은지 알게 되면 그때 만든다

## 2026-08-30 (11) — Phase 6 을 닫았다

서버에서 확인한 것.

| 확인 | 결과 |
|---|---|
| 테스트 | 394건 통과 (DB 포함, 스킵 0) |
| 실데이터 완주 | 2025-03, 20 거래일, 체결 122건, 매매 63건, -0.74% |
| 적재 | `run_id 8`. 지표 다섯이 DB 에 들어갔다 |
| 화면 | 운영·로그 탭에 실행 둘, 경고 둘 |

경고가 둘인 것이 맞다. **실행마다 붙이기로 한 결과가 그대로 보인 것이다.**

### 체결 모델 미결을 닫았다

`ROADMAP.md` 와 `INTERFACES.md` 12장에 미결로 남아 있던 항목이다.
**다음 거래일 시가**로 확정하고 두 문서에서 뺐다. 결정 내용과 이유는
`ROADMAP.md` Phase 6 에 남겼다. 미결 표에서 지우기만 하면 왜 그렇게
정했는지가 사라진다.

단타는 닫지 않았다. 분봉이면 '다음 봉' 의 뜻이 달라진다. Phase 10 이다.

### 남은 임시값

- **슬리피지 0.1%** — 실측 없이 정할 수 없다. Phase 8 모의투자에서 대조한다
- **국면별 자금 배분** `danger 0.3 / neutral 0.7 / safe 1.0` — 근거는
  백테스트를 돌려야 생긴다. Phase 3 국면 임계값과 같은 취급이다

둘 다 결과를 크게 흔드는 값이다. **틀이 돌아간다는 것과 결과를 믿을 수
있다는 것은 다르다.** Phase 7 에서 전략을 넣기 전에 기억해야 한다.

### 수수료율은 실제 값이다

임시값으로 시작했지만 4단계에서 실제 값을 받아 넣었다. 위탁수수료 0.015%,
증권거래세 0.20%. 설정값 자체를 테스트로 고정해 두었다. 값이 틀리면 결과가
통째로 틀리는데 코드는 멀쩡히 돌기 때문이다.

## 2026-08-30 (12) — Phase 7 시작, 시작 전에 정한 것

`ROADMAP.md` 가 "여기서 가장 많은 시간을 쓴다" 고 적은 단계다. 코드보다
정하는 일이 많다. 아직 전략 코드를 한 줄도 쓰지 않았다.

### 추세 추종으로 간다 (확인 사항)

평균 회귀와 수급 기반이 후보였다.

**평균 회귀는 생존편향과 상성이 나쁘다.** 폐지된 종목이 데이터에 없으니
'떨어지면 오른다' 가 실제보다 잘 듣는 것처럼 보인다. 크게 떨어진 뒤 사라진
종목이 표본에서 빠져 있기 때문이다. 편향이 전략의 핵심 가정을 직접 부풀린다.

**수급 기반은 과거를 못 본다.** `trading_flow` 수집이 2026-08 부터다.
백테스트 구간 대부분에 수급이 없다. 게다가 피드에 통로가 없어
`INTERFACES.md` 3장을 고쳐야 한다.

추세 추종은 일봉만으로 만들 수 있어 **피드를 넓히지 않아도 된다.** 규칙이
단순해 손잡이가 적은 것도 이 데이터 양에서는 장점이다.

### 앞 2년 개발 / 뒤 1년 검증 (확인 사항)

`price_daily` 는 2023-08-29 ~ 2026-08-28, 729 거래일이다.

- 개발 2023-08-29 ~ 2025-08-28
- 검증 2025-08-29 ~ 2026-08-28

**개발 구간 앞 120 거래일은 신호가 없다.** `ma_long` 예열이다. 실질 개발
표본은 1년 반쯤이다. 검증 구간은 시작 시점에 뒤로 2년이 있어 예열 손실이
없다. 피드가 커서 이전을 전부 보기 때문이다.

교차 검증도 후보였지만 사실상 전 구간을 보게 되어 과최적화를 막는 힘이
약해진다. **검증 구간을 한 번도 안 보는 것**이 이 단계의 핵심이라
단순 분할을 골랐다.

### 규칙은 넷으로 제안했다 (확인 대기)

진입 둘(신고가 돌파 + 장기 이평 위), 청산 둘(단기 이평 이탈 + 손절).

**조건 하나가 곧 과최적화할 손잡이 하나다.** 데이터가 3년뿐이고 실질 개발
구간은 1년 반이다. 조건을 늘리면 그 구간에만 맞는 규칙이 나온다.

추적 손절을 따로 두지 않았다. **이평 이탈이 이미 그 역할이다.** 둘 다 두면
어느 쪽이 먼저 걸리는지에 결과가 좌우되고, 파라미터가 하나 더 는다.

국면도 전략에 넣지 않았다. `RiskManager` 가 이미 국면별로 금액을 줄인다
(`regime_allocation`). 전략이 또 보면 두 번 적용된다.

### `strength` 는 돌파 폭으로 한다

`EntryIntent.strength` 는 지금 아무 데도 쓰이지 않는다. `RiskManager` 가
무시한다. 그렇다고 상수를 넣으면 나중에 순위를 매기고 싶을 때 다시 만들어야
한다. 직전 최고가 대비 초과 비율을 쓴다. **이미 읽은 데이터에서 나오므로
새 파라미터가 필요 없다.**

### 속도를 먼저 재기로 했다

더미는 `get_universe` 만 불렀다. 추세 전략은 매일 유니버스 200종목의 일봉을
읽는다. 2년이면 조회가 10만 번이고 지금 피드는 종목당 한 번씩 쿼리한다.

묶어 읽도록 먼저 고칠 수도 있지만 **재보지 않고 고치면 필요 없는 복잡도를
넣는 것**이다. 한 달 구간으로 재고 추정한 뒤 판단한다.

### 결과를 볼 때 기억할 것

숫자를 낙관 쪽으로 미는 것이 셋이다. 생존편향, 실측 아닌 슬리피지,
근거 없는 국면 배분. 셋 다 Phase 7 안에서 없앨 수 없다.

`ROADMAP.md` 가 "아슬아슬하게 통과하는 결과는 통과하지 못한 것으로 본다"
고 적은 이유가 이것이다. 체크리스트에도 옮겨 적었다. **기준을 낮추고
싶어질 때 읽으라고 적어둔 것이다.**

## 2026-08-30 (13) — 스윙 전략 구현, 권리락 결함을 찾았다

### 규칙은 승인받은 넷 그대로다

진입 둘(직전 60일 최고가 돌파 + 120일 이평 위), 청산 둘(20일 이평 이탈 +
진입가 -7%). 더 넣지 않았다.

돌파 기준은 **고가**다. 직전 N일의 `high` 최대값을 오늘 종가가 넘어야 한다.
종가끼리 견주는 것보다 엄격해 신호가 적게 나온다. 표본이 줄지만 이 데이터
양에서는 조건이 느슨한 쪽이 더 위험하다.

**직전 최고가에서 오늘을 뺀다.** 넣으면 오늘 종가가 오늘 고가를 넘을 수 없어
무엇도 돌파가 되지 않는다. 테스트로 고정했다.

진입 후보는 유니버스 순서(거래대금 상위)대로 채운다. 돌파 강도 순으로
정렬하는 안도 있었지만 **승인받지 않은 규칙을 하나 더 넣는 것**이다.
거래대금 순은 유동성 순이라 실제 체결에도 유리하다.

### 손절은 원주가, 추세는 조정가

`position.avg_price` 는 체결가 기반이라 **원주가**다. `get_candles` 는
**조정가**를 준다 (`INTERFACES.md` 3.3). 섞으면 조정계수만큼 어긋난 손절이
나간다.

- 손절: `get_quote`(원주가) vs `avg_price`(원주가)
- 추세 이탈: 조정 종가 vs 조정 이평

### 결함 — 권리락에 보유 수량이 안 맞았다

이 자리를 파다가 더 큰 것을 찾았다. **`Portfolio` 가 권리락에 아무것도 하지
않는다.** 20,000원 10주가 1:2 분할로 10,000원 20주가 되어야 하는데 10주로
남아 평가액이 그날 절반으로 증발한다.

`PROJECT.md` 11장이 "조정하지 않으면 분할일이 급락으로 보여 **손절이 대량
발동한다**" 고 이미 지목한 상황이다. 설계가 요구하는 것을 구현하지 않은
것이지 설계를 바꾸는 것이 아니라서 그대로 고쳤다.

**더미로는 안 보였다.** 하루만 들고 있어서다. 스윙은 몇 주를 들고 있다.
틀을 더미로만 검증한 것의 한계가 여기서 드러났다.

#### 평가액을 바꾸지 않는 것이 조정의 정의다

수량과 평단가를 반비례로 바꾼다. 원가 총액을 보존하므로 조정 순간 평가액이
그대로다. **분할은 부를 바꾸지 않는다.** 시장가가 반토막 나고 수량이 배가
되어 평가액도 그대로다.

단주는 모사하지 않는다. 반올림하되 원가 총액을 보존한다. 한 주 밑으로
줄어드는 감자여도 최소 한 주를 남겨 값을 버리지 않는다. 실제로는 현금
정산되지만 백테스트 근사로 받아들인다.

#### 체결보다 먼저 반영한다

권리락일 기준가는 **그날 시가부터** 적용된다. 체결 뒤에 조정하면 그날 매매가
조정 전 수량으로 일어난다.

대기 주문의 수량은 어제 정한 것이라 조정 전 수량이다. 분할 뒤 매도가 모자라게
나가면 남은 만큼은 다음 주기에 `manage` 가 다시 판다. **주문을 여기서 다시
계산하지 않는다.** 하루 늦어지지만 기계가 자기 판단을 덧쓰지 않는 편이 낫다.

## 2026-08-30 (14) — 속도는 괜찮고, 결과가 이상하다

### 속도: 재보길 잘했다

20 거래일에 7.8초. 2년치 약 3.2분이다. 묶어 읽도록 고칠 이유가 없다.

**안 재고 고쳤으면 필요 없는 복잡도를 넣을 뻔했다.** 10만 번 조회라는 숫자만
보면 느릴 것 같지만 실제로는 아니었다. 추정과 실측은 다르다.

### 한 달 결과가 이상하다 (진단 전)

2025-03 한 달에 -15.17%, 승률 8.33%(24건 중 2건). **같은 구간 더미는
-0.74% 였고 시세는 올랐다.**

시장이 오르는 달에 추세 추종이 15% 를 잃었다. 나쁜 전략도 승률 30~40% 는
나온다. 둘 중 하나다 — 규칙이 실제로 나쁘거나, 버그가 있거나.

#### 의심하는 것

**진입 후보를 유니버스 순서(거래대금 상위)대로 채운 것.**

돌파일에 거래대금이 가장 큰 종목은 대개 방금 급등한 종목이다. 그것을 다음 날
시가에 사면 고점 매수가 된다. 갭상승으로 열고 종가까지 밀리는 흐름이 국내
급등주에 흔하다. 그러면 진입 당일에 -7% 손절이 걸린다.

구현할 때 "돌파 강도 순 정렬은 승인받지 않은 규칙" 이라 거래대금 순을 골랐다.
**안전한 선택이라 생각했는데 반대일 수 있다.** 거래대금 순도 규칙이고,
그것이 하필 가장 나쁜 쪽을 고르는 규칙이었을 수 있다.

#### 지금 파라미터를 만지지 않는다

한 달 24건은 표본이 아니다. 여기에 맞춰 `stop_loss` 를 넓히면 그것이 곧
과최적화다. **한 달을 보고 고친 값은 그 한 달에만 맞는다.**

순서를 지킨다. 2년 기준선 → 매매 단위 진단 → 원인 확인 → 그때 조정.

#### 진단은 지표가 아니라 매매로 한다

`backtest_trade` 를 `exit_reason` 별로 묶어 건수·평균 수익률·평균 보유일을
본다. 지표만 보면 왜 그런지 모른다.

- 전부 `stop` 이고 보유일이 1~2일 → 고점 매수가 맞다. 진입 규칙 문제
- `signal` 이 섞이고 보유일이 길다 → 추세는 타는데 비용·손절폭 문제
- 평균 수익률이 -7% 에 몰림 → 손절이 거의 전부를 결정한다

`backtest_trade` 를 남겨둔 것이 여기서 값을 한다. 지표만 적재했으면
지금 아무것도 못 본다.

## 2026-08-30 (15) — 개발 구간 진단, 손절이 -7% 가 아니었다

`run_id 23` (2023-08-29 ~ 2025-08-28, 486 거래일). -50.88%, 승률 23.77%,
매매 355건. 2분 16초.

### 지표가 아니라 매매를 봐야 알 수 있었다

총수익률만 보면 "규칙이 나쁘다" 로 끝난다. `backtest_trade` 를 사유별로
묶으니 다른 그림이 나왔다.

| 구분 | 건수 | 평균 | 평균보유일 |
|---|---|---|---|
| 이익 | 82 | +19.70% | 35.4일 |
| 손실 | 263 | -9.34% | 9.4일 |

**손실은 9일에 끊고 이익은 35일 들고 간다.** 추세 추종이 원하는 비대칭이
정확히 나온다. 최대 이익 +142.71%. 우측 꼬리가 살아 있다. 규칙의 뼈대는
망가지지 않았다.

이평 이탈 청산(129건)은 평균 **+11.11%** 로 이익이다. 추세를 타는 부분은
작동한다. 문제는 손절 216건이 평균 **-10.53%** 라는 것이다.

### 손절이 규칙대로 -7% 에서 안 끊긴다

`stop_loss` 0.07 인데 실현 손실이 -10.53% 다. 3.5%p 가 더 빠진다.

**버그가 아니라 설계의 대가다.** 종가에 -7% 를 확인하고 다음 날 시가에 판다.
그 사이 갭하락이 얹힌다. 7% 빠진 종목이 다음 날 더 빠지는 것은 흔하다.

미래 참조를 구조적으로 막으려고 택한 체결 모델이 여기서 청구서를 내민다.
종가 체결로 바꾸면 이 손실이 사라지지만 그건 현실에 없는 체결이다.
**받아들이고 손절폭을 그 위에서 정해야 한다.**

### 산수가 얼마나 모자란지

승률 23.77%, 손익비 2.11배. 본전에 필요한 것은 3.21배 — 이익 평균 +30%.
매매당 기대값 -2.44%, 345건이면 -50%.

두 길뿐이다. 승률을 32% 로 올리거나, 이익 평균을 +30% 로 올리거나.

### 내 의심이 틀렸다

한 달 결과를 보고 "거래대금 순서로 후보를 채워서 고점을 산다" 를 의심했다.
**이익 평균 +19.7%, 최대 +142% 가 나오는데 고점만 사고 있을 수는 없다.**

한 달 24건으로 세운 가설이었고, 355건을 보니 다른 그림이었다.
**표본이 작을 때 세운 가설은 그럴듯해도 틀린다.** 그래서 그때 안 고쳤다.

### `--param` 을 넣었다 (확인 사항)

실험마다 YAML 을 고쳐 push 하는 왕복을 없앤다. 설정 파일은 건드리지 않고
그 실행에만 적용된다. **실제 사용한 값은 `backtest_run.params` 에 통째로
남아** 어떤 값으로 돌린 결과인지 추적이 끊기지 않는다.

`CLAUDE.md` 1 의 금지는 운영 파라미터를 런타임에 바꾸는 것을 막는 것이고,
백테스트 한 번의 입력은 거기 해당하지 않는다고 보고 확인받았다.

없는 키는 거부한다. **오타가 조용히 새 키를 만들면 아무것도 안 바뀐 채
바뀐 줄 알고 결과를 읽게 된다.** 값의 형은 `yaml.safe_load` 로 정해
설정 파일과 같은 규칙을 쓴다.

### 다음 실험은 둘뿐이다

손절이 전체 청산의 63% 다. 도움인지 해악인지가 승패를 가른다.

1. `stop_loss=0.99` — 사실상 끄고 이평 이탈만으로 청산. 손절의 순효과
2. `stop_loss=0.15` — 중간값

**값을 여러 개 훑지 않는다.** 그것이 곧 과최적화다. 가설 두 개만 본다.

## 2026-08-30 (16) — 손절 실험, 가설이 또 틀렸다

| 실행 | `stop_loss` | 총수익률 | MDD | 승률 | 매매 |
|---|---|---|---|---|---|
| 23 | 0.07 | -50.88% | 53.01% | 23.77% | 355 |
| 31 | 0.15 | -59.85% | 62.81% | 26.98% | 286 |
| 30 | 0.99 (끔) | -67.16% | 69.72% | 24.61% | 268 |

**손절은 해악이 아니라 방파제였다.** 넓힐수록 나빠진다. 손절이 비싸다고
(-10.53%) 봤는데, 없으면 더 비쌌다.

### 단조성이 진단이다

값 하나를 고르려고 돌린 실험인데 **방향이 답을 줬다.**

위험을 빨리 끊을수록 좋아진다는 것은 진입에 우위가 없다는 뜻이다. 우위가
있으면 손절폭에 중간 최적점이 생긴다. 너무 좁으면 이길 매매를 죽이고 너무
넓으면 손실이 커지기 때문이다. 좁을수록 좋기만 하다면 재고 있는 것은
**"덜 참여할수록 덜 잃는다"** 이고 그 끝은 "안 사는 것" 이다.

`stop_loss` 를 0.03 으로 내리면 숫자는 더 좋아질 것이다. **하지 않는다.**
전략이 아니라 매매를 줄이는 것이고 정확히 과최적화다. 개발 구간 숫자만
좋아지고 검증 구간에서 무너진다.

### 가설이 두 번 틀렸다

1. "거래대금 순서 때문에 고점을 산다" — 이익 평균 +19.7% 를 보고 지웠다
2. "손절이 해악이다" — 넓히니 나빠졌다

두 번 다 그럴듯했다. **재기 전에 고쳤으면 두 번 다 잘못된 방향으로 갔다.**
가설을 코드로 바로 옮기지 않은 것이 이 단계에서 가장 잘한 일이다.

### 지수 데이터가 없다

`-50%` 가 얼마나 나쁜지 판단하려면 시장을 알아야 하는데 지수 시계열이 DB에
없다. 지표 계산 때 키움에서 받아 쓰고 저장하지 않는다.

대신 **유니버스 자체의 표류**를 잰다. 같은 유니버스에서 아무 날 아무 종목이나
사서 20 거래일 들고 있었을 때의 평균과 중앙값이다. 우리 매매는 평균 15.6일
보유에 매매당 -2.44% 다. 견주면 돌파 필터가 무작위보다 나은지 나온다.

**평균과 중앙값을 함께 본다.** 평균만 보면 소수의 대박이 전체를 끌어올려
착시가 생긴다. 평균이 양수인데 중앙값이 음수면 "대부분 내리고 소수가 크게
올랐다" 는 뜻이고, 그건 추세 추종에 유리한 환경이라는 신호다.

### 다음 가설 — 유니버스가 오염돼 있다

`universe_at` 은 그날의 거래대금 상위를 뽑는다. **돌파일은 거래대금이
폭증하는 날이다.** 돌파했기 때문에 유니버스에 들어온 종목을 고르고 있을 수
있다. 유니버스 선정이 매매하려는 사건 자체에 오염된다.

스윙은 평소 유동성으로 유니버스를 잡아야 한다. 진입 규칙 변경이 아니라
6단계(유니버스 확정)의 일이다.

**또 가설이다. 재고 판단한다.**

## 2026-08-30 (17) — 신호를 백테스트 밖에서 쟀다

### 백테스트로는 못 가르는 것이 있다

백테스트 결과는 진입·청산·비용·자금관리가 전부 섞인 값이다. -50% 를 보고
"규칙이 나쁘다" 까지는 알아도 **어느 규칙이 나쁜지**는 모른다.

그래서 SQL 로 신호만 떼어 쟀다. 같은 유니버스에서 20 거래일 뒤 수익률을
'돌파한 날' 과 '나머지' 로 갈랐다 (2024-03 ~ 2025-08).

| 구분 | 표본 | 평균 20일 | 중앙값 |
|---|---|---|---|
| 나머지 | 62,363 | -0.59% | -2.26% |
| 돌파 | 5,882 | -1.00% | **-4.69%** |

**돌파가 무작위보다 나쁘다.** 표본 5,882건이라 우연이 아니다.
승인받은 진입 규칙이 이 유니버스에서 작동하지 않는다.

이 측정을 먼저 했으면 파라미터 실험 셋을 안 돌려도 됐다. **다음부터는
신호를 먼저 재고 백테스트를 돌린다.** 백테스트는 신호가 살아 있다는 것을
확인한 뒤에 비용과 자금관리를 얹어보는 도구다.

### 유니버스 자체가 내리막이다

`나머지` 의 중앙값도 -2.26% 다. 돌파든 아니든 이 유니버스의 종목은 20일 뒤
대개 내려 있다. **롱 온리가 구조적으로 불리한 모집단이다.**

그 모집단을 만든 것이 `universe_at` 의 "그날 거래대금 상위 200" 이다.
거래대금이 폭증한 날은 급등한 날이고 급등 뒤에는 되돌린다. 돌파 신호는
그 되돌림을 **더 정확히 집어내고 있는 셈**이다.

즉 돌파 규칙이 나쁜 것인지 오염된 모집단 안에서만 나쁜 것인지 아직 안
갈렸다. 같은 규칙을 평소 유동성 기준 유니버스에서 재면 갈린다.

### 우측 왜도는 있다

평균(-1.32) > 중앙값(-2.93). 대부분 내리고 소수가 크게 오른다.
**추세 추종에 유리한 환경인데도 못 벌었다.** 환경 탓만 할 수 없다.

돌파 쪽은 왜도가 더 크다 (평균 -1.00, 중앙값 -4.69, 차이 3.69%p).
소수의 대박은 있는데 중앙값 손실이 너무 커서 못 덮는다. 백테스트의
손익비 2.11 배(필요 3.21배)와 같은 이야기다.

## 2026-08-30 (18) — 유니버스가 범인이었다

같은 규칙, 같은 기간. 유니버스 선정만 바꿨다.

| 유니버스 | 구분 | 표본 | 평균 20일 | 중앙값 |
|---|---|---|---|---|
| 그날 거래대금 | 돌파 | 5,882 | -1.00% | -4.69% |
| 평소 유동성 | 돌파 | 2,235 | **+0.44%** | -2.43% |

**돌파 평균이 뒤집혔다.** 모집단(-0.55%) 대비 +1%p 우위다. 중앙값 열세도
-2.43%p 에서 -0.25%p 로 사라졌다.

### 왜 오염이었나

`universe_at` 이 그날 거래대금 상위를 뽑았다. **거래대금이 폭증한 날은 급등한
날이다.** 급등 뒤에는 되돌린다. 그러니 유니버스가 '되돌릴 종목' 으로 채워지고,
돌파 신호는 하필 그 되돌림을 가장 정확히 집어냈다.

매매하려는 사건(돌파=거래량 폭증)이 유니버스 선정 기준(거래대금)과 같은 것을
보고 있었다. **신호와 모집단이 같은 변수를 공유하면 안 된다.**

### 고친 것

`universe_at` 이 직전 `liquidity_days` 거래일의 **평균** 거래대금으로 순위를
매기고 **그날은 뺀다.** `config/universe.yaml` 을 새로 만들어 `size` 와
`liquidity_days` 를 뒀다. `limits.yaml` 의 `universe_size` 는 옮겼다.

**평소가 없는 종목은 넣지 않는다** (`HAVING COUNT(*) >= liquidity_days`).
신규 상장은 며칠 치 평균이 부풀어 유동성이 큰 것처럼 보인다.

달력일이 아니라 거래일 수로 세야 해서, 넉넉한 달력 구간(`days*2+10`)을 뽑은
뒤 종목별로 최근 N행만 쓴다.

`universe_at` 은 `BacktestFeed` 만 쓴다. 분봉 수집기의 `top_by_value` 는
다른 함수라 건드리지 않았다.

### 여유가 없다는 것을 적어둔다

절대 수익 +0.44% 에 왕복 비용이 0.43% 다. **고정 20일 기준으로는 본전이다.**
우위가 진짜인지는 비대칭 청산이 고정 20일보다 얼마나 버느냐에 달렸다.

숫자가 좋아졌다고 안심할 자리가 아니다. 이 정도 여유면 슬리피지 임시값
0.1% 가 틀렸을 때 부호가 바뀐다.

### 방법론이 하나 더 늘었다

신호를 백테스트 밖에서 재는 것에 더해, **신호와 모집단이 같은 변수를 보는지**
확인해야 한다. 이번엔 둘 다 거래량이었다. 백테스트 숫자만 봐서는 영원히
못 찾았을 것이다.

## 2026-08-30 (19) — Phase 7 을 미완으로 닫는다

### 손절 곡선의 모양이 바뀌었다

| `stop_loss` | 오염된 유니버스 | 깨끗한 유니버스 |
|---|---|---|
| 0.07 | -50.88% (23) | -34.38% (38) |
| 0.15 | -59.85% (31) | **-24.05%** (42) |
| 0.99 (끔) | -67.16% (30) | -47.02% (41) |

오염된 쪽은 **단조**였다. 깨끗한 쪽은 **중간 최적점**이 생겼다.

손절 실험을 처음 할 때 적어둔 문장이 그대로 맞았다.

> 진입에 우위가 있으면 손절폭에 중간 최적점이 생긴다. 너무 좁으면 이길
> 매매를 죽이고 너무 넓으면 손실이 커진다.

**진입 신호에 실제 우위가 있다는 세 번째 독립 증거다.** 서로 다른 방법
셋이 같은 것을 가리킨다.

1. SQL 신호 측정 — 돌파 +0.30% vs 모집단 -0.67% (표본 2,235)
2. 유니버스 교체 — -50.88% → -34.38%
3. 손절 곡선이 단조에서 중간 최적점으로

### 그런데 전부 손실이다

가장 나은 것이 -24.05% 다. **우위는 진짜인데 마찰과 모집단 하락을 못 넘는다.**

| | 값 |
|---|---|
| 돌파 신호 20일 수익 (다음 날 시가 진입) | +0.30% |
| 왕복 비용 | -0.43% |
| 남는 것 | **-0.13%** |

모집단 중앙값이 -2.2% 인 시장에서 롱 온리로 이만한 우위를 갖고는 안 된다.

### `stop_loss` 를 0.15 로 바꾸지 않았다

가장 나은 값이지만 넣지 않는다.

- 점 세 개로 고른 값이고 **검증 구간을 안 봤다**
- 지는 값 중 덜 지는 것을 고르는 데 의미가 없다
- **설정 파일에 넣는 순간 검증된 값처럼 보인다**

측정 사실만 남긴다. 재개하는 사람이 여기서 시작하면 된다.

### 재개 지점 — 다음 사람이 볼 것

세 갈래가 열려 있다. 우선순위 순이다.

1. **마찰을 줄인다.** 왕복 0.43% 가 우위 0.30% 보다 크다. 매매를 덜 하고
   더 오래 들고 가야 한다. `ma_exit` 을 늘리는 것이 첫 후보다.
   **`stop_loss` 는 0.15 근처가 개발 구간에서 나았다** (검증 안 됨)
2. **신호를 강화한다.** 돌파에 조건을 더한다. 규칙 변경이라 승인 대상이고
   조건이 늘수록 과최적화 위험이 는다
3. **모집단을 좁힌다.** 유니버스 200종목의 20일 중앙값이 -2.2% 다. 대형주
   쪽으로 더 좁히면 하락 편향이 줄 수 있다. 재보지 않았다

**검증 구간(2025-08-29 ~ 2026-08-28)은 아직 한 번도 열지 않았다.** 그대로 둔다.

### 방법론 셋을 남긴다

이 단계에서 얻은 것 중 전략보다 오래 갈 것들이다.

1. **신호를 백테스트 밖에서 먼저 잰다.** 백테스트 결과는 진입·청산·비용·
   자금관리가 섞인 값이라 어느 규칙이 나쁜지 못 가린다. SQL 한 번이
   백테스트 세 번보다 명확했다
2. **신호와 모집단이 같은 변수를 보면 안 된다.** 돌파=거래량 폭증인데
   유니버스도 거래대금으로 뽑고 있었다. 백테스트 숫자만 봐서는 못 찾는다
3. **오염된 데이터에서 얻은 결론은 오염을 고친 뒤 다시 재야 한다.**
   "손절은 방파제" 를 폐기하고 다시 재니 답이 달랐다

### 가설이 세 번 틀렸다

1. "거래대금 순서로 후보를 채워서 고점을 산다" — 이익 평균 +19.7% 를 보고 지웠다
2. "손절이 해악이다" — 오염된 유니버스에서 넓히니 나빠졌다
3. "익일 갭이 우위를 먹는다" — 갭은 0.30% 로 작았다

**세 번 다 그럴듯했고 세 번 다 재보고서야 알았다.** 코드로 바로 옮기지 않은
것이 이 단계에서 가장 잘한 일이다.

### 왜 여기서 멈추는가 (확인 사항)

`ROADMAP.md` 가 Phase 7 완료를 Phase 8 앞의 게이트로 걸어뒀는데 **그 게이트는
Phase 9(실전 소액) 앞에 있어야 맞다.**

Phase 8 항목은 전부 프레임워크다 — 주문 API, 중복 주문 방지, 재시작 복구,
`LiveFeed`, 엔진 프로세스, `command` 폴링, 포지션 대조, 비상 중단. 좋은
전략이 필요한 항목이 하나도 없다. 완료 기준도 기계적이다.

**모의투자는 돈이 나가지 않는다.** 나쁜 전략으로 돌려도 손해가 없고, 오히려
엔진 결함이 드러난다. 오늘 그 일이 일어났다 — 더미로는 안 보이던 백테스트
결함 둘(유니버스 오염, 권리락 미반영)을 실제 전략을 돌리다 찾았다.

**위험은 하나다.** 검증 안 된 전략이 검증된 것처럼 굳는 것. `SwingStrategy`
의 측정된 기대값은 매매당 -1.60% 다. 전략 파일, 설정 파일, `ROADMAP.md`
세 곳에 실전 금지를 박아뒀다.

## 2026-08-30 (20) — 엔진 운영 시간을 정했다 (확인 사항)

24시간 상주 하나, 19:00 판단, 08:30 동시호가 시장가 제출.

### 판단 시각이 데이터 경로를 정한다

처음엔 "15:40 장 마감 직후" 를 생각했는데 **불가능했다.** 백테스트는
`price_daily` 를 보고, 일봉 수집기는 19:00 에 돈다. 15:40 에는 그날 종가가
DB 에 없다.

브로커 API 로 직접 받는 안도 있었지만 `LiveFeed` 가 `price_daily` 가 아닌
다른 것을 보게 되어 **백테스트와 실전이 갈라진다** (`PROJECT.md` 8.3 이
막으려는 바로 그것이다). 200종목 × 120봉을 매일 받는 호출량도 크다.

**체결이 어차피 다음 날 시가라 판단을 서둘러도 얻는 것이 없다.** 15:40 이든
19:00 이든 결과가 같다. 그래서 데이터 경로가 같은 쪽을 골랐다.

이건 운영 시간을 정하려다 나온 것이다. **시각을 정하는 문제가 아니라
데이터 경로를 정하는 문제였다.**

### 동시호가 시장가라야 시가에 체결된다

백테스트가 "다음 날 시가 체결" 을 가정한다. 09:00 개장 후에 주문을 내면
시가가 아니라 그때 가격에 체결된다. **시가로 체결되려면 08:30~09:00 장 시작
동시호가에 넣어야 한다.**

슬리피지가 크다. 그런데 그것이 바로 `slippage_rate` 임시값 0.1% 를 실측하는
기회다. **Phase 8 산출물 중 가장 값진 것이 이 실측치가 될 것이다.**
백테스트 결과가 통째로 이 값에 걸려 있다.

### 상시 프로세스는 판단과 무관하게 필요하다

판단이 하루 한 번이어도 장중에 할 일이 있다 — 미체결 추적, `command` 폴링,
`heartbeat`. 배치 둘로는 안 된다. **장중에 비상 중단을 눌러도 반응할
프로세스가 없다.**

배치 둘 + 감시 상주로 셋을 나누는 안도 있었지만, 상태를 전부 DB 로 주고받아야
하고 재시작 복구가 셋 다 필요해진다. 24시간 상주 하나가 단순하다.
`telegram` 수집기가 이미 같은 패턴이라 운영 방식도 그대로 쓴다.

### 파생된 결정 넷

시간표를 정하니 따라 나온 것들이다.

- **미체결은 장 마감에 취소한다.** 백테스트에 미체결이라는 상태가 없다.
  시가가 없으면 주문을 버리는 백테스트 `_settle` 과 같은 처리다
- **일봉 수집이 실패하면 그날 판단을 건너뛴다.** '데이터가 없어서 안 샀다'
  는 안전한 실패다. 오래된 데이터로 판단하는 것보다 낫다
- **일봉 적재 여부는 `price_daily` 를 직접 본다.** heartbeat 는 프로세스가
  돌았다는 뜻이지 데이터가 들어왔다는 뜻이 아니다
- **수량은 19:00 종가로 계산하는데 체결은 다음 날 시가다.** 갭상승하면 현금이
  모자란다. 백테스트 `_buy` 의 감량 처리가 실전에도 그대로 필요하다.
  없으면 주문 거부가 나거나 미수가 난다

## 2026-08-31 — Phase 8 1단계 읽기 전용 완료

`get_balance`, `get_positions` 를 실측으로 구현하고 서버에서 확인했다
(435건 통과). **모의투자에서 계좌 API 가 전부 동작한다.** 주문 API 도 될
가능성이 크게 올라갔고, Phase 8 최대 불확실성이 상당히 줄었다.

### 실측이 잡아낸 것 셋

**종목코드 접두어.** `kt00018` 만 `A005930` 을 준다. 시세 API 는 `005930`
을 그대로 준다. 같은 증권사 API 안에서 형식이 갈린다. 벗기지 않았으면
`KRX:A005930` 이 되어 DB 의 어느 종목과도 안 맞았다. **문서만 봤으면
못 잡았다.**

**모의투자 수수료 0.35%.** 실전 키움 온라인 0.015% 의 23배다.
`sell_cmsn` 910 ÷ 평가금액 260,000 이 정확히 0.35% 이고 10원 미만 절사다.
→ **모의투자 손익을 백테스트와 직접 비교하지 않는다.** 슬리피지 실측
(체결가만 본다)에는 지장이 없어 Phase 8 의 목적은 유지된다.

**예수금과 주문가능금액이 다르다.** 매수 뒤 예수금(`entr`)은 10,000,000
그대로인데 주문가능금액은 9,718,980 이다. 차이 281,020 이 매입금액
280,060 + 수수료 960 과 정확히 같다.

국내는 **D+2 결제라 매수해도 예수금이 즉시 줄지 않는다.** `entr` 을 보고
주문을 냈으면 이미 쓴 돈을 또 썼다. `100stk_ord_alow_amt` 를 고른 판단이
여기서 검증됐다.

### `total_asset` 은 청산 비용을 뺀 값이다

`prsm_dpst_aset_amt` 10,007,949 를 검산하면
현금 9,718,980 + 평가 290,550 − 예상 매도비용 1,580 이다.

추정예탁자산이 **팔 때 낼 수수료·세금까지 미리 뺀 보수적인 값**이다.
`RiskManager` 가 총자산 대비로 한도를 잡으므로 이쪽이 안전하다.

### 엔진이 볼 값이 갈린다 (4단계에서 정한다)

백테스트는 `Portfolio.cash` 가 매수 한도였다. 실전은 `available`
(주문가능금액)이다. `RiskManager` 는 `total_asset` 으로 배분을 잡고
`available` 을 보지 않는다.

갭상승으로 현금이 모자랄 때 수량을 줄이는 처리(백테스트 `_buy`)를 실전에
옮길 때, **`available` 을 한도로 써야 한다.** 4단계에서 확정한다.

## 2026-08-31 (2) — 주문 API 가 모의투자에서 동작한다

`submit_order` 를 실측 규격으로 구현했다. **Phase 8 최대 불확실성이었던
"모의투자에서 주문이 되는가" 가 풀렸다.**

### 장이 닫혀 있는 것이 오히려 유리했다

19:06 에 작업했다. 처음엔 장중까지 기다려야 하는 줄 알았는데 반대였다.

키움은 **필드 검증을 먼저 하고 매매 엔진에 넘긴다.** 그래서 장 외 시간에
바디를 아무리 던져도 주문이 나갈 수 없으면서 검증 결과는 그대로 받는다.
빈 바디부터 하나씩 올려 필수 다섯을 확정했다.

```
{}                                   → 필수 파라미터=dmst_stex_tp
{dmst_stex_tp}                       → 필수 파라미터=stk_cd
{dmst_stex_tp, stk_cd}               → 필수 파라미터=ord_qty
{다섯 개 전부}                        → [2000](RC4058:모의투자 장종료)
```

마지막 줄이 답이다. **필드 검증을 통과해 매매 엔진까지 도달했다.** 장 외
시간이라 막힌 것이지 API 가 없어서가 아니다. 매수 `kt10000`, 매도 `kt10001`
둘 다 같다.

`ord_qty` 를 뺀 채로만 던지면 어떤 조합도 주문이 될 수 없다. 그것을 안전
장치로 삼아 필드를 탐색했다.

### 시장가 규격도 같은 방법으로 확인했다

운영 계획의 핵심이 08:30 동시호가 시장가라 `trde_tp=3` 을 미리 확인했다.

| 던진 것 | 결과 |
|---|---|
| `trde_tp=3`, `ord_uv` 생략 | 장종료 (= 바디 유효) |
| `trde_tp=3`, `ord_uv=""` | 장종료 (= 바디 유효) |

**시장가에 가격 필드가 필요 없다.** 둘 다 유효해서 구현은 빈 문자열을
보낸다 — 규격에 있는 필드를 빼는 것보다 명시적이다. 한 줄이라 바꾸기 쉽다.

### 거부를 예외로 던지지 않는다 (확인 사항)

`submit_order` 는 거부를 `OrderResult(status='rejected')` 로 돌려준다.
예외는 **네트워크·HTTP 오류에만** 나온다.

경계가 여기다.

- **거부** — 접수 실패가 **확정**됐다. 호출부가 그대로 DB 에 기록하고 끝낸다
- **예외** — 접수됐는지 **모른다.** `get_order_status` 로 확인해야 한다
  (CLAUDE.md 3)

둘을 같은 예외로 묶으면 호출부가 매번 문자열을 파싱해 구분해야 한다.
`OrderResult` 에 `error_code`·`error_message` 자리가 이미 있는 것도
이 구분을 전제한 설계로 보인다.

`_request` 가 `return_code != 0` 에 무조건 예외를 던져서 거부 본문을 읽을
수 없었다. `check_return_code` 플래그를 뚫었다. 기본값이 True 라 조회
경로는 그대로다.

**거부 사유 문자열을 파싱하지 않는다.** `[2000](RC4058:모의투자 장종료)`
안에 코드가 또 들어 있는데, 한 건 본 것으로 형식을 단정하지 않는다.
`error_code` 에는 `return_code` 를, `error_message` 에는 원문을 그대로 담는다.

### probe 가 주문을 두 번 낼 뻔했다

체크리스트에 "주문 응답 샘플을 고정할 때도 같은 도구를 쓴다" 고 적혀 있는데
`probe.py` 는 `_call` 을 부르고 있었다. **`_call` 은 `TransientError` 를
세 번까지 재시도한다.**

조회 도구였을 때는 맞는 선택이었다. 시험주문을 이 도구로 내는 순간 재시도
금지 규칙이 깨진다. `_call_once` 를 뽑아 probe 와 `submit_order` 가 그쪽을
쓴다.

**용도가 바뀌면 전제도 바뀐다.** 도구를 만들 때 옳았던 것이 그 도구를 다른
데 쓰는 순간 결함이 된다.

### 아직 못 뜬 것 하나

**성공 응답의 주문번호 필드.** 성공 응답은 장중에만 나온다. 규격상 `ord_no`
로 두되, 없으면 `logger.error` 로 응답 키 목록을 남긴다.

예외는 던지지 않는다. **그 시점에 주문은 이미 나갔다.** 예외를 던지면
접수된 주문의 번호를 잃는다. 조용히 `None` 이 되는 것만 막는다.

다음 개장 때 **하한가 지정가 1주**로 확정한다. 정상 접수되면서 체결되지
않아, 그 한 건으로 `submit_order`·`get_order_status`·`cancel_order` 를
전부 실측할 수 있다.

### 곁가지로 얻은 것

`ka10001` 이 상하한가를 직접 준다 — `upl_pric` / `lst_pric`. 2026-08-31
삼성전자는 기준가 257,000 에 하한가 180,000 이다. 지정가 검증에 쓸 수 있다.

## 2026-08-31 (3) — 주문 조회 API 조사와 get_order_status

`ka10075`(미체결) + `ka10076`(체결) 로 `get_order_status` 를 구현했다.
후보가 넷이었고 고른 이유가 분명하다.

### 스펙 파일을 통째로 받았다

공식 포털이 SPA 라 문서가 안 열린다고 적어뒀었는데, **공식 저장소에
`kiwoom/_data/kiwoom_api_spec.json` 이 있다.** 337개 API 의 요청·응답 필드,
필수 여부, 설명이 전부 들어 있다. 앞으로 API 를 찾을 때 여기부터 본다.

### 넷 중 둘을 고른 이유

| api-id | 상태 필드 | 수수료·세금 | 미체결 | 체결 |
|---|---|---|---|---|
| `ka10075` 미체결 | `ord_stt` | 있다 | O | X |
| `ka10076` 체결 | `ord_stt` | **있다** | X | O |
| `kt00007` 상세 | 없다 | 없다 | O | O |
| `kt00009` 현황 | 없다 | 없다 | O | O |

`kt00007` 하나면 한 번의 호출로 끝난다. 그런데 **수수료·세금이 없다.**
`SCHEMA.md` 의 `execution` 테이블이 "수수료와 세금을 반드시 기록한다" 고
못박고 있다. 백테스트 비용 가정을 실제와 대조하려면 그 값이 있어야 한다.

호출 하나를 아끼려다 Phase 8 의 목적인 비용 실측을 잃을 뻔했다.
`get_balance` 가 `kt00001`+`kt00018` 을 합치는 것과 같은 판단이다.

### 주문번호로 조회하는 API 가 없다

넷 다 목록을 받아 우리 주문번호를 찾아야 한다. `ka10076` 에 `ord_no`
파라미터가 있어서 조회 키인 줄 알았는데 **"입력한 주문번호보다 과거"**
라는 페이징 필터였다. 스펙 설명을 안 읽었으면 그대로 썼을 것이다.

### `ord_stt` 를 믿지 않는다

응답에 `ord_stt`(주문상태)가 한글로 온다. 실측으로 본 값은 `"체결"`
하나뿐이다. 접수·확인·취소가 무엇으로 오는지 모른다.

**어휘를 다 모르는 채로 문자열을 분기하면 조용히 틀린다.** 상태는
`ord_qty`·`cntr_qty` 로 끌어낸다. 수량은 두 API 가 같은 이름으로 주고
뜻이 명확하다.

### 형식이 또 갈렸다

같은 증권사 API 안에서 세 번째다.

| | 종목코드 | 금액 | KRX |
|---|---|---|---|
| `kt00018` | `A005930` | 15자리 제로패딩 | `"KRX"` |
| `ka10076` | `005930` | `"1116"` 그냥 숫자 | `"1"` |

`to_amount` 가 둘 다 읽어서 금액은 지장이 없다. **거래소 구분이 문자열
`"KRX"` 와 `"1"` 로 갈리는 것이 함정이다.** 잘못 보내면 조회가 빈다.

수수료는 교차검증됐다. 삼성전자 `tdy_trde_cmsn` 870 이 `kt00018` 의
`pur_cmsn` 870 과 같다. 두 API 가 같은 값을 준다.

### 조회는 장종료에 막히지 않는다

주문은 `모의투자 장종료` 로 거부되는데 **조회는 그대로 된다** (19:20 실측).
15:30 이후 미체결을 취소하는 계획이 성립한다. 막혔으면 시간표를 다시
짜야 했다.

### 규격을 둘 고쳤다 (확인 사항)

`OrderResult` 는 `client_order_id` 를 필수로 갖는데 **`get_order_status`
와 `cancel_order` 는 그 값을 알 방법이 없다.** 키움이 우리 멱등성 키를
저장하지 않는다 — 보내지도 않는다.

호출부는 `broker_order_no` 를 `order_request` 에서 찾아왔으므로 같은 행의
`client_order_id` 를 갖고 있다. 인자로 받아 에코한다.

`cancel_order` 는 `stock_id` 도 받는다. `kt10003` 이 종목코드를 필수로
요구한다. 15:30 취소 시점에 엔진은 `ka10075` 미체결 목록을 이미 갖고 있어
종목코드를 안다.

**어댑터 안에서 조회해 채우는 안**도 있었지만, 취소마다 호출이 하나 늘고
그 사이에 체결되면 목록에서 사라져 취소할 것을 못 찾는다.

### 유량 초과가 본문 코드로도 온다

오류표에 `1700`·`1701`·`1702`(유량 초과)가 있다. 지금 어댑터는 HTTP 429
만 `RateLimitError` 로 보고 본문 `return_code` 는 전부 `PermanentError` 다.
**재시도하면 될 것을 안 하게 된다.** 1단계 남은 항목인 에러 분류에서 다룬다.

RC4058(장종료)은 오류표에 없다. 37개가 전부 게이트웨이 계층(1xxx)이고
매매 엔진의 RC 코드는 다른 계층이다. **거부 메시지를 파싱하지 않기로 한
판단이 여기서 뒷받침됐다.**

### 아직 모르는 것 둘

- **취소된 주문이 어느 목록에도 없을 수 있다.** 지금은 `PermanentError` 다.
  재시작 복구(`INTERFACES.md` 2.2)에서 문제가 될 수 있어 실측이 필요하다
- **한 주문이 여러 행으로 오는지** 못 봤다. 부분체결에서 나올 수 있고
  그러면 수량 계산이 틀린다. 여러 행이면 `logger.error` 를 남긴다.
  Phase 8 완료 기준에 부분체결 확인이 들어 있으니 거기서 잡힌다

## 2026-08-31 (4) — 유량 초과는 결함이 아니었다

앞 절에서 "유량 초과가 본문 코드로도 온다, 재시도하면 될 것을 안 하게
된다" 고 적었다. **실측해 보니 틀렸다.**

간격 없이 시세 조회를 12번 던졌다. 전부 HTTP 429 였고 `RateLimitError`
로 제대로 잡혔다. 본문 `return_code` 로는 한 번도 오지 않았다.
**코드는 처음부터 맞았고 고칠 것이 없다.**

### 왜 틀렸나

오류표에 `1700`(유량 초과)이 `1511`(필수 입력 누락)과 같은 목록에 있었다.
`1511` 은 본문으로 오는 것을 실측으로 봤다. 그래서 **"같은 표에 있으니
1700 도 본문으로 오겠다"** 고 추론했다.

표가 같다고 전달 계층이 같지 않다. 게이트웨이가 어떤 것은 HTTP 상태로,
어떤 것은 본문으로 내보낸다. 표는 코드 목록이지 전달 방식의 명세가 아니다.

**실측 없이 결함이라고 보고한 것이 잘못이다.** 이 프로젝트가 다른 API 를
전부 실측으로 정한 이유가 이것인데, 정작 오류 분류에서는 표를 읽고 단정했다.

### 덤으로 얻은 것

12회 중 0번째와 3번째만 통과했다. **초당 1건**이라는 2026-08-28 기록과
맞는다. `RateLimitError` 의 기본 `retry_after` 1.0초가 이 값에 맞다.
`_throttle` 의 `min_interval` 기본값 1.0 도 마찬가지다.

측정한 것은 **api-id 별 유량**이다. `1701`(총유량)·`1702`(그룹유량)은
재보지 않았다. 어댑터가 api-id 별로 간격을 두므로 닿기 어렵다.

### 커밋을 push 하지 않고 있었다

측정하려다 서버에 `_call_once` 가 없어서 드러났다. **이 세션의 커밋 4개가
전부 로컬에만 있었다.** 앞선 probe 들은 옛 `probe.py`(재시도하는 `_call`)
로 돌았다. 조회와 거부뿐이라 결과에는 지장이 없었다.

**시험주문 때 이러면 안 된다.** 응답을 못 받으면 주문이 두 번 나간다.
체크리스트의 시험주문 절차에 `git pull` 을 명시했다.

여기서 배운 것 — **재시도 금지를 코드로 막아놔도 그 코드가 배포돼 있지
않으면 소용이 없다.** 안전장치는 도달해야 안전장치다.

## 2026-08-31 (5) — cancel_order, 응답 실측 전에 만든 이유

미루려던 것을 앞당겨 구현했다. 다시 보니 **성공 응답의 필드에 로직이
걸려 있지 않았다.** `return_code == 0` 이면 접수된 것이고, 응답의 새
주문번호는 로그에만 쓴다. 미확정인 것이 동작을 바꾸지 않는다.

`submit_order` 때는 주문번호를 반드시 읽어야 해서 사정이 달랐다.
**"응답이 미확정" 이라는 말이 곧 "구현할 수 없다" 는 뜻은 아니다.**
그 필드에 무엇이 걸려 있는지를 봐야 한다.

### 원 주문번호를 돌려준다

키움은 취소에 **새 주문번호를 발급한다** (`ord_no` 가 새 번호,
`base_orig_ord_no` 가 원 번호). 새 번호를 `OrderResult` 에 담으면 엔진이
추적하던 번호가 바뀐다. 원 번호를 돌려주고 새 번호는 로그에만 남긴다.

취소 뒤 조회에서 새 번호로 뜰 수도 있어서 로그가 필요하다. 내일 6번
단계에서 확인한다.

### 취소 거부는 예외로 던진다 (submit_order 와 다르다)

`submit_order` 는 거부를 `status='rejected'` 로 돌려주기로 정했는데
취소는 예외다. 일관성이 없어 보이지만 이유가 있다.

- **주문 거부**는 그 주문의 **최종 상태**를 확정한다. `rejected` 가
  `order_request.status` 로 그대로 들어간다
- **취소 거부**는 주문의 상태를 확정하지 않는다. 주문은 여전히 살아
  있거나 이미 체결됐다

`OrderResult` 는 주문을 기술하는 타입이다. 취소 실패를 담을 자리가 없다.
`status='rejected'` 로 돌려주면 호출부가 그대로 DB 에 쓸 위험이 있는데,
그러면 체결된 주문이 거부로 기록된다.

15:30 일괄 취소 중 방금 체결된 건이 이 경로로 온다. 드문 일이 아니라
정상 흐름이므로 호출부가 `try/except` 로 감싸고 다음 주기에 다시 본다.

### `filled_qty` 가 함정이다

취소 응답에 체결량이 없다 — 취소수량(`cncl_qty`)만 온다. 그래서
`OrderResult.filled_qty` 를 0 으로 채웠는데, **이 0 은 '모른다' 이지
'체결이 없었다' 가 아니다.**

부분체결된 주문을 취소한 뒤 이 값으로 DB 를 덮어쓰면 체결 기록이
사라진다. docstring 에 적고 **2단계 체크리스트에 항목으로 올렸다.**
문서로만 막는 것이라 약하다. 2단계에서 코드로 막을 자리를 찾는다.

호출부가 조회해 채우게 하는 안도 있었지만, **취소된 주문은 미체결
목록에서 빠지므로 흔한 경우에 조회 둘이 헛돌고 에러 로그만 남는다.**

## 2026-08-31 (6) — 2단계 중복 주문 방지

`common/db/orders.py` 와 `common/order.py` 를 만들었다. 테이블은 이미
마이그레이션 `001` 에 있어 스키마 변경이 없었다.

### 커밋이 이 코드의 존재 이유다

`place_order` 는 트랜잭션을 **둘**로 나눈다.

```
with transaction(conn):   # 1) pending INSERT — 여기서 커밋된다
    record_pending(...)
result = broker.submit_order(...)   # 2) 주문
with transaction(conn):   # 3) 응답 반영
    apply_result(...)
```

하나로 묶으면 안 된다. 주문은 나갔는데 예외로 롤백되면 **기록이 사라지고
다음에 같은 주문을 또 낸다.** 증권사는 우리 `client_order_id` 를 모르므로
(보내지도 않는다) 멱등성이 이 행 하나에 달려 있다.

`INTERFACES.md` 2.1 이 네 단계를 나열한 이유가 이것이고, 함수를 따로 둔
이유도 이것이다. 호출부가 편의로 한 트랜잭션에 묶는 일을 막는다.

### 문서로 막던 것을 SQL 로 막았다

앞 절에서 "`cancel_order` 의 `filled_qty` 0 은 '모른다' 이니 덮어쓰지 말
것" 을 docstring 과 체크리스트에 적었다. **문서로만 막는 것이라 약하다고
적어뒀는데, 자리를 찾았다.**

```sql
filled_qty = GREATEST(filled_qty, %s)
```

체결량은 줄어들지 않는다. 취소뿐 아니라 **늦게 도착한 오래된 응답**도
같이 막힌다. 호출부가 무엇을 넘기든 기록이 뒤로 가지 않는다.

체결가도 같은 이유로 `CASE WHEN %s > 0` 으로 체결이 있는 응답만 덮어쓴다.
주문번호는 `COALESCE` 로 지킨다 — 취소·조회 응답에 번호가 없어도 받아둔
값을 잃지 않는다.

**불변식을 아는 계층에 두는 것이 낫다.** 호출부마다 조심하는 것보다
UPDATE 문 한 줄이 확실하다.

### ULID 를 직접 짰다 (확인 사항)

48비트 밀리초 + 80비트 난수, Crockford Base32 26자. 의존성 추가와
직접 구현 중 후자를 골랐다 — 운영 의존성이 여섯 개뿐이고 전부 없으면
안 되는 것들이다.

**단조증가는 넣지 않았다.** 같은 밀리초에 두 번 부르면 순서가 섞이는데,
시간순은 `created_at` 인덱스가 잡고 중복은 UNIQUE 가 막는다. 정렬
가능성을 실제로 쓰는 곳이 없어 쓰지 않는 보장을 만들지 않았다.

`time.time()` 을 직접 부른다. 전략 판단이 아니라 식별자 생성이고
(CLAUDE.md 2 의 시스템 레벨 예외), 백테스트는 이 경로를 타지 않는다 —
자체 `Portfolio` 로 체결을 흉내낸다.

### 커밋하는 테스트 픽스처가 필요했다

기존 `cur` 픽스처는 롤백한다. 그런데 **`place_order` 의 핵심이 커밋**이라
롤백 픽스처로는 확인할 수 없다. `db_conn` 픽스처를 더했다 — 커밋되는
커넥션을 주고, 끝나면 `order_id` 수위표 위의 모의투자 계좌 행만 지운다.

'무엇을 확인하려는지' 가 픽스처 모양을 정했다. 롤백 픽스처를 그대로 썼으면
테스트는 통과하는데 정작 막으려던 사고는 못 잡았을 것이다.

### 자리

`common/order.py` 를 최상위에 뒀다. `common/risk.py` 가 이미 그 자리에
있고, 나중에 단타 엔진도 같은 흐름을 쓴다. `engines/swing/` 에 두면
재사용이 안 된다.

`PROJECT.md` 디렉토리 트리에 `risk.py` 가 빠져 있다. 기존부터 그랬고
내 작업과 무관해 건드리지 않았다.

### 가드가 내 테스트를 잡았다 (2026-08-31)

서버에서 `tests/test_orders.py` 를 돌리니 1건이 깨졌다. **프로덕션 코드가
아니라 테스트 잘못이었다.**

```
RuntimeError: 이미 트랜잭션이 열려 있습니다.
```

`list_open_orders` 를 raw 커서로 먼저 읽어 기준선을 잡았는데, **psycopg 는
SELECT 하나에도 트랜잭션을 연다.** 그 상태로 `place_order` 에 들어가니
`transaction()` 이 거부했다.

이 가드는 2026-08-27 에 일봉 백필 204만건을 잃고 세운 것이다. 세이브포인트만
만들어져 커밋된 줄 알고 넘어가는 사고를 막는다. **그 가드가 이번엔 내
테스트를 잡았다.**

읽고 나서 `rollback()` 으로 트랜잭션을 닫는 헬퍼를 뒀다. 커밋되는 커넥션을
쓰는 테스트는 읽기도 조심해야 한다 — 롤백 픽스처를 쓸 때는 없던 제약이다.

## 2026-08-31 (7) — 3단계 LiveFeed

`common/feed/live.py`. 이 단계는 코드보다 **무엇을 어디서 가져오는지**가
전부다.

### 같은 함수를 부르는 것이 규약이다

`LiveFeed` 는 `candles_until` · `universe_at` · `regime_history` 를
`BacktestFeed` 와 **똑같이** 부른다. 기준을 옮겨 적지 않았다.

브로커가 일봉도 유니버스도 줄 수 있지만 그러면 데이터 경로가 갈린다.
`PROJECT.md` 8.3 이 막으려는 것이고, `INTERFACES.md` 3.2 도 `LiveFeed`
출처를 "Broker + DB" 로 적어 시세만 브로커임을 명시하고 있다.

Phase 7 에서 유니버스 기준 하나가 총수익률을 -50.88% 와 -34.38% 로 갈랐다.
같은 일이 백테스트와 실전 사이에서 일어나면 **백테스트 결과 전체가
무의미해진다.** 여기가 Phase 8 에서 가장 조심할 자리였다.

### 갈라짐을 테스트로 고정했다

`Candle` 을 만드는 코드가 두 피드에 각각 있다. 공용 함수로 뽑을까 했는데,
`BacktestFeed` 를 건드리는 것보다 **등가성 테스트**가 낫다고 봤다.

```
같은 거래일 → 두 피드의 get_candles 결과가 같다
같은 거래일 → 두 피드의 get_universe 결과가 같다
```

값뿐 아니라 **봉의 시각까지 같아야** 통과한다. 빈 목록끼리 같아서 통과하는
것을 막으려고 `assert expected` 를 함께 뒀다.

구조로 막는 것과 테스트로 막는 것 중 후자를 골랐다. 공용 함수는 한쪽이
쓰지 않게 되면 조용히 갈라지지만, 등가성 테스트는 갈라지는 순간 깨진다.

### 빈 유니버스를 예외로 만들지 않았다

`universe_at` 은 `WHERE p.trade_date = %s` 로 **그날 거래된 종목**을
요구한다. 19:00 일봉 적재 전에는 빈 목록이 나온다.

여기서 예외를 던지고 싶었다 — 빈 유니버스는 '후보 없음' 과 구분되지 않아
조용한 실패가 되기 때문이다. **그런데 그러면 `BacktestFeed` 와 동작이
달라진다. 그것이 곧 갈라짐이다.**

일봉 적재 확인은 엔진의 일로 남기고 4단계 체크리스트 맨 앞에 올렸다.
운영 시간표를 정할 때 이미 "일봉 적재 여부는 `price_daily` 를 직접 본다"
고 적어둔 것과 같은 결론이다.

### 커서일은 한국 날짜다

`now()` 의 UTC 날짜를 그대로 쓰면 오전 09:00 이전 한국 시각이 전날로
밀린다. 08:30 동시호가 제출 시각이 정확히 그 구간이다.
거래일은 시장 현지 기준으로 저장한다 (CLAUDE.md 5).

### 상주 프로세스라 커넥션을 다르게 잡았다

`BacktestFeed` 는 `cur` 를 받아 배치 트랜잭션 하나 안에서 산다.
`LiveFeed` 는 24시간 도는 프로세스가 쓰므로 트랜잭션을 오래 열어둘 수 없다.

커넥션을 받아 **autocommit 으로 두고** 조회마다 짧게 쓴다. 읽은 뒤
`rollback()` 을 부르는 방식도 있었지만, 그 커넥션을 주문 기록과 공유하면
남의 미커밋 작업을 지운다. autocommit 이 그 위험이 없다.

**읽기 전용 커넥션을 준다. 주문 기록 커넥션과 공유하지 않는다.**
docstring 에 적었다.

### get_signals 는 비워뒀다

`BacktestFeed` 도 비어 있고 **부르는 곳이 없다.** `signal` 을 읽는
DB 함수도 없다. 읽는 쪽이 생기면 그때 만든다.

### autocommit 을 켜지 않고 요구한다 (2026-08-31)

서버 테스트에서 3건이 깨졌다.

```
psycopg.ProgrammingError: can't change 'autocommit' now:
connection in transaction status INTRANS
```

테스트가 기준일을 먼저 SELECT 해서 트랜잭션이 열린 채로 생성자에
들어갔다. **테스트만의 문제가 아니었다.**

`__init__` 에서 `self.conn.autocommit = True` 로 켜던 것이 문제다.

- 남의 커넥션 상태를 바꾼다
- **이미 트랜잭션이 열려 있으면 생성자가 터진다.** 4단계 엔진이 우연히
  조회를 먼저 하면 같은 일이 난다

켜는 대신 **요구하고 확인한다.** `autocommit` 이 아니면 `RuntimeError` 다.
읽기만 하므로 트랜잭션 상태와 무관하고, 계약이 생성자에 드러난다.

앞서 `db_conn` 테스트도 같은 뿌리에서 깨졌다 — **psycopg 는 SELECT 하나에도
트랜잭션을 연다.** 커넥션을 넘겨받는 코드는 이 사실을 계약에 적어야 한다.

## 2026-08-31 (8) — 4단계 시작 전에 정한 다섯

장이 닫혀 있어(21:37) 시험주문을 할 수 없었다. 4단계는 장과 무관하므로
그쪽을 연다. 체크리스트의 "아직 안 정한 것" 셋에 설계 갈림길 둘이 더해져
다섯을 정했다.

### 폴링 10초 / heartbeat 60초

`telegram` 수집기가 60초 하나로 둘을 겸한다. 그대로 맞추려다 나눴다.

**비상 중단 버튼의 반응 속도가 폴링 주기다.** 5단계 완료 기준에 그 버튼이
있는데, 전량청산을 눌러놓고 최대 60초를 기다리는 것은 장중에 길다.

`command` 는 DB 폴링이라 키움 API 유량(초당 1건, 2026-08-31 실측)과
무관하다. 10초로 줄여도 부담이 없다. heartbeat 는 화면이 생사를 보는
용도라 60초로 충분하고, 6배로 늘리면 쓰기만 늘고 얻는 게 없다.

루프 틱을 10초로 두고 6틱마다 신호를 찍는다. 값 둘 다 `engine.yaml` 에 둔다.

### allocation 0 = 무제한

`accounts.yaml` 에 `allocation` 필드가 있는데 **읽는 코드가 없었다.**
세 계좌 모두 0 이다.

셋 중에 골랐다.

| 안 | 문제 |
|---|---|
| 항상 강제 | 지금 값이 0 이라 아무것도 안 산다. 설정을 먼저 고쳐야 한다 |
| 무시 | Phase 9 실전 소액 운용 때 다시 만들어야 하고 필드가 계속 죽어 있다 |
| **0 = 무제한** | 설정을 안 바꿔도 돌고, 장치가 코드 한 줄로 생긴다 |

`min(브로커 총자산, allocation)` 을 `RiskManager` 에 넘긴다. Phase 9 에서
실계좌에 소액만 굴리려면 숫자 하나만 채우면 된다.

### daily_pnl 은 15:40

15:30 미체결 취소와 19:00 판단 사이가 비어 있었다.

15:30 정각은 이르다. **장 마감 동시호가가 15:20~15:30 이라** 그 체결이
브로커 잔고에 반영되기 전일 수 있다. 10분을 둔다.

19:00 판단 직전에 붙이는 안도 있었지만, **일봉 미적재로 판단을 건너뛰는
날 스냅샷까지 같이 빠진다.** 손익 기록이 데이터 적재 성공 여부에 걸리면
안 된다. `daily_pnl` 은 브로커에서 예수금·평가금액을 받으므로
`price_daily` 와 애초에 무관하다.

15:40 은 `CLAUDE.md` 의 배포 허용 시각과 같아 기억하기도 쉽다.

### 주문 계획을 signal 테이블에 남긴다

여기가 4단계의 구조를 정하는 결정이었다.

백테스트 `BacktestLoop` 는 `self.pending` 리스트로 하루를 넘긴다. 실전에
그대로 옮기면 **19:00 과 08:30 사이 13시간을 메모리로 버텨야 한다.**

그 사이 재시작이 드물지 않다. **배포가 15:40 이후로 정해져 있어** 저녁
배포가 정상 운영이다. 서버 재부팅도 있다. 그때마다 다음 날 주문이 통째로
사라지는데, 로그를 보지 않으면 '오늘은 후보가 없었나 보다' 와 구분되지
않는다. **조용한 실패다.**

`signal` 테이블이 정확히 이 용도로 설계돼 있었다.

- `consumed_at` — 주문으로 이어진 시각
- `order_request.signal_id` — 어느 신호가 어느 주문이 됐는지
- `idx_signal_pending ... WHERE consumed_at IS NULL` — 미소비 목록 인덱스

**스키마를 바꾸지 않는다.** 이미 있는 것을 안 쓰고 있었을 뿐이다.
`LiveFeed.get_signals` docstring 에 "`signal` 적재는 엔진이 하고(4단계)"
라고 적어둔 것과도 맞는다.

### 수량은 signal 에 담지 않고 08:30 에 다시 뽑는다

계획을 남기기로 하니 "수량도 같이 남길까" 가 따라왔다. 담지 않기로 했다.

`payload` 는 "전략별 근거" 다 (`SCHEMA.md` 5장). 주문 수량은 근거가 아니라
운영 값이다. 넣으면 규정이 넓어지고, 나중에 payload 를 읽는 쪽이 무엇을
기대해야 할지 흐려진다.

**담지 않아도 되는 이유가 더 크다.** `RiskManager._budget` 이 이미 네 값의
최솟값을 잡는데 그중 하나가 `ctx.balance.available` 이다. 08:30 에 그
시점 잔고로 다시 돌리면 주문가능금액이 자동으로 반영된다.

이게 곧 백테스트 `_buy` 의 갭상승 감량이다. 백테스트는 종가로 정한 수량이
시가에 안 맞으면 줄이고, 실전은 08:30 잔고로 처음부터 맞춰 뽑는다.
**결과가 같은 곳에 도달한다.** 09-01 실측에서 확인한다.

`entr`(예수금)이 아니라 `100stk_ord_alow_amt`(주문가능금액)를 보는 것은
2026-08-31 에 이미 정했다. 국내는 D+2 결제라 매수해도 예수금이 즉시 줄지
않아, `entr` 을 보면 이미 쓴 돈을 또 쓴다.

### 대조 불일치는 진입만 막는다

체크리스트에 "브로커 잔고와 DB 가 어긋나면 멈춘다" 고만 적혀 있어 범위를
정했다.

**전부 멈추면 청산도 멈춘다.** `INTERFACES.md` 4.1 이 "신규 진입을 막은
상태에서도 청산은 계속 동작해야 한다" 고 못박은 이유가 이것이다. 들고
있는 종목이 손절선을 넘었는데 대조가 안 맞는다고 몸이 묶이면, 원장이
어긋난 것보다 그쪽이 위험하다.

브로커 값으로 덮어쓴다 — `SCHEMA.md` 가 "증권사 잔고를 정본으로 삼는다,
이 테이블은 캐시다" 라고 이미 정해뒀다. `event_log` 와 알림을 남기고
`halt_entry` 상태로 들어간다. 사람이 보고 푼다.

## 2026-08-31 (9) — 4단계 engine-swing

`engines/swing/` 을 만들었다. 하루의 순서는 `BacktestLoop` 와 같고, 실전에만
있는 것들(체결 추적·미체결 취소·명령 폴링)이 그 사이에 들어간다.

### 시각 분기를 순수 함수로 뺀 것이 가장 값졌다

`schedule.due_tasks(now, table, done, retry_at)` 하나가 시간표 판정 전부다.
상주 루프 안에 `if now.time() >= ...` 를 묻어두면 **테스트할 방법이 없다** —
하루를 기다리거나 시계를 조작해야 한다.

빼놓으니 경계 조건 9개를 0.06초에 확인할 수 있었다. 그중 둘은 뺐으면
놓쳤을 것이다.

- **정각이 아니라 '지났는데 안 했으면 한다'.** 15:00 에 올라온 프로세스가
  08:30 제출을 건너뛰고 15:30 취소부터 하는 것이 옳다
- **`retry_at` 은 `done` 과 다르다.** 19:00 에 일봉이 없어 20분 뒤 다시 볼
  때 `done` 에 넣으면 그날이 끝나버린다

### 공휴일을 달력 없이 풀었다

여기서 한 번 막혔다. 08:30 에 전날 계획을 내는데 **오늘이 휴장일인지
모른다.** `exchange_holiday` 를 보려 했더니 `collectors/market/holidays.py`
가 **일봉에서 역산해** 채우고 있었다. 과거 휴일만 안다.

주말은 요일로 걸러진다. 공휴일이 남았다.

풀이는 달력이 아니라 데이터였다. **계획은 그것을 만든 일봉이 여전히 최신일
때만 유효하다.**

| 상황 | 최신 일봉 | 계획의 날짜 | 판정 |
|---|---|---|---|
| 평범한 화요일 아침 | 월 | 월 | 유효 |
| 월요일이 휴장, 화요일 아침 | 금 | 금 | **유효** |
| 엔진이 이틀 쉼, 수요일 아침 | 화 | 금 | 폐기 |

세 경우가 전부 맞는다. 구현은 `pending_signals` 에 넘길 `since` 를 마지막
거래일의 `plan_time` 으로 잡는 것 하나다.

**달력이 필요한 문제로 보였는데 아니었다.** 휴장일에는 일봉이 안 쌓인다는
사실이 이미 달력 노릇을 하고 있었다.

### 수량을 계획에 담지 않아도 되는지 확인했다

앞 절에서 그렇게 정했는데, 매도가 걸렸다. 매수는 08:30 에 `RiskManager` 가
다시 뽑으면 되지만 **매도 수량은 `ExitIntent` 가 정한다.**

`SwingStrategy.manage` 를 열어보니 두 경로 다 `position.quantity` 였다.
**전량 청산만 낸다.** 그러면 08:30 에 보유 수량을 그대로 쓰면 된다.

지금 전략이 그렇다고 규격이 그런 것은 아니다. `ExitIntent` 는 부분 청산을
표현할 수 있다. 그래서 19:00 에 검사한다 — `intent.quantity` 가 보유 수량과
다르면 계획에 담지 않고 `event_log` 에 ERROR 를 남긴다.

**조용히 전량 파는 것이 최악이다.** 부분 청산 전략이 생기면 여기가 먼저
걸리고, 그때 계획 저장 방식을 다시 정하면 된다.

### 계획 소비와 주문 기록을 한 커밋에 넣었다

`place_order` 에 `signal_id` 를 붙이고 `record_pending` 과 **같은
트랜잭션에서** `consume` 을 부른다.

나눠서 하면 그 사이에 죽었을 때 신호가 남아 있어 다음 폴링이 같은 주문을
또 낸다. `client_order_id` 는 부를 때마다 새로 만들어지므로 **UNIQUE 가
이것을 막지 못한다.**

2단계에서 "커밋이 이 코드의 존재 이유다" 라고 적었던 것이 그대로 한 겹
늘어났다. 커밋 하나가 '이 계획은 소비됐고 이 주문은 기록됐다' 를 함께
보장한다.

### 빈 캐시는 '어긋났다' 가 아니라 '모른다' 다

`position` 대조를 넣고 보니 **처음 켰을 때 무조건 불일치가 난다.** DB 가
비어 있고 증권사에 3종목이 있으면 3건이 어긋난 것으로 잡힌다.

`SCHEMA.md` 가 "증권사 잔고를 정본으로 삼는다, 이 테이블은 캐시다" 라고
정해뒀다. **빈 캐시는 오류가 아니라 아직 안 채워진 상태다.** 시작할 때만
`db_quantity == 0` 인 항목을 불일치에서 뺀다.

운영 중에 나는 불일치는 그대로 잡는다. 그때는 캐시가 채워져 있어야 하는데
안 맞는 것이므로 뜻이 다르다.

### 실패한 일을 그날 다시 하지 않기로 했다 (한계로 남긴다)

네 가지 일 모두 시작할 때 `done` 에 찍는다. 08:30 제출이 터지면 그날은
끝이다. 재시도하게 만들고 싶었는데 **`_plan` 이 멱등이 아니다.**

계획을 절반 남기고 터진 뒤 다시 돌면 같은 종목의 신호가 둘이 되고, 다음 날
아침에 주문이 두 번 나간다. `scan` 은 보유 종목만 거르지 이미 신호를 낸
종목을 거르지 않는다.

일마다 다르게 처리할 수도 있었다 — `_submit`·`_cancel`·`_snapshot` 은
멱등이다. 규칙이 셋으로 갈리는 값보다 하나로 도는 값이 크다고 봤다.

**숨기지 않고 체크리스트에 한계로 적었다.** 브로커 어댑터가 이미
`TransientError` 를 세 번 재시도하므로 일시 장애는 그 아래에서 걸러진다.
그래도 08:30 이 통째로 날아가는 경우가 실제로 생기면 `_submit` 부터 연다.

### realized_pnl 을 0 으로 적지 않았다

`daily_pnl` 에 당일 실현손익 자리가 있는데 채우지 못한다. 매도 체결마다
그 종목의 원가가 필요한데 `execution` 적재가 아직 없어 판 뒤에는 평단가가
사라진다.

**모르는 값을 0 으로 적으면 화면에서 '오늘 손익 0원' 으로 보인다.** NULL 은
모른다는 뜻이고 0 은 없었다는 뜻이라 다르다. `SCHEMA.md` 가 스윙의 주
지표를 `unrealized_pnl` 로 정해뒀으므로 지장이 없다.

`execution` 적재는 4단계 항목에 없었다. 슬리피지 실측에는 지장이 없다 —
`order_request.avg_fill_price` 가 체결가를 준다.

### 체결 추적을 틱마다 하지 않는다

루프 틱이 10초인데 `get_order_status` 는 목록 API 둘을 부른다. 주문 5건이면
틱마다 10회다. **api-id 별 유량이 초당 1건이라** (2026-08-31 실측) 바로 닿는다.

`fill_poll_sec` 60초를 따로 뒀다. 명령 폴링은 DB 라 10초여도 되지만 브로커
호출은 아니다. **같은 루프 안이라고 주기가 같아야 하는 것은 아니다.**

### 테스트를 둘로 나눴다

`_quantity_for` 처럼 DB 가 필요 없는 것은 목으로 로컬에서 돌고, 나머지는
서버에서만 돈다. 로컬 433건 통과 / 93건 skip 이다.

`engine_conn` 픽스처를 더했다. 전용 `process_name`·전용 전략 이름을 써서
진짜 엔진이 같은 DB 를 쓰고 있어도 안전하고, `position` 은 계좌를 공유하므로
**미리 떠놨다가 되돌린다.**

**서버에서 526건 전부 통과했다** (2026-08-31 밤). 로컬에서 skip 되던 26건이
처음 돌았고 실패가 없었다. 앞선 두 단계와 달리 **서버에서 새로 깨진 것이
없었다** — 2단계는 트랜잭션 가드에, 3단계는 autocommit 에 걸렸었다.

깨질 만한 자리를 미리 알고 있었던 것이 이유로 보인다. 그 둘이 남긴 교훈이
**"psycopg 는 SELECT 하나에도 트랜잭션을 연다"** 였고, 이번에는 `conn.cursor()`
로 읽은 뒤마다 `rollback()` 을 붙여두고 시작했다. 같은 데 세 번 넘어지지는
않았다.

### 곁가지

- `common/db/positions.py` 에 `invested_amount` 를 만들었다가 지웠다.
  `RiskManager._budget` 이 이미 같은 계산을 안에서 한다. 부르는 곳이 없는
  코드였다
- `config/portal.yaml` 에 `engine-swing` 을 등록했다. 없으면 기본 임계값
  26시간이 적용돼 **60초마다 신호를 남기는 프로세스가 죽어도 화면이
  멀쩡해 보인다**
- `engines/swing/__main__.py` 는 `is_active` 가 아닌 계좌면 시작하지 않는다.
  실계좌가 이 경로로 걸린다

## 2026-08-31 (10) — 5단계 자동매매 탭

포털에 **제어가 처음 붙었다.** 지금까지는 조회 전용이었고 쓰기는 키워드
병합 하나뿐이었다.

### 화면을 만들려다 엔진 결함 둘을 찾았다

제어를 붙이려고 보니 앞 단계에서 만든 것 둘이 어긋나 있었다.

**`Restart=always` 가 정지 명령을 무의미하게 만든다.** 엔진이 정지 명령을
받으면 0 으로 끝나는데, systemd 가 30초 뒤 되살린다. `on-failure` 로
바꿨다 — 크래시만 되살리고 정상 종료는 그대로 둔다.

`telegram` 유닛을 보고 `always` 를 그대로 가져온 것이 원인이다. **그
프로세스에는 정지 명령이 없다.** 같은 패턴이어도 전제가 다르면 값이
달라진다. 4단계에서 probe 의 재시도가 조회일 땐 맞고 주문일 땐 틀렸던 것과
같은 종류다.

**엔진이 `stock_filter` 를 안 읽고 있었다.** 4단계 체크리스트에 없었고
5단계에도 "자동매매 탭 — 제외·허용 목록" 이라고만 적혀 있었다. 화면만
만들면 **누르면 듣는 줄 알고 안 듣는 버튼**이 된다.

`block` 만 엔진이 지키게 했다. 계획(19:00)과 제출(08:30) 두 곳에서 본다 —
어젯밤 계획한 뒤 아침에 막을 수 있어야 한다.

**진입에만 쓴다.** 청산은 이 목록을 보지 않는다. 들고 있는 종목을 제외
목록에 넣었다고 팔지 못하면 갇힌다.

`allow`(화이트리스트)는 모드 전환 설정이 필요해 미뤘다. 넣을 수는 있게 두되
**화면이 '엔진 미반영' 이라고 적는다.** 안 되는 것을 숨기지 않는다.

### 만들 수 없는 엔드포인트를 문서에서 지웠다

`INTERFACES.md` 10장에 `POST /api/control/engine/{name}/start` 가 있었다.

**만들 수 없다.** 엔진이 죽어 있으면 `command` 를 폴링할 주체가 없다.
포털이 프로세스를 직접 띄우는 것은 "엔진과 포털은 DB 로만 통신한다"
(CLAUDE.md 8) 를 깬다. systemd 의 일이다.

문서에 남겨두면 다음 세션이 또 만들려 든다. 지우고 왜 없는지를 적었다.

정지도 같은 이유로 **API 만 두고 화면에 버튼을 두지 않았다.** 되살리려면
ssh 가 필요한 일방통행이라 휴대폰에서 누를 자리에 둘 것이 아니다.
매매를 멈추는 것은 `halt_entry` 가 이미 다 한다 — 새로 안 사고 청산만 돈다.

### 375px 에서 실제로 봤다

관측 대기에 "백테스트 표 휴대폰 폭 확인 — 열이 다섯이라 실제로 봐야 안다"
가 있었다. 이번에 함께 봤고, **다섯 열이면 마지막 열이 화면 밖으로 밀린다**
는 것이 확인됐다.

보유 종목 표를 처음에 다섯 열로 만들었더니 **청산 버튼이 잘렸다.**

| | 밀리는 것 | 문제인가 |
|---|---|---|
| 백테스트 표 | 샤프(숫자) | 스크롤하면 된다 |
| 보유 종목 | **청산 버튼** | 스크롤할 수 있다는 걸 모르면 못 누른다 |

**데이터가 밀리는 것과 조작이 밀리는 것은 다르다.** 셋으로 줄이고 수량·평단·
종가를 종목명 아래 dim 줄로 내렸다. 이 프로젝트가 이미 쓰던 방식이다.

최근 주문도 다섯이라 넷으로 줄였다 — 수량을 체결의 분모로 합쳤다.
`4/120` 이 '120주 중 40주' 보다 빨리 읽힌다.

'엔진 미반영' 이 좁은 칸에서 `엔진 미 / 반영` 으로 갈렸다. `.nowrap` 을
더했다.

**화면은 눈으로 봐야 안다.** 테스트는 문자열이 들어 있는지만 확인하지,
버튼이 화면 밖에 있는지는 모른다.

### 계좌 이름을 두 곳에 적지 않았다

포털이 `account_id`·`process_name`·`strategy` 를 `engine.yaml` 에서 읽는다.
`portal.yaml` 에는 표시 건수만 둔다.

처음엔 `portal.yaml` 에 다시 적으려 했는데, **두 곳이 어긋나면 화면이 남의
계좌를 보여준다.** 그 김에 `engine.yaml` 에 `strategy` 를 더하고 엔진
`__main__` 도 그 값으로 전략을 고르게 했다 (backtest CLI 와 같은 방식).

### 눌렀다와 엔진이 했다는 다른 것이다

화면에 '보낸 명령' 절을 뒀다. `event_log` 는 '포털이 명령을 넣었다' 를,
`command.status` 는 '엔진이 처리했다' 를 기록한다.

둘 중 하나만 보면 안 된다. 엔진이 멈춰 있으면 명령은 `pending` 으로 남는데,
`event_log` 만 보면 눌린 것이 처리된 것처럼 보인다.

### 전량청산을 접어뒀다

`INTERFACES.md` 10.2 가 확인 토큰(`LIQUIDATE`)을 요구한다. 화면에서는
`<details>` 안에 넣어 **열고 토큰을 쳐야** 눌리게 했다. 두 동작이 필요하다.

진입차단에는 토큰을 요구하지 않는다. **안전한 쪽으로 가는 조작이라 우발적으로
눌려도 손해가 없다.** 위험한 것과 안전한 것에 같은 마찰을 주면 사람이
둘 다 무시한다.

## 2026-09-01 — 장중 실측. 절반은 얻고 절반은 결함이 막았다

예약 실행이 17단계를 돌았다. **다섯을 실측으로 닫았고, 못 잰 둘은 원인이
내 코드에 있었다.**

### 얻은 것 다섯

| | |
|---|---|
| `ord_no` | 성공 응답의 주문번호 필드가 맞다. 1단계 마지막 미확정 항목 |
| 매도 경로 | `kt10001` 로 시장가 매도 체결 |
| **세금 0.1998%** | `limits.yaml` 의 `tax_rate` 0.0020 과 일치 |
| `ord_stt` | **부분체결에서도 `"체결"`** 이었다 |
| 한 주문 = 한 행 | 미체결·체결 목록에 각각 하나씩 |

**`ord_stt` 를 안 믿기로 한 판단이 실측으로 뒷받침됐다.** 서원 1766주 중
1326주만 체결된 상태에서 `ord_stt` 가 `"체결"` 로 왔다. 2026-08-31 에
"어휘를 다 모르는 채로 문자열을 분기하면 조용히 틀린다" 고 적고 수량으로
끌어내게 해뒀는데, 그게 아니었으면 부분체결을 완전체결로 읽었다.

`get_order_status` 가 `partial / 1326 / 1130` 과 `filled / 1 / 260250` 을
둘 다 맞게 읽었다. 파서가 실전 응답에서 확인됐다.

### 못 잰 것 둘, 둘 다 내 결함이었다

**슬리피지 — 모의투자는 동시호가에 주문을 안 받는다.**

```
08:28 시장가 → [2000](RC4057:모의투자 장시작전)
09:10 지정가 → 접수
```

이건 내 코드 결함이 아니라 키움 쪽 사실인데, **엔진 시간표의 전제를
무너뜨린다.** 2026-08-30 에 "시가로 체결되려면 08:30~09:00 동시호가에 넣어야
한다" 고 정했는데 모의투자에서는 그 시각에 주문 자체가 안 된다.

Phase 8 이 모의투자 검증이므로 시간표를 09:00 이후로 옮겨야 한다. 그러면
백테스트의 '다음 날 시가 체결' 가정과 벌어지는데, **그 벌어짐이 곧 재려던
슬리피지다.** 값을 못 재게 된 것이 아니라 재는 방법이 바뀐 것이다.

**15:30 취소 — `_sleep_until` 이 한 시간마다 그냥 진행했다.**

`min(wait, max_wait_sec)` 로 한 번에 최대 한 시간만 자게 해놓고 **깨어난 뒤
목표 시각에 도달했는지 다시 안 봤다.** 15:25 단계가 11:15 에, 15:31 취소가
12:19 에 돌았다. 실험이 통째로 무의미해졌다.

상한을 둔 이유(시계가 어긋났을 때 하루를 잃지 않는다)는 옳았는데, **상한을
'한 번 자는 시간' 이 아니라 '전체 기다림' 으로 구현했다.** 도달할 때까지
다시 재도록 고쳤고, 잔 만큼 흐르는 시계로 테스트했다.

멈춘 시계로는 이 버그를 못 잡는다. 처음 쓴 테스트가 `now()` 를 상수로
고정해서 통과했었다.

### 진짜 문제는 따로 있었다 — 일봉이 이틀째 안 쌓였다

하한가 주문이 `기준가가 넘어가지 않았다` 로 두 번 다 건너뛰어졌다. 대조
로직을 의심했는데 **로직은 맞았고 데이터가 낡아 있었다.**

```
price_daily 최신 = 2026-08-28
API base_pric    = 260,000  (08-31 종가)
```

`daily` 수집기가 `exit_code 1` 로 죽고 있었고, 첫 `ExecStart` 라 뒤따르는
지표·국면까지 함께 멈췄다.

원인은 **두 수집기의 충돌**이었다.

```
16:10  stock_flags  →  stock_status 에 valid_from = 오늘 로 새 구간을 연다
19:00  daily        →  stock_status.main(어제) → MAX(valid_from) 이 오늘
                       → '시간을 되돌리지 않는다' 가드 → return 2 → failed
```

**KRX 는 D일 데이터를 D+1 에 공개한다.** 그래서 `daily` 는 늘 *어제*를
채운다. 도장이 오늘로 찍혀 있으면 영영 어제로 못 돌아간다.

가드 자체는 정당하다 — 과거 날짜로 돌리면 그 뒤에 상장한 종목이 폐지로
잡힌다. 순서 문제라 **순서로 풀었다.** `stock_flags` 를 kiwoom 유닛(16:10)
에서 daily 유닛(19:00) 맨 끝으로 옮겼다. 가드를 약하게 만들지 않고 스키마도
안 바꾼다.

### 대비를 해뒀는데 불완전했다

kiwoom 유닛에 이런 주석이 있었다.

> 상태 이력을 쓰므로 종목 마스터(19시)와 같은 날 안에서 순서가 엇갈려도
> 되도록 **제자리 수정 경로를 둔다**

순서 충돌을 예상하고 대비까지 해뒀다. 그런데 `stock_flags` 는 플래그가
바뀌면 제자리 수정이 아니라 **구간을 닫고 새로 연다.** 그때 날짜가 올라간다.
대비한 경로와 실제로 타는 경로가 달랐다.

`ROADMAP.md` 에 "자동 적재는 아직 확인 못 했다. 타이머 첫 발화가 08-31
16:10 이다" 라고 적혀 있었다. **첫 발화가 곧 첫 고장이었다.** 관측 대기로
적어둔 항목이 실제로 터진 셈이라, 그 목록을 가볍게 보면 안 된다.

### 엔진의 일봉 확인이 옳았다

4단계에서 "일봉 적재 여부는 `price_daily` 를 직접 본다. heartbeat 는
프로세스가 돌았다는 뜻이지 데이터가 들어왔다는 뜻이 아니다" 라고 정했다.

**오늘이 정확히 그 상황이었다.** `indicators`·`regime` 은 08-31 에 exit 0
으로 정상 종료했지만 08-28 데이터로 계산한 것이다. heartbeat 만 봤으면
멀쩡해 보인다.

### 복구

08-31 은 `price_daily` 를 직접 불러 손으로 채웠다 (2,848건, 26건 건너뜀).
도장이 이미 09-01 에 찍혀 있어 자동 경로로는 못 돌아간다.

09-01 치는 09-02 19:00 에 자동으로 들어온다 — 그때 `MAX(valid_from)` 이
09-01 이고 `09-01 > 09-01` 이 거짓이라 가드를 통과한다. 그 뒤로는 플래그가
일봉 뒤에 찍히므로 항상 하루 여유가 생긴다.

건너뛴 26건은 `stock` 에 없는 종목이다 (ETN·신주인수권 계열로 보인다).
마스터에 들어오면 이후는 정상이고, 08-31 봉만 계속 빈다. 유니버스가 거래대금
상위 200종목이라 실질 영향이 없고, `universe_at` 이 그날 거래된 종목만
뽑으므로 조용히 틀리지 않고 그냥 빠진다.

## 2026-09-01 (2) — 키워드가 이상했던 이유는 셋이었고 프롬프트는 그중 가장 작았다

'AI'(39회)와 '금'(26회)이 상위라 이상하다는 지적에서 시작했다. 사용자가
가진 추출 프롬프트로 바꿀 수 있는지 물었는데, **원인의 대부분이 프롬프트
밖에 있었다.**

### 1. 화면이 절대 빈도로 줄 세우고 있었다

`daily_ranked` 가 `ORDER BY mention_count DESC` 였다. 그런데 스키마에는
이런 인덱스가 있다.

```sql
CREATE INDEX idx_keyword_surge ON keyword_daily(trade_date, surge_ratio DESC);
```

**급등도 정렬을 위해 만든 인덱스인데 쓰는 곳이 없었다.** 설계 의도와 구현이
갈려 있었다. `SCHEMA.md` 는 "절대 빈도가 아니라 surge_ratio 가 신호다" 라고
못박고 있고, 화면 템플릿 주석에도 "배수가 급등 판단의 핵심이다" 라고 적혀
있다. 정렬만 어긋났다.

그날 숫자를 보면 차이가 분명하다.

| 키워드 | 건수 | 배수 |
|---|---|---|
| AI | 39 | 2.97 |
| 금 | 26 | 2.02 |
| 폴더블 | 9 | **63.00** |
| 플랫폼 | 7 | **49.00** |
| PCB | 5 | **35.00** |

**늘 많이 나오는 말은 배수가 안 움직인다.** 그게 정보가 없다는 뜻이다.

### 처음 나온 말을 어떻게 줄 세우나

`ma7` 이 0 이면 `surge_ratio` 가 NULL 이다. `NULLS LAST` 로 밀면 오늘 처음
나온 새 테마가 목록 맨 아래로 가고, 상한이 60줄이라 아예 잘린다.

`COALESCE(surge_ratio, mention_count)` 로 풀었다. **0회에서 10회가 된 것은
10배 급등과 크기가 비슷하다.** 한 번 나온 신규는 1배 취급이라 자연히
아래로 간다. 임계값을 DB 계층으로 내리지 않아도 된다.

### 2. 키워드 매처에 낱말 경계 검사가 없었다

```python
for term, keyword_id in self.keywords.items():
    if term in rest:      # 그냥 부분문자열
```

바로 위 `match_stocks` 는 `_appears()` 로 경계를 본다. 주석까지 있다 —
"'펩타이드' 의 '타이드' 를 종목으로 세지 않으려는 것이다".

**같은 위험을 한쪽만 막고 있었다.** 실제로 `본느, 신주발행금지 가처분` 의
'금' 이 잡혔다.

지우는 것도 경계를 지키게 했다. 그냥 `replace` 하면 `신주발행금지` 가
`신주발행 지` 가 되어 **LLM 이 망가진 문장을 본다.** 잔여 텍스트가 LLM 에
그대로 가는 구조라 이쪽이 더 위험했다.

### 경계 검사로는 부족했다

고치고 나서 재보니 `금리` 가 여전히 걸렸다. **'금' 이 맨 앞이라 왼쪽 경계가
깨끗하다.** 오른쪽은 볼 수 없다 — `반도체가` 의 조사와 구분이 안 되기
때문이다. 기존 주석에 그 이유가 이미 적혀 있었다.

**한 글자 한글 키워드는 구조적으로 못 고친다.** 종목명에 `min_name_length: 3`
을 둔 것과 같은 이유인데, 키워드에는 최대 길이(30자)만 있고 최소가 없었다.

`min_term_length: 2` 를 두고 **분석기가 사전을 받을 때 거르게** 했다.
이미 DB 에 들어간 '금' 도 지우지 않고 멈춘다. 데이터를 건드리지 않고
동작을 막는 쪽이 되돌리기 쉽다.

### 3. 프롬프트 — 셋 중 가장 작았다

"좁은 표현을 우선한다" 가 모호했다. **"넓은 말과 좁은 말이 같이 나오면 좁은
것만 뽑는다"** 로 바꾸고 예시를 넣었다. 한 글자 금지와 일반어 금지도 넣었다.

**왜 그런지도 프롬프트에 적었다.** 키워드가 배수로 쓰인다는 것을 모르면
모델이 규칙을 기계적으로만 따른다.

### 사용자 프롬프트를 그대로는 못 쓴 이유

받은 프롬프트는 좋은 브리핑 프롬프트인데 **만드는 물건이 달랐다.**

- 글 번호가 없다 → `keyword_mention(message_id, keyword_id)` 를 못 만든다
- 출력이 자유 서술이다 → JSON 스키마 강제와 안 맞는다
- "언급 횟수 정렬" 을 LLM 에게 시킨다 → **DB 가 이미 정확히 세고 있다.**
  게다가 절대 빈도는 신호가 아니라는 원칙과 정면으로 부딪힌다
- "최근 4일" 이 배치에 없다 → 없는 것을 요구하면 모델이 지어낸다

**정확한 것을 부정확한 것으로 바꾸는 셈이었다.** 대신 텔레그램 일일
브리핑으로 살리기로 했다 — 숫자는 DB 가 주고 LLM 은 해석만 한다.

### 남은 것

이미 쌓인 `keyword_mention` 의 '금' 행은 그대로다. 매처가 안 잡으니 앞으로는
안 늘지만, 최근 3일치 집계에는 남아 있다 (`rebuild_days: 3`).
지울지는 사용자가 정한다.

## 2026-09-02 — 슬리피지를 쟀고, 엔진을 막는 결함을 찾았다

두 번째 실측 실험. **슬리피지를 잡았고**, 그 과정에서 **엔진 기동을 막는
결함**이 드러났다.

### 슬리피지 — Phase 8 최대 산출물

| | 체결가 | 그날 시가(252,000) 대비 |
|---|---|---|
| 09:00:00 시장가 | 252,000 | **0.000%** |
| 09:02:00 시장가 | 252,750 | **+0.298%** |

**개장 정각 시장가가 시가에 그대로 체결됐다.** 동시호가가 막혀(`RC4057`)
시간표를 어떻게 할지 고민이었는데, **09:00 정각이면 백테스트의 '다음 날
시가 체결' 가정이 거의 그대로 성립한다.**

`limits.yaml` 의 임시값 0.1% 는 정각이면 과대, 2분 뒤면 과소다.
**2분이 0.3% 다.** 제출 시각이 값을 정한다.

단정하지 않는다. **표본이 하루 한 건이고 삼성전자 1주다.** 모의투자
체결 시뮬레이터가 시가로 채워준 것일 수도 있다. 며칠 더 재야 한다.

### 토큰이 만료 전에 죽는다 — 엔진 기동을 막는 결함

16:12 와 16:13 단계가 이렇게 죽었다.

```
PermanentError: 인증에 실패했습니다[8005:Token이 유효하지 않습니다]
```

08:55 에 받은 토큰이 7시간 17분 만에 죽었다. `INTERFACES.md` 에는 "약
24시간" 이라고 적혀 있다.

시각이 답이었다. **`stock-portal-kiwoom.timer` 가 평일 16:10 에 돈다.**

```
16:10  키움 수집 유닛 발화 -> 같은 앱키로 새 토큰 발급
16:12  실험의 호출 -> 8005
```

`collect.yaml` 을 보니 시세 수집기가 **전부 `use_paper: true`** 다.
"실전 앱키가 없다. 시세 조회는 계좌와 무관하므로 모의로 받는다" 는 주석이
붙어 있다. 엔진도 모의투자 계좌라 **모두가 앱키 하나를 공유한다.**

**키움은 앱키당 토큰 하나만 유효하게 한다.** 만료가 아니라 새 발급이
밀어낸 것이다.

### 왜 엔진을 막는가

엔진은 24시간 상주하며 토큰을 캐시한다.

- 매 평일 **16:10** 키움 유닛이 토큰을 죽인다
- **19:00** daily 유닛의 지표 수집이 또 죽인다
- 8005 는 `return_code 3` 이라 `PermanentError` 였다 — **재시도하지 않는다**

**띄웠으면 매일 오후부터 마비됐다.** 19:00 계획도 실패한다. 실험이 아니었으면
엔진을 띄우고 나서야 알았을 것이다.

### 고친 방법

`return_code = 3` 을 `TransientError` 로 분류하고 **캐시한 토큰을 버린다.**

- 조회는 `_call` 의 재시도로 **스스로 낫는다** — 새 토큰을 받아 다시 건다
- 주문은 `_call_once` 라 재시도하지 않는다. 예외가 올라가고 호출부가 판단한다
  (CLAUDE.md 3)

**`check_return_code` 와 무관하게 버린다.** 주문 경로는 거부 사유를
`OrderResult` 에 담으려고 예외를 끄고 부르는데, 거기서 안 버리면 죽은
토큰을 계속 들고 간다.

**메시지의 `8005` 를 파싱하지 않았다.** "한 건 본 것으로 형식을 단정하지
않는다" 는 2026-08-31 의 결정을 따랐다. 앱키가 진짜로 틀렸으면 토큰 재발급이
스스로 실패해 시끄럽게 드러난다.

### 하한가 대조가 구조적으로 틀렸다 (아직 안 고침)

하한가 주문이 **세 번째로** `기준가가 넘어가지 않았다` 로 건너뛰었다.
이제 왜인지 안다.

**KRX 는 D일 데이터를 D+1 에 공개한다.** 그래서 `daily` 는 늘 어제를
채우고, **아침에 `price_daily` 최신은 항상 '그저께'** 다. API 기준가는
'어제 종가' 다. **영원히 안 맞는다.**

대조는 원래 "장 전에 읽으면 지난 장 값이 나온다" 를 막으려던 것인데,
09:06 은 **이미 장중**이라 애초에 필요 없었다. 장이 열린 뒤면 `lst_pric`
이 오늘 값이다.

그래서 15:30 취소 실험이 또 못 돌았다.

### 일봉 수집은 정상화됐다

```
('daily', 'idle', 2026-09-02 19:01)
price_daily 최신 = 2026-09-01
```

어제 `stock_flags` 를 daily 뒤로 옮긴 것이 실제로 들어맞았다.

## 2026-09-03 — 실측이 끝났다. 시간표가 둘 다 틀렸었다

세 번째 실험. **재려던 셋을 다 닫았고**, 그 결과로 **엔진 시간표 두 값이
전부 틀렸다는 것**이 드러났다.

### 슬리피지 — 표본 둘이 같은 말을 한다

| | 시가 | 09:00 정각 | 09:02 |
|---|---|---|---|
| 09-02 | 252,000 | 252,000 (**0.000%**) | 252,750 (+0.298%) |
| 09-03 | 254,000 | 254,000 (**0.000%**) | 254,500 (+0.197%) |

**09:00 정각이 이틀 연속 시가와 정확히 같다.** 우연이 아니다 — 모의투자
체결 시뮬레이터가 09:00 주문을 시가로 처리하는 것으로 보인다.

**그래서 `slippage_rate` 를 0 으로 내리지 않았다.** 실전은 09:00:00.001 에
들어간 주문이 그 시점 호가에 체결되므로 시가와 미세하게 다를 것이다.
**모의투자로는 여기까지가 한계다.** 0.1% 는 0 과 0.3% 사이의 보수적인 값이라
그대로 둔다. Phase 9 실계좌에서 다시 잰다.

쓸 수 있는 것은 **2분이 0.2~0.3%** 라는 것이다. 값이 아니라 **제출 시각이
중요하다**는 사실이 이번 실측의 알맹이다.

### 시간표 두 값이 다 틀렸었다

`submit_time` 은 09-01 에 이미 알았다 — 모의투자가 동시호가 주문을 안 받는다
(`RC4057`). **08:30 → 09:00.** 실측이 이 선택을 뒷받침했다.

`cancel_time` 은 이번에 드러났다.

```
09:15 취소 -> 모의투자 취소주문완료   (return_code 0)
15:31 취소 -> [2000](RC4058:모의투자 장종료)
15:45 취소 -> 장종료
16:10 취소 -> 장종료
```

**15:30 이후에는 취소가 안 된다.** 장중에는 되는데 마감 뒤에는 막힌다.
`cancel_time: "15:30"` 은 **정확히 안 되는 시각**이었다.

15:20 부터 장 마감 동시호가라 **15:10** 으로 옮겼다. 백테스트는 어차피
시가에만 체결하므로 20분 일찍 버려도 결과가 같다.

시간표를 정한 것이 2026-08-30 인데, **두 값 다 실측 없이 정한 것이었고
둘 다 틀렸다.** 엔진을 띄웠으면 아침에 주문이 안 나가고 저녁에 취소가
안 됐을 것이다.

### 취소된 주문이 사라진다 — 엔진이 기동조차 못 했을 것이다

8월 31일에 "아직 모르는 것" 으로 남겨둔 항목의 답이 나왔다.

```
09:15 취소 성공
09:20 미체결 목록 -> []
     get_order_status -> PermanentError: 모르는 주문번호입니다
```

**어느 목록에도 안 남는다.** 체결 목록(`ka10076`)은 당일 것만 주므로
**날이 바뀐 주문도 마찬가지다.**

문제는 재시작 복구였다.

```python
def run(self) -> int:
    self.recover()        # <- try 밖이다
    while not self.stopping:
        try:
            self.tick(...)
```

`_reconcile_orders` 가 이 예외를 그대로 올려보내고, `recover()` 는 `run()` 의
예외 처리 밖이다. **엔진이 시작하자마자 죽고** systemd 가 다섯 번 시도한 뒤
포기한다. 어제 `submitted` 로 남은 주문 하나면 다음 날 아침부터 영영 못
뜬다.

`cancelled` 로 닫고 넘어가게 했다. 실제로 체결됐을 가능성이 남지만
**보유 수량은 브로커 잔고가 정본이다** (`SCHEMA.md` 5장) — `_sync_positions`
가 바로 뒤에 맞추고 어긋나면 진입을 막는다. **주문 행 하나 때문에 엔진을
못 띄우는 것보다 낫다.**

`filled_qty` 는 `apply_result` 의 `GREATEST` 가 지킨다. 부분체결된 주문이
사라져도 체결 기록을 잃지 않는다. 2026-08-31 에 "문서로 막던 것을 SQL 로
막았다" 고 적은 그 장치가 여기서 값을 했다.

### 덤 — `ord_stt` 어휘 하나가 채워졌다

미체결 상태가 **`"접수"`** 로 왔다. 부분체결·완전체결이 `"체결"` 이므로
어휘로는 둘만 구분된다. 수량으로 판정하기로 한 결정은 그대로 옳다.

### 실측 사흘이 남긴 것

엔진을 띄우기 전에 **결함 다섯**을 잡았다.

| 날 | 잡은 것 |
|---|---|
| 09-01 | 일봉 수집 순서 충돌 (이틀 유실, 매 평일 반복될 뻔) |
| 09-01 | `_sleep_until` 이 한 시간마다 그냥 진행 |
| 09-02 | 토큰이 매일 16:10 에 죽음 (엔진이 오후마다 마비될 뻔) |
| 09-03 | `submit_time`·`cancel_time` 둘 다 안 되는 시각 |
| 09-03 | 사라진 주문에 걸려 엔진이 기동조차 못 함 |

**넷은 엔진을 띄웠어야 알 수 있는 것들이었다.** 하루 세 번 실측을 돌린
값이 여기 있다.

## 2026-09-03 (2) — 같은 가정이 세 번째로 물었다

엔진을 띄우려다 **띄워도 아무것도 안 한다**는 것을 발견했다.

```python
return spanned[1] == day          # day = 오늘
```

`_daily_loaded` 가 오늘 일봉을 요구하는데 **오늘 일봉은 없다.** KRX 가 D일
데이터를 D+1 에 공개하므로 19:00 수집기가 채우는 것은 어제 것이다.
09-03 19:00 수집 뒤에도 최신은 09-02 였다.

`LiveFeed.trade_date()` 도 같았다. 오늘을 커서로 쓰니 `universe_at` 이 그날
거래된 종목을 요구해 늘 빈 목록을 준다. **진입 후보가 영영 0 이다.**

### 뿌리는 8월 30일의 한 문장이다

> 일봉 수집기가 19:00 에 도니 **그 전에는** 그날 종가가 DB 에 없다

"그 뒤에는 있다" 를 전제했다. **없다.** 이 가정이 이번 주에 세 번 물었다.

| | 어디서 |
|---|---|
| 09-01 | 시험주문 기준가 대조 — 하한가 주문이 안 나감 |
| 09-02 | 같은 자리에서 또 |
| 09-03 | 엔진 `_daily_loaded` 와 `LiveFeed.trade_date` |

**한 번 틀린 가정은 여러 곳에 같은 모양으로 박혀 있다.** 처음 물었을 때
"왜 안 맞나" 만 고치고 "이 가정이 또 어디 있나" 를 안 찾은 것이 잘못이다.

### 고친 방법

**커서일을 데이터가 정하게 했다.** `trade_date()` 가 `MAX(price_daily.
trade_date)` 를 준다. 오늘 날짜를 안 본다.

`_daily_loaded` 는 '오늘 것이 있나' 대신 **'며칠 묵었나'** 를 본다.
정상은 하루, 월요일이면 금요일 종가라 사흘이다. `plan_max_stale_days: 5`
는 수집기가 며칠 멈춘 것을 잡는 값이지 정상 지연을 잡는 값이 아니다.

**캐시를 뒀다.** 한 번 스캔에 200종목을 도는데 종목마다 `MAX(trade_date)`
를 부르면 인덱스 없는 220만 행을 200번 훑는다. TTL 60초다.

하루 한 번 캐시하면 안 된다 — 09:00 제출 때 잡은 값이 19:00 판단 때는
낡는다. 그 사이 수집기가 새 거래일을 넣기 때문이다. **캐시 수명을 정할 때
'무엇이 값을 바꾸는가' 를 봐야 한다.**

### 남는 갈라짐은 기록했다

| | 판단 근거 | 체결 |
|---|---|---|
| 백테스트 | D 종가 | D+1 시가 |
| 실전 | **D-1 종가** | D+1 시가 |

**하루가 더 뜬다.** 데이터가 하루 늦게 오는 한 지금 구조로는 못 좁힌다.
Phase 8 은 틀 검증이라 그대로 가고, **Phase 9 전에 푼다.**

유력한 길은 **KRX 가 전날 데이터를 아침 몇 시에 공개하는지 재는 것**이다.
08:00 에 이미 있으면 아침 수집 + 08:30 판단으로 백테스트와 정확히 맞는다.
관측 대기에 올렸다.

## 2026-09-03 (3) — 엔진을 띄웠고, 띄우자마자 결함이 하나 나왔다

20:57 에 기동했다. 첫 로그가 이랬다.

```
20:57:19  engine-swing 시작. 계좌 paper
20:57:27  계획: 청산 1건, 진입 1건 (국면 neutral)
```

**엔진이 스스로 전략을 돌려 판단을 마쳤다.** 스냅샷도 찍고 포지션 3종목도
인식했다. Phase 8 의 틀이 실제로 도는 것을 처음 봤다.

### 그런데 재시작하니 계획을 장 외 시간에 던졌다

21:03 에 로그 버퍼 설정을 적용하려고 재시작했더니 이랬다.

```
21:03:03  SELL KRX:005930 4주 -> rejected
21:03:03  BUY  KRX:138040 7주 -> rejected
21:03:10  계획: 청산 1건, 진입 1건
```

`done` 이 프로세스 메모리에만 있어 재시작하면 빈다. `due_tasks` 가
"09:00 제출을 아직 안 했다" 고 판단해 **21:03 에 제출했다.** 장종료로
거부되며 **계획 두 건이 소진됐다.**

### 내가 쓴 docstring 이 이미 답을 적어놨었다

> '정각에 깨운다' 가 아니라 '지났는데 안 했으면 한다' 이다. **15:00 에
> 올라온 프로세스가 08:30 제출을 건너뛰고** 15:30 취소부터 하는 것이 옳다

**의도는 건너뛰는 것인데 코드는 다 실행했다.** 창(window)이 없었다.
docstring 이 코드보다 정확했던 셈이다.

창을 30분 뒀다. 이제 15:00 에 올라오면 제출 창(09:00~09:30)이 지나
건너뛰고 취소만 한다. **다시 보기(`retry_at`)에는 창을 적용하지 않는다** —
일봉을 두 시간까지 기다리는데 30분으로 자르면 그 기다림이 무의미해진다.

### 창만으로는 부족했다

창 안(19:00~19:30)에 재시작하면 `_plan` 이 다시 돌아 **같은 종목의 신호가
둘이 된다.** 다음 날 아침에 주문이 두 번 나간다.

`_planned_today` 를 DB 로 본다. `done` 은 메모리라 못 믿는다.

**메모리에 있는 상태는 재시작을 못 견딘다.** 그걸 알면서도 `done` 을
메모리에 뒀고, 창과 DB 확인 둘 다 필요했다. 계획을 `signal` 테이블에
남기기로 한 결정(2026-08-31)은 같은 이유였는데, 시간표 상태에는 그 판단을
적용하지 않았다.

### 로그가 통째로 안 보였다

`StandardError=append:파일` 은 파이프가 아니라 파일이라 Python 이 블록
버퍼로 잡는다. **24시간 도는 프로세스는 종료할 일이 없어 flush 가 안 된다.**
배치(oneshot)는 끝날 때 flush 되므로 안 겪는다.

프로세스는 멀쩡히 돌고 heartbeat 도 찍히는데 `tail` 이 빈 파일을 보여줬다.
`PYTHONUNBUFFERED=1` 을 swing 과 news 유닛에 넣었다.

**상주 프로세스와 배치는 같은 로깅 설정을 공유할 수 없다.**
