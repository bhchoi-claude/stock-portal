# checklist.md — Phase 6 백테스트 프레임워크

> **다음 할 일: Phase 6 마무리 확인.** 1~7단계를 다 만들었다.
>
> **서버에서 화면을 확인해야 한다.** 6단계 적재는 2026-08-30 에 확인했다.
>
> **전략 내용은 만들지 않는다.** 틀만 만들고 더미 전략으로 완주를 확인한다.
> 전략은 Phase 7 에서 별도로 논의한다.
> Phase 4·5 체크리스트는 커밋 `b0b782f` 이전 이력에 있다.

## 지금까지 만든 것 (Phase 6)

| 파일 | 무엇 |
|---|---|
| `common/feed/base.py` | `DataFeed` 규격 |
| `common/feed/backtest.py` | 커서 이후를 안 보여주는 피드 |
| `common/strategy/base.py` | `Context`·`EntryIntent`·`ExitIntent`·`Strategy` |
| `common/strategy/dummy.py` | 틀 검증용 더미 전략 |
| `common/risk.py` | 한도 다섯을 강제하는 `RiskManager` |
| `backtest/execution.py` | 다음 봉 시가 체결, 수수료·세금·슬리피지 |
| `backtest/market.py` | 체결측 조회 경계 (`Market`·`DbMarket`) |
| `backtest/portfolio.py` | 현금·보유 장부 |
| `backtest/loop.py` | 하루 단위 루프 |
| `backtest/__main__.py` | CLI |
| `backtest/metrics.py` | FIFO 짝짓기, 지표 5종, 생존편향 문구 |
| `common/db/backtest.py` | `backtest_run`·`backtest_trade` 적재와 조회 |
| `portal/` | 운영·로그 탭의 백테스트 표 |

남은 것은 서버 확인뿐이다.

---

## 시작 전에 정한 것 (2026-08-30)

- **체결은 다음 봉 시가.** 종가로 신호를 계산하고 다음 거래일 시가에 체결한다.
  미래 참조가 구조적으로 불가능해진다 (승인 사항)
- **생존편향 경고를 결과에 붙인다.** `backtest_run.note` 에 자동으로 적어
  숫자를 볼 때마다 함께 보이게 한다 (승인 사항)
- **스윙(일봉)만 한다.** 분봉이 나흘치뿐이다. 단타는 Phase 10
- 유니버스는 최소한으로 — 해당 시점 상장 + 관리종목·거래정지 제외 +
  거래대금 상위 N. 확정은 Phase 7 (`config/universe.yaml`)

## 1단계 — DataFeed

- [x] `DataFeed` ABC — `common/feed/base.py` (2026-08-30)
- [x] `BacktestFeed` — 커서 시각이 `now()` 다. 커서는 그날 장 마감
- [x] **`now()` 이후 데이터를 반환하지 않는 단위 테스트** (CLAUDE.md 필수)
- [x] `get_candles` 는 조정가를, `get_quote` 는 원주가를 준다 (3.3)
- [x] `get_universe` 는 `stock_status` 의 **해당 시점 값**을 읽는다 (3.4)
- [x] 국면 이력이 없는 구간은 중립으로 본다. 판정을 지어내지 않는다
- [x] `Signal` 타입 추가 (`common/types.py`). `get_signals` 는 Phase 8 까지 빈 목록
- [x] 서버에서 DB 테스트 통과 확인 — 365건 전부, 스킵 0건 (2026-08-30)

## 2단계 — Strategy 규격

- [x] `Context`, `EntryIntent`, `ExitIntent` 타입 (2026-08-30)
- [x] `Strategy` ABC — `scan`, `manage`, `on_start`, `on_day_end`
- [x] 더미 전략 — 매일 사서 다음 주기에 판다. **틀 검증용이다**
- [x] 전략은 의도만 반환한다. **`EntryIntent` 에 수량이 없다** (4.2)
- [x] 파라미터는 `config/strategy_dummy.yaml`. 코드에 숫자를 쓰지 않는다 (4.3)
- [x] 규격 테스트 6건 — DB 없이 스텁 피드로 돈다

## 3단계 — RiskManager

- [x] `RiskDecision`, `RiskManager.evaluate` (2026-08-30)
- [x] **한도 초과 시 거부하는 테스트** 12건 (CLAUDE.md 필수)
- [x] 확인 항목 다섯 — 국면 배분, 건당 최대, 종목당 비중, 보유 수, 일일 손실
- [x] 한도는 `config/limits.yaml`. 국면 배분은 **임시값** (승인 사항)
- [x] 국면 배분은 총 투입 한도라 이미 들고 있는 금액을 뺀다
- [x] 일일 손실 한도는 전략 밖에서 강제한다. `start_day()` 로 기준을 잡는다

## 4단계 — 체결 시뮬레이터

- [x] `Executor` — 다음 거래일 시가 체결 (2026-08-30)
- [x] 수수료 0.015% 는 양쪽, 증권거래세 0.20% 는 **매도에만**
- [x] 슬리피지는 사는 쪽에 비싸게, 파는 쪽에 싸게
- [x] 수수료·세금은 원 미만 절사. 체결금액은 원 단위 반올림
- [x] 모르는 시장은 가장 높은 세율. 낙관 쪽으로 틀리지 않는다
- [x] 금액은 전부 `Decimal` (CLAUDE.md 4)
- [x] 테스트 8건 — 왕복하면 가격이 그대로여도 손해라는 것까지 고정
- [x] 보유 중 상장폐지 → 정리매매 마지막 가격으로 청산 (5단계에서, 2026-08-30)

**호가단위는 모사하지 않는다.** 슬리피지를 곱한 단가가 실제 호가에 맞지
않을 수 있다. 백테스트 근사로 받아들인다.

## 5단계 — 루프와 CLI

- [x] `backtest/` 패키지 — `market`·`portfolio`·`loop`·`__main__` (2026-08-30)
- [x] `python -m backtest run --strategy dummy --from 2025-01-01 --to 2025-12-31`
- [x] 하루 단위 진행 — 휴장일 건너뛰기, `manage` 는 매일, `scan` 도 매일
- [x] 하루의 순서 — 시가 체결 → 커서 이동 → 폐지 청산 → 평가 → `manage` → `scan`
- [x] 체결측 조회를 `Market` 으로 분리. **피드는 다음 날 시가를 주지 않는다**
- [x] 매도를 매수보다 먼저 체결한다. 매도대금으로 당일 매수가 된다
- [x] 현금이 모자라면 수량을 줄인다. **음수 현금 = 없는 레버리지**
- [x] 일일 손실 한도의 기준은 전일 종가 평가액
- [x] 루프 테스트 11건 + 장부 테스트 4건 — DB 없이 돈다
- [x] **서버에서 실제 일봉으로 완주 확인** (2026-08-30)

2025-03 한 달을 돌린 결과다. 숫자가 아니라 **틀이 도는지**를 본 것이다.

| 항목 | 값 | 무엇을 확인했나 |
|---|---|---|
| 거래일 | 20일, 03-04 시작 | 3/1 토요일과 3/3 대체공휴일이 빠졌다 |
| 체결 | 122건 | 19일 × 5 = 95 보다 많다. 매도·매수가 겹친 날이 있었다 |
| 총수익률 | -0.74% | 왕복 비용 0.43% × 61회 ≈ 2.6% 를 시세가 덮었다 |

**더미의 수익률에는 뜻이 없다.** 판단 없이 매일 사고 파는 것이라 비용만 쌓인다.

## 6단계 — 결과 적재

- [x] `backtest_run` — 파라미터 전체와 `fee_rate`·`slippage_rate` 를 남긴다
      0 으로 돌린 결과도 그 사실이 남는다 (2026-08-30)
- [x] `backtest_trade` — 매매 하나하나. 적재는 실행과 **같은 트랜잭션**이다
- [x] 지표 — 총수익률, MDD, 승률, 거래수, 샤프
- [x] `note` 에 생존편향 경고 자동 첨부
- [x] **FIFO 짝짓기** (승인 사항). 분할 매수는 먼저 산 것부터 판 것으로 본다
- [x] **샤프는 무위험 0, 연 252거래일** (승인 사항). 관례이므로 `params` 에 남긴다
- [x] 미청산 포지션도 남긴다. 빼면 손익이 실제와 달라진다
- [x] 계산할 수 없는 지표는 `NULL`. **0 으로 적으면 '다 졌다' 로 읽힌다**
- [x] `Execution` 으로 체결에 사유·근거를 붙였다. `exit_reason` 이 없으면
      `backtest_trade` 를 채울 수 없다
- [x] 지표 테스트 15건 + 적재 테스트 7건
- [x] **서버에서 실제 적재 확인** — `run_id 7`, 매매 63건 (2026-08-30)
- [x] `note` 문구를 고쳤다. '6개 중 6개만' 은 뜻이 반대로 읽혔다

## 7단계 — 포털 화면

- [x] `GET /api/backtest/runs` (`INTERFACES.md` 10장에 이미 있다) (2026-08-30)
- [x] 실행 목록과 지표 비교 — 운영·로그 탭 안의 '백테스트' 섹션
- [x] **화면 위치는 운영·로그 안으로 정했다** (2026-08-30 확인).
      새 탭을 만들지 않았다. `PROJECT.md` 6장에 반영했다
- [x] 생존편향 경고를 지표 바로 아래 붙였다. 문서에만 적으면 잊는다
- [x] `ratio_plain` 필터 추가 — MDD 에 `+` 를 붙이면 오른 것처럼 읽힌다
- [x] 화면 테스트 6건
- [ ] **서버에서 화면 확인** — 휴대폰 폭 포함

## Phase 6 완료 기준

- [x] 더미 전략으로 백테스트가 완주한다 — 서버 실데이터로 확인 (2026-08-30)
- [x] 수수료·슬리피지 변경이 결과에 반영된다
- [x] 미래 참조 방지 테스트가 통과한다

---

## 사용자 조치 필요

- [ ] **수수료율 확인** — 키움 온라인 매매수수료율과 증권거래세율.
      임시값으로 시작하되 실제 값을 받으면 바꾼다. 값이 틀리면 결과가 통째로 틀린다

## 관측 대기 (Phase 4·5 에서 이월)

- [ ] 일봉·지표·국면 heartbeat — 매일 19:00
- [ ] 분봉·수급·플래그 heartbeat — 08-31 16:10 이 첫 발화
- [ ] 분봉 자동 적재 — 지금까지 쌓인 것은 손으로 돌린 결과다
- [ ] `stock_flags` 플래그 생존 — KRX 마스터가 돈 뒤에도 남는지
- [ ] Claude Design 결과물 연결 — 산출물이 나오면 템플릿만 바꾼다
- [ ] 실제 데이터로 키워드 병합을 한 번 해본다

## 받아들이고 닫은 것 (Phase 5)

| 항목 | 왜 |
|---|---|
| 쓰레기 키워드 (`NV`) | 버린다는 표시 자리가 스키마에 없다 (승인) |
| `제우스` 오탐 | 종목명이자 게임 이름. 규칙으로 못 가른다 (승인) |
| 언급의 무게 | 주인공과 스쳐 나온 종목이 같은 무게다 |
| 종목 약칭 | 사람이 사전을 만들어야 한다 |
| 급등 임계값 3.0 | 근거 없는 임시값. 일주일 뒤 조정 |

## 건드리지 않은 것

- 서버 `~/stock-portal/ion` — 정체불명 파일(398바이트, 8/25). 내 작업과 무관하다
- `docs/INTERFACES.md` 코드블록이 `ruff format` 기준에 걸린다. 기존부터 그랬다
