# checklist.md — Phase 8 스윙 엔진 (모의투자)

> **1단계 진행 중이다.** 조회 넷과 `submit_order` 가 끝났고 주문 조회·취소가
> 남았다. 시작 전에 정한 것들은 아래에 그대로 둔다.
>
> Phase 7 체크리스트는 커밋 `ec58462` 이전 이력에 있다.

## Phase 7 은 미완이다 (2026-08-30 중단)

전략 검증은 끝나지 않았다. **게이트를 Phase 8 앞에서 Phase 9(실전) 앞으로
옮겼다** (확인 사항). Phase 8 은 전부 프레임워크이고 모의투자는 돈이 나가지
않는다.

| 측정 (개발 구간 2023-08~2025-08) | 값 |
|---|---|
| `SwingStrategy` 총수익률 | **-34.38%** |
| 매매당 기대값 | -1.60% |

**`SwingStrategy` 를 실전에 쓰지 않는다.** 코드에 남긴 것은 엔진을 돌려볼
대상이 필요해서다. 더미보다 진짜에 가까워야 엔진 결함이 드러난다.

재개 지점은 `ROADMAP.md` Phase 7 과 `context-notes.md` 2026-08-30 (19) 에 있다.
**검증 구간(2025-08-29 ~ 2026-08-28)은 아직 한 번도 열지 않았다.**

---

## 시작 전에 정할 것

### 모의투자 계좌 (2026-08-30)

- [x] `config/accounts.yaml` 생성
- [x] DB `account` 적재 — `paper` 가 `is_paper=t`, `is_active=t`.
      실계좌 둘은 `is_active=f` 로 꺼져 있다
- [x] **모의투자 API 연결 확인** — `mockapi.kiwoom.com` 에 인증·시세 통과.
      Phase 2 에서 만든 계좌가 지금도 유효하다
- [x] **`.env` 의 `KIWOOM_ACCOUNT_PAPER`** — 넣었다 (2026-08-30).
      키움 화면 표기가 8자리이고 `.env` 길이도 8로 일치한다. 상품코드
      접미사는 이 계좌에 없다. 읽는 코드는 `submit_order` 를 만들 때 붙는다
- [x] **계좌 종류가 '상시모의투자' 다.** 기간제가 아니라 만료 위험이 없다.
      완료 기준인 '2주 무사고 운영' 중간에 끊길 걱정을 안 해도 된다

**시세가 된다고 주문이 되는 것은 아니다.** 아래가 남은 위험이다.

### 남은 것

- [x] 키움 **주문** API 스펙 조사 (2026-08-30). 규격은 나왔고 모의투자
      지원 여부는 **간접 근거만** 얻었다 (아래)
- [x] 조회 API 로 모의투자 동작을 먼저 검증한다 (1단계 순서를 바꿨다)
- [x] **모의투자에서 주문 API 가 동작한다** (2026-08-31 실측). 아래 참조

### 주문 API 규격 (2026-08-30 조사)

| 항목 | 값 |
|---|---|
| 엔드포인트 | `/api/dostk/ordr` |
| API ID | `kt10000` 매수, `kt10001` 매도 |
| 요청 바디 | `dmst_stex_tp`, `stk_cd`, `ord_qty`, `ord_uv`, `trde_tp` |
| `trde_tp` | `0` 보통(지정가), `3` 시장가 |

**요청 바디에 계좌번호가 없다.** 계좌는 앱키·토큰에 묶인다. 모의투자는
별도 앱키를 쓰고 도메인이 `mockapi.kiwoom.com` 이므로 그 키가 곧 모의투자
계좌를 가리킨다.

→ **`KIWOOM_ACCOUNT_PAPER` 는 주문 요청에 안 들어갈 가능성이 크다.**
DB 대조·로그 기록용으로만 쓰게 될 것 같다. 구현할 때 확정한다.

### 모의투자 주문 지원 — 실측으로 확인됐다 (2026-08-31)

**간접 근거뿐이던 것이 풀렸다.** `/api/dostk/ordr` 에 빈 바디를 던지자
키움이 필수 파라미터를 알려줬다. 필드를 하나씩 올려 다섯을 확정했고,
완전한 바디는 **필드 검증을 통과해 매매 엔진까지 도달했다.**

```
return_code 20  [2000](RC4058:모의투자 장종료)
```

장 외 시간이라 막힌 것이지 API 가 없어서가 아니다. 매수·매도 둘 다 같다.

| 실측 항목 | 결과 |
|---|---|
| 필수 필드 순서 | `dmst_stex_tp` → `stk_cd` → `ord_qty` |
| 시장가에 `ord_uv` | **필요 없다.** 생략해도, 빈 문자열이어도 통과 |
| 매도 `kt10001` | 매수와 바디가 같다 |
| 거부 `return_code` | 입력 오류 `2`, 장종료 `20` |

**주문 응답의 주문번호 필드는 아직 못 봤다.** 성공 응답은 장중에만 나온다.

`ka10001` 이 상하한가를 직접 준다는 것도 알았다 — `upl_pric` / `lst_pric`.
지정가 검증에 쓸 수 있다.

### (참고) 조사 당시 기록 — 간접 근거뿐이었다

"모의투자에서 주문 API 가 된다" 는 명시적 문장을 찾지 못했다. 공식 포털의
API 가이드가 SPA 라 본문이 안 열리고 모의투자 지원/미지원 표는 공개 페이지에
없다.

간접 근거는 강하다.

- 키움 공식 안내가 "프로그램 디버깅·테스트를 위해 실제 환경 적용 전에
  모의투자로 충분히 테스트하라" 고 한다. 프로그램 테스트의 핵심은 주문이다
- 키움 **공식 클라이언트 저장소**가 `KIWOOM_MODE=demo/real` 전환과
  `examples/국내주식/주문/` 예제를 함께 제공한다
- 지원 API 목록에 "주문" 이 포함돼 있다

**단정하지 않는다.** 구 Open API+ 에는 모의투자 미지원 TR 목록이 따로
있었다. REST 에도 없다고 확신할 근거가 없다.
- [x] 엔진 운영 시간 (2026-08-30 확인 사항) — 아래 시간표

## 엔진 하루 시간표 (2026-08-30 확인 사항)

**24시간 상주 프로세스 하나.** 내부 시각표로 아래를 한다.
`telegram` 수집기가 이미 같은 패턴이라 운영 방식을 그대로 따른다.

| 시각 | 하는 일 |
|---|---|
| 08:30 | 전날 19:00 에 계획한 주문을 **동시호가 시장가**로 제출 |
| 09:00~15:30 | 체결 추적, `command` 폴링, `heartbeat` |
| 15:30 | 미체결 잔량 **취소** |
| 19:00 이후 | 일봉 적재 확인 → `scan`·`manage` → 주문 계획 기록 |
| 그 외 | `heartbeat` 만 |

### 왜 19:00 인가

백테스트는 `price_daily` 의 일봉을 본다. **`LiveFeed` 도 같은 것을 봐야
갈라지지 않는다** (`PROJECT.md` 8.3). 일봉 수집기가 19:00 에 도니 그 전에는
그날 종가가 DB 에 없다.

15:40 에 브로커 API 로 직접 받는 안도 있었지만, 데이터 경로가 갈리고
200종목 × 120봉을 매일 받아야 한다. **체결은 어차피 다음 날 시가라
서둘러도 얻는 것이 없다.**

### 왜 동시호가 시장가인가

백테스트가 "다음 날 **시가** 체결" 을 가정한다. 09:00 개장 후에 내면 시가가
아니라 그때 가격에 체결된다. 시가로 체결되려면 08:30~09:00 장 시작
동시호가에 넣어야 한다.

슬리피지가 크지만 **그것이 바로 `slippage_rate` 임시값 0.1% 를 실측하는
기회다.** Phase 8 산출물 중 가장 값진 것이 이 실측치가 된다.

### 파생되는 것들

- **미체결은 취소한다.** 백테스트에 미체결이라는 상태가 없다. 시가가 없으면
  주문을 취소하는 백테스트 `_settle` 과 같은 처리다. 다음 주기에 `scan` 이
  다시 만든다
- **일봉 수집이 실패하면 그날 판단을 건너뛴다.** '데이터가 없어서 안 샀다'
  는 안전한 실패다. 오래된 데이터로 판단하는 것보다 낫다
- **일봉 적재 여부는 `price_daily` 를 직접 본다.** heartbeat 는 프로세스가
  돌았다는 뜻이지 데이터가 들어왔다는 뜻이 아니다
- **수량은 19:00 종가로 계산하는데 체결은 다음 날 시가다.** 갭상승하면 현금이
  모자란다. 백테스트 `_buy` 의 감량 처리와 같은 것이 실전에도 필요하다

### 아직 안 정한 것

- [ ] `command` 폴링 주기 — `telegram` 이 60초다. 맞출지
- [ ] 계좌 자산을 브로커 잔고 그대로 쓸지, `accounts.yaml` 의 `allocation`
      으로 상한을 둘지. 지금 `allocation` 을 읽는 코드가 없다
- [ ] 장 마감 후 미체결 취소와 19:00 판단 사이에 할 일이 있는지
      (`daily_pnl` 스냅샷 시각)

## 절대 규칙 — 이 단계에서 가장 위험하다

여기서부터 **주문이 나간다.** 아래를 어기는 코드는 쓰지 않는다.

- **주문을 자동 재시도하지 않는다** (CLAUDE.md 3). 응답을 못 받으면
  `get_order_status` 로 실제 접수 여부를 확인한 뒤 판단한다
- **`client_order_id` 를 주문 전에 DB 에 기록한다** (`INTERFACES.md` 2.1).
  기록이 먼저다. 순서가 바뀌면 중복 주문을 못 막는다
- **엔진은 포털의 HTTP 응답을 기다리지 않는다.** 통신은 DB(`heartbeat`,
  `command`)로만 한다 (CLAUDE.md 8)
- **테스트는 `is_paper=TRUE` 계좌 또는 목으로만 돌린다.** 테스트 코드에
  실계좌 ID 를 쓰지 않는다
- 주문 로그에 계좌번호 전체를 남기지 않는다 (마스킹)
- **장중 재시작 금지.** 배포는 15:40 이후 (CLAUDE.md 배포)

## 1단계 — 브로커 API

`get_order_status` 와 `cancel_order` 가 `NotImplementedError` 로 남아 있다
(`common/broker/kiwoom.py`).

**읽기 전용부터 만든다** (2026-08-30 순서 변경). 모의투자에서 주문이 되는지
확실하지 않은데, 계좌에 묶인 조회 API 가 돌면 불확실성이 크게 줄어든다.
위험 없는 것으로 먼저 확인한다.

- [x] `common/broker/probe.py` — 실제 응답을 떠보는 도구 (2026-08-30).
      **문서만 보고 필드를 정하면 조용히 틀린 값을 읽는다.** 다른 API 도
      전부 실측으로 정했다. 주문 응답 샘플을 고정할 때도 같은 도구를 쓴다
- [x] **모의투자에서 계좌 API 가 동작한다** (2026-08-30). `kt00001`·`kt00018`
      둘 다 `mockapi.kiwoom.com` 에서 응답한다. 예수금 1,000만원
- [x] `get_balance` — `kt00001` + `kt00018` 을 합친다. 테스트 5건
- [x] `get_positions` — 실측으로 구현 (2026-08-31)
- [ ] `get_order_status` — 역시 조회다. **조회용 api-id 를 아직 모른다**
- [x] `submit_order` — 실측 규격으로 구현 (2026-08-31). 요청부는 확정,
      **성공 응답의 주문번호 필드만 미확정**이다
- [ ] `cancel_order`
- [x] **주문은 재시도 경로를 타지 않는다** — `_call_once` 를 분리했다.
      `probe.py` 도 이쪽을 쓴다. 이 도구로 시험주문을 낼 예정이라 재시도가
      남아 있으면 주문이 두 번 나간다
- [x] **응답 파싱 테스트 — 실제 응답 샘플을 고정한다** (CLAUDE.md 필수)
      — 계좌 API 둘과 **주문 거부 응답**을 고정했다. 주문 성공 응답은 아직
- [ ] `TransientError` / `PermanentError` 구분 (`INTERFACES.md` 2.3)

**주문이 실제로 되는지는 한 번 내봐야 안다.** 모의투자 계좌에서 사용자가
직접 소량으로 시험한다. 조회가 통과한 뒤에 한다.

### 다음에 할 것 — 장중 시험주문 (장이 열려야 한다)

19:06 에 던진 주문은 `모의투자 장종료` 로 막혔다. **성공 응답은 장중에만
나온다.** 다음 개장 때 아래를 낸다.

```
kt10000 /api/dostk/ordr
{"dmst_stex_tp":"KRX","stk_cd":"005930","ord_qty":"1",
 "ord_uv":"<하한가>","trde_tp":"0"}
```

**하한가 지정가 1주다.** 정상 접수되면서 체결되지 않는다. 하한가는
`ka10001` 의 `lst_pric` 이 준다 (2026-08-31 기준 180,000).

이 한 건으로 남은 셋을 전부 실측한다.

- `submit_order` 성공 응답의 주문번호 필드
- 미체결로 남아 있는 동안 `get_order_status`
- 그 뒤 `cancel_order`

### 계좌 API 실측 (2026-08-30)

| 항목 | 값 |
|---|---|
| 경로 | `/api/dostk/acnt` (POST, JSON) |
| `kt00001` | 예수금상세현황요청 |
| `kt00018` | 계좌평가잔고내역요청 |

둘 다 `qry_tp` 가 필수다. 빈 바디로 던지면 키움이 무엇이 빠졌는지 알려준다.

| 쓰는 필드 | 어디서 | 뜻 |
|---|---|---|
| `entr` | kt00001 | 예수금 |
| `100stk_ord_alow_amt` | kt00001 | **증거금 100% 기준** 주문가능금액 |
| `tot_evlt_amt` | kt00018 | 총평가금액 |
| `prsm_dpst_aset_amt` | kt00018 | 추정예탁자산 |
| `acnt_evlt_remn_indv_tot` | kt00018 | 보유종목 배열 |
| `stk_cd` | 배열 | 종목코드. **`A005930` 처럼 접두어가 붙는다** |
| `rmnd_qty` | 배열 | 보유수량 |
| `pur_amt` + `pur_cmsn` | 배열 | 평단가의 분자. 수수료를 포함한다 |

**금액은 15자리 제로패딩 문자열이다.** `"000000010000000"` 이 1,000만원.

**`ord_alow_amt` 를 쓰지 않는다.** 증거금률별로 금액이 나뉘어 오는데
`20stk_ord_alow_amt` 는 5,000만원이다. 미수를 쓰지 않으므로 증거금 100%
기준만 본다. 그대로 쓰면 실계좌에서 미수가 난다.

**계좌번호를 보내지 않는다.** 계좌는 앱키·토큰에 묶인다.

### 보유종목 실측에서 나온 것 둘 (2026-08-31)

**종목코드에 접두어가 붙는다.** `kt00018` 만 `A005930` 처럼 준다. 시세 API 는
`005930` 을 그대로 준다. 벗기지 않으면 `KRX:A005930` 이 되어 DB 의 어느
종목과도 안 맞는다.

**모의투자 수수료율이 실전과 다르다.**

| 종목 | 매입금액 | `pur_cmsn` | 비율 |
|---|---|---|---|
| 삼성전자 | 250,250 | 870 | 0.35% |
| 서원 | 11,160 | 30 | 0.35% |
| 지아이에스 | 18,650 | 60 | 0.35% |

`sell_cmsn` 910 ÷ 평가금액 260,000 = 정확히 0.35%. 10원 미만은 절사한다.
**실전 키움 온라인은 0.015% 다. 모의투자가 23배 비싸다.**

→ **모의투자 손익을 백테스트와 직접 비교하지 않는다.** 슬리피지 실측
(체결가만 본다)에는 지장이 없지만 손익 대조는 왜곡된다.

**평단가에 매입수수료를 포함한다.** 백테스트 `Portfolio` 와 같은 정의다.
맞추지 않으면 같은 전략이 백테스트와 실전에서 다른 손절가를 본다.

## 2단계 — 중복 주문 방지

- [ ] `client_order_id` 를 주문 **전에** 기록한다
- [ ] **중복 `client_order_id` 가 차단되는 테스트** (CLAUDE.md 필수)
- [ ] 응답 없음 → `get_order_status` 로 확인. **재시도하지 않는다**

## 3단계 — LiveFeed

- [ ] `DataFeed` 규격 그대로 구현한다. **백테스트와 같은 전략 코드가 돈다**
- [ ] `now()` 는 실제 시각이다. 전략 코드는 바뀌지 않는다
- [ ] `get_universe` — 백테스트와 같은 기준(평소 유동성)을 쓰는지 확인한다.
      다르면 백테스트와 실전이 갈라진다

## 4단계 — engine-swing 프로세스

- [ ] 하루 주기 루프. 백테스트 루프와 순서가 같아야 한다
- [ ] `command` 폴링 — 정지, 진입차단, 청산
- [ ] `heartbeat` 기록
- [ ] 재시작 복구 — **미체결 주문 대조**
- [ ] `position` 대조 — 브로커 잔고와 DB 가 어긋나면 멈춘다
- [ ] `daily_pnl` 스냅샷
- [ ] systemd 유닛 (`deploy/`)

## 5단계 — 포털 화면

- [ ] 자동매매 탭 — 상태, 포지션, 제외·허용 목록
- [ ] **비상 중단 버튼** — 진입차단 / 전량청산(2단계 확인) 둘로 나뉜다
- [ ] 버튼은 `command` 에 기록만 한다. 엔진을 직접 부르지 않는다

## Phase 8 완료 기준 — 모의투자 계좌에서

- [ ] 매수 → 부분체결 → 전량체결 → 매도 전 과정 정상
- [ ] 주문 취소 정상
- [ ] 엔진 강제 종료 후 재시작 시 미체결 주문 복구
- [ ] 비상 중단 버튼 동작
- [ ] **최소 2주 무사고 운영**

---

## 사용자 조치 필요

- [ ] **슬리피지 0.1% 는 임시값이다.** Phase 8 모의투자에서 실제 체결가와
      대조해 조정한다. 백테스트 결과가 통째로 이 값에 걸려 있다
- [ ] 모의투자 계좌 정보 (`config/accounts.yaml`)

## 관측 대기 (Phase 4·5 에서 이월)

- [ ] 일봉·지표·국면 heartbeat — 매일 19:00
- [ ] 분봉·수급·플래그 heartbeat — 08-31 16:10 이 첫 발화
- [ ] 분봉 자동 적재 — 지금까지 쌓인 것은 손으로 돌린 결과다
- [ ] `stock_flags` 플래그 생존 — KRX 마스터가 돈 뒤에도 남는지
- [ ] Claude Design 결과물 연결 — 산출물이 나오면 템플릿만 바꾼다
- [ ] 실제 데이터로 키워드 병합을 한 번 해본다
- [ ] 백테스트 표 휴대폰 폭 확인 — 열이 다섯이라 실제로 봐야 안다

## 미룬 것

- [ ] `price_daily(trade_date)` 인덱스 — 백테스트 유니버스 조회가 매일 220만
      행을 전체 스캔한다 (2분 16초 → 9분 36초). Phase 7 을 재개하면 먼저 넣는다.
      마이그레이션 `003` 이 필요하고 스키마 변경이라 확인이 필요하다

## 건드리지 않은 것

- 서버 `~/stock-portal/ion` — 정체불명 파일(398바이트, 8/25). 내 작업과 무관하다
- `docs/INTERFACES.md` 코드블록이 `ruff format` 기준에 걸린다. 기존부터 그랬다
