# checklist.md — Phase 7 전략 개발·검증

> **지금 할 일: 4단계 개발 구간 기준선.** 속도는 문제없다.
>
> **파라미터를 아직 만지지 않는다.** 기준선과 진단이 먼저다.
>
> **여기서 가장 많은 시간을 쓴다** (`ROADMAP.md`). 서둘러 넘어갈 단계가 아니다.
> Phase 6 체크리스트는 커밋 `f8025b9` 이전 이력에 있다.

## 시작 전에 정한 것 (2026-08-30)

- **추세 추종으로 간다** (확인 사항). 오르는 것을 사서 꺾이면 판다.
  일봉만으로 만들 수 있어 피드를 넓히지 않아도 된다
- **앞 2년 개발 / 뒤 1년 검증** (확인 사항). 검증 구간은 파라미터를 정할 때
  한 번도 보지 않는다. 한 번이라도 보고 고치면 그 순간 검증이 아니다
- 국면은 전략이 보지 않는다. `RiskManager` 가 이미 국면별로 금액을 줄인다.
  전략이 또 보면 두 번 적용된다

### 데이터 범위 (2026-08-30 실측)

`price_daily` 2023-08-29 ~ 2026-08-28, **729 거래일**, 3,083 종목.

| 구간 | 날짜 | 비고 |
|---|---|---|
| 개발 | 2023-08-29 ~ 2025-08-28 | 앞 120 거래일(~2024-02)은 `ma_long` 예열. 신호가 없다 |
| 검증 | 2025-08-29 ~ 2026-08-28 | 시작 시점에 뒤로 2년이 있어 예열 손실이 없다 |

실질 개발 표본은 약 1년 반이다. **스윙은 매매가 드물어 표본이 적다.**

### 전략이 볼 수 있는 것

| 주는 것 | 내용 |
|---|---|
| `get_candles` | 일봉 조정가 (OHLCV) |
| `get_quote` | 현재가 원주가 |
| `get_universe` | 그 시점 거래대금 상위 |
| `get_regime` | 시장 국면 (2026-08 이전은 전부 중립) |

**수급(`trading_flow`)과 지표 8종은 피드에 통로가 없다.** 쓰려면
`INTERFACES.md` 3장을 고쳐야 한다. 이번 전략은 일봉만 쓴다.

---

## 1단계 — 규칙 확정

- [x] **진입·청산 규칙 승인** (2026-08-30 확인)

제안한 것은 아래 넷이다. **조건 하나가 곧 과최적화할 손잡이 하나다.**

진입 — 둘 다 만족할 때
1. 오늘 종가가 직전 `breakout_days` 일 최고가를 넘는다
2. 종가가 `ma_long` 이평 위에 있다

청산 — 하나라도 걸리면
1. 종가가 `ma_exit` 이평을 밑돈다
2. 진입가 대비 `stop_loss` 만큼 빠진다

추적 손절을 따로 두지 않았다. **이평 이탈이 이미 그 역할을 한다.**
둘 다 두면 파라미터만 늘고 서로 간섭한다.

- [x] 초기 파라미터를 `config/strategy_swing.yaml` 에 둔다. **전부 임시값이다**

| 파라미터 | 초기값 | 뜻 |
|---|---|---|
| `breakout_days` | 60 | 3개월 신고가 |
| `ma_long` | 120 | 6개월 추세 |
| `ma_exit` | 20 | 1개월 이평 이탈 시 청산 |
| `stop_loss` | 0.07 | 진입가 -7% |
| `entries_per_day` | 5 | 하루 신규 진입 후보 상한 |

## 2단계 — 구현

- [x] `common/strategy/swing.py` — `Strategy` 규격을 그대로 따른다 (2026-08-30)
- [x] 수량을 정하지 않는다. `EntryIntent` 에 수량 칸이 없다 (4.2)
- [x] 코드에 숫자를 쓰지 않는다. 전부 `ctx.params` (4.3)
- [x] `strength` 는 돌파 폭(직전 최고가 대비 초과 비율). 새 파라미터가 없다
- [x] **손절은 원주가끼리, 추세는 조정가끼리 견준다** (3.3). 섞으면 안 된다
- [x] 규격 테스트 15건 — DB 없이 스텁 피드로 돈다
- [x] CLI `STRATEGIES` 에 등록

### 구현 중 찾은 결함 — 권리락에 보유 수량이 안 맞았다

`Portfolio` 가 권리락에 아무것도 하지 않았다. 20,000원 10주가 분할로
10,000원 20주가 되어야 하는데 10주 그대로 남아 **평가액이 그날 절반으로
증발**했다. `PROJECT.md` 11장이 지목한 "분할일이 급락으로 보여 손절이 대량
발동한다" 가 실제로 일어나는 상태였다.

더미는 하루만 들고 있어 드러나지 않았다. 스윙은 몇 주를 들고 있어 드러난다.

- [x] `Portfolio.adjust` — 수량과 평단가를 반비례로. **평가액은 그대로다**
- [x] `Market.adjustments(day)` · `adjustments_on` 조회
- [x] 루프가 체결보다 **먼저** 반영한다. 권리락은 그날 시가부터다
- [x] 테스트 6건 + DB 테스트 1건

## 3단계 — 속도 측정 ✅ (2026-08-30)

- [x] **먼저 재봤다.** 미리 최적화하지 않았다
- [x] 20 거래일에 7.8초. 2년치 약 3.2분
- [x] **고치지 않는다.** 묶어 읽을 이유가 없다

재보지 않고 고쳤으면 필요 없는 복잡도를 넣을 뻔했다. 종목당 한 번씩
쿼리하는 지금 방식으로 충분하다.

## 4단계 — 개발 구간 반복

- [x] 초기값으로 한 번 돌려 기준선을 만든다 (`run_id 23`, 2026-08-30)
- [x] **매매 단위로 진단한다.** 지표만 보면 왜 그런지 모른다
- [x] 손절 실험 둘 (2026-08-30). **손절은 방파제다. 넓히면 나빠진다**
- [ ] 유니버스 표류 측정 ← 지금 여기
- [ ] 파라미터 조정. **검증 구간은 열지 않는다**
- [ ] 매매 표본 수를 함께 본다. 표본이 적으면 지표에 뜻이 없다
- [ ] 조정 이력을 `context-notes.md` 에 남긴다. 무엇을 왜 바꿨는지

### 개발 구간 기준선 (2026-08-30, `run_id 23`)

2023-08-29 ~ 2025-08-28, 486 거래일, 2분 16초.

| 항목 | 값 |
|---|---|
| 총수익률 | **-50.88%** |
| MDD | 53.01% |
| 승률 | 23.77% |
| 매매 | 355건 (청산 345, 미청산 10) |

#### 구조는 옳다

| 구분 | 건수 | 평균 | 평균보유일 |
|---|---|---|---|
| 이익 | 82 | +19.70% | 35.4일 |
| 손실 | 263 | -9.34% | 9.4일 |

**손실은 9일에 끊고 이익은 35일 들고 간다.** 추세 추종이 원하는 비대칭이
그대로 나온다. 최대 이익 +142.71% 로 우측 꼬리도 살아 있다.

| 사유 | 건수 | 평균수익률 | 평균보유일 |
|---|---|---|---|
| `signal` (이평 이탈) | 129 | **+11.11%** | 29.5 |
| `stop` (손절) | 216 | **-10.53%** | 7.3 |

#### 진짜 문제 — 손절이 -7% 가 아니다

`stop_loss` 는 0.07 인데 실현 손실은 평균 **-10.53%** 다. 3.5%p 가 더 빠진다.

버그가 아니라 설계다. 종가에 -7% 를 확인하고 **다음 날 시가에 판다.** 그
사이 갭하락이 얹힌다. 미래 참조를 막으려 택한 체결 모델의 대가가 여기서
청구된다.

#### 산수

승률 23.77%, 손익비 19.70/9.34 = **2.11배**. 본전에 필요한 것은 **3.21배**,
곧 이익 평균 **+30%** 다. 매매당 기대값 **-2.44%** × 345건 = -50%.

#### 틀린 의심을 지운다

한 달 결과로 "거래대금 순서 때문에 고점을 산다" 를 의심했다. **이익 평균
+19.7%, 최대 +142% 가 나오는데 고점만 사고 있을 수는 없다.** 주된 원인이
아니다.

### 손절 실험 (2026-08-30)

| 실행 | `stop_loss` | 총수익률 | MDD | 승률 | 매매 |
|---|---|---|---|---|---|
| 23 | 0.07 | **-50.88%** | 53.01% | 23.77% | 355 |
| 31 | 0.15 | -59.85% | 62.81% | 26.98% | 286 |
| 30 | 0.99 (끔) | **-67.16%** | 69.72% | 24.61% | 268 |

**손절은 해악이 아니라 방파제다.** 넓힐수록 나빠진다. 손절폭은 건드릴
자리가 아니다. 가설이 틀렸다.

#### 단조성이 말하는 것

위험을 빨리 끊을수록 좋아진다는 것은 **진입에 우위가 없다**는 뜻이다.
우위가 있으면 손절폭에 중간 최적점이 생긴다. 너무 좁으면 이길 매매를
죽이고 너무 넓으면 손실이 커지기 때문이다.

좁을수록 좋기만 하다면 재고 있는 것은 "덜 참여할수록 덜 잃는다" 이고
그 끝은 "안 사는 것" 이다.

**`stop_loss` 를 0.03 으로 내리면 숫자는 더 좋아진다. 하지 않는다.**
그건 전략이 아니라 매매를 줄이는 것이고 정확히 과최적화다.

### 한 달 시험 결과 (2026-08-30, `run_id 21`)

2025-03 한 달. **파라미터를 정하는 데 쓰지 않는다.** 표본이 아니다.

| 항목 | 값 |
|---|---|
| 총수익률 | -15.17% |
| 승률 | 8.33% (24건 중 2건) |
| 매매 | 32건 (체결 56건) |

**같은 구간 더미는 -0.74% 였고 시세는 올랐다.** 시장이 오르는 달에 추세
추종이 15% 를 잃었다. 승률 8.33% 는 나쁜 전략치고도 낮다.

의심하는 것 — **진입 후보를 거래대금 순서대로 채운 것.** 돌파일에 거래대금이
가장 큰 종목은 방금 급등한 종목이고, 다음 날 시가에 사면 고점 매수가 된다.
안전한 선택이라 생각했는데 반대일 수 있다.

**추측이다. 2년치 기준선과 매매 단위 진단으로 확인한 뒤 판단한다.**
한 달 24건에 맞춰 손절폭을 넓히는 것이 곧 과최적화다.

## 5단계 — 검증 구간

- [ ] **딱 한 번 돌린다.** 결과가 나쁘면 파라미터가 아니라 규칙을 다시 본다
- [ ] 개발 구간과 검증 구간의 지표를 나란히 놓는다
- [ ] 차이가 크면 과최적화다. 인정하고 되돌린다

## 6단계 — 유니버스 확정

- [ ] `config/universe.yaml` — 지금은 `limits.yaml` 의 `universe_size` 뿐이다
- [ ] 거래대금 하한, 가격 하한 같은 것을 넣을지 판단한다
- [ ] 넣는 조건마다 **왜 넣는지**를 적는다. 근거 없는 필터는 과최적화다

### 유니버스가 오염돼 있을 수 있다 (가설, 측정 대기)

`universe_at` 은 **그날의 거래대금 상위**를 뽑는다.

```sql
WHERE p.trade_date = %s ... ORDER BY p.value DESC
```

돌파일은 거래대금이 폭증하는 날이다. 즉 **돌파했기 때문에 유니버스에 들어온
종목**을 고르고 있을 수 있다. 유니버스 선정이 매매하려는 사건 자체에
오염된다.

스윙은 그날 거래대금이 아니라 **평소 유동성**으로 유니버스를 잡아야 한다.
진입 규칙 변경이 아니므로 승인받은 규칙을 건드리지 않는다.

**가설이다.** 한 달 표본으로 세운 가설이 이미 한 번 틀렸다. 재고 판단한다.

## Phase 7 완료 기준

- [ ] 수수료·슬리피지 반영 후에도 유의미한 결과
- [ ] 여러 기간에서 일관된 성과

**아슬아슬하게 통과하는 결과는 통과하지 못한 것으로 본다** (`ROADMAP.md`).
기준을 낮추고 싶어지면 이 줄을 읽는다.

---

## 결과를 볼 때 기억할 것

숫자를 낙관 쪽으로 미는 것이 셋 있다. **틀이 돈다는 것과 결과를 믿을 수
있다는 것은 다르다.**

| 항목 | 상태 |
|---|---|
| 생존편향 | 없앨 수 없다. 이미 폐지된 종목이 데이터에 없다 |
| 슬리피지 0.1% | 실측이 아니다. Phase 8 모의투자에서 대조한다 |
| 국면별 자금 배분 | `danger 0.3 / neutral 0.7 / safe 1.0`. 근거 없는 임시값 |

## 사용자 조치 필요

- [ ] **슬리피지 0.1% 는 임시값이다.** 실측 없이 정할 수 없다.
      Phase 8 모의투자에서 실제 체결가와 대조해 조정한다

## 관측 대기 (Phase 4·5 에서 이월)

- [ ] 일봉·지표·국면 heartbeat — 매일 19:00
- [ ] 분봉·수급·플래그 heartbeat — 08-31 16:10 이 첫 발화
- [ ] 분봉 자동 적재 — 지금까지 쌓인 것은 손으로 돌린 결과다
- [ ] `stock_flags` 플래그 생존 — KRX 마스터가 돈 뒤에도 남는지
- [ ] Claude Design 결과물 연결 — 산출물이 나오면 템플릿만 바꾼다
- [ ] 실제 데이터로 키워드 병합을 한 번 해본다
- [ ] 백테스트 표 휴대폰 폭 확인 — 열이 다섯이라 실제로 봐야 안다

## 건드리지 않은 것

- 서버 `~/stock-portal/ion` — 정체불명 파일(398바이트, 8/25). 내 작업과 무관하다
- `docs/INTERFACES.md` 코드블록이 `ruff format` 기준에 걸린다. 기존부터 그랬다
